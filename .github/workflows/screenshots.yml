name: Screenshots

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  screenshots:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            os_name: linux
          - os: macos-latest
            os_name: macos
          - os: windows-latest
            os_name: windows

    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v6

      - uses: dtolnay/rust-toolchain@stable

      - uses: Swatinem/rust-cache@v2

      - name: Install system dependencies (Linux)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y xvfb libxkbcommon-dev libxkbcommon-x11-dev libvulkan-dev mesa-vulkan-drivers

      - name: Grant screen recording permission (macOS)
        if: runner.os == 'macOS'
        run: |
          # screencapture -l needs kTCCServiceScreenCapture on macOS 14+
          # Grant to the shell process so the showcase can invoke screencapture
          sudo sqlite3 "/Library/Application Support/com.apple.TCC/TCC.db" \
            "INSERT OR REPLACE INTO access (service, client, client_type, auth_value, auth_reason, auth_version) \
             VALUES ('kTCCServiceScreenCapture', '/bin/bash', 1, 2, 4, 1);"
          sudo sqlite3 "/Library/Application Support/com.apple.TCC/TCC.db" \
            "INSERT OR REPLACE INTO access (service, client, client_type, auth_value, auth_reason, auth_version) \
             VALUES ('kTCCServiceScreenCapture', '/bin/zsh', 1, 2, 4, 1);"

      - name: Build iced showcase (release)
        run: cargo build -p native-theme-iced --example showcase --release

      - name: Build gpui showcase (macOS and Windows)
        if: runner.os == 'macOS' || runner.os == 'Windows'
        run: cargo build -p native-theme-gpui --example showcase --release

      - name: Create output directory
        run: mkdir -p docs/assets
        shell: bash

      - name: Capture screenshots (Linux)
        if: runner.os == 'Linux'
        run: |
          # Linux-native presets with matching icon sets
          # Format: theme:variant:icon-set
          themes=(
            "kde-breeze:dark:freedesktop"
            "kde-breeze:light:freedesktop"
            "material:dark:material"
            "material:light:material"
            "catppuccin-mocha:dark:lucide"
            "catppuccin-mocha:light:lucide"
          )
          for entry in "${themes[@]}"; do
            IFS=':' read -r theme variant icon_set <<< "$entry"
            echo "Capturing: $theme $variant (icons: $icon_set)"
            xvfb-run -a --server-args="-screen 0 1920x1080x24" \
              cargo run -p native-theme-iced --example showcase --release -- \
                --theme "$theme" --variant "$variant" --icon-set "$icon_set" \
                --tab buttons \
                --screenshot "docs/assets/iced-${{ matrix.os_name }}-${theme}-${variant}.png"
          done

      - name: Capture screenshots (macOS)
        if: runner.os == 'macOS'
        run: |
          # macOS-native preset only (community themes already captured on Linux)
          # OS icon set (SF Symbols) via --icon-set system
          themes=(
            "macos-sonoma:light:system"
            "macos-sonoma:dark:system"
          )
          for entry in "${themes[@]}"; do
            IFS=':' read -r theme variant icon_set <<< "$entry"
            echo "Capturing: $theme $variant (icons: $icon_set)"
            cargo run -p native-theme-iced --example showcase --release -- \
              --theme "$theme" --variant "$variant" --icon-set "$icon_set" \
              --tab buttons \
              --screenshot "docs/assets/iced-${{ matrix.os_name }}-${theme}-${variant}.png"
          done

      - name: Capture screenshots (Windows)
        if: runner.os == 'Windows'
        shell: bash
        run: |
          # Windows-native preset only (community themes already captured on Linux)
          # OS icon set (Segoe Fluent) via --icon-set system
          themes=(
            "windows-11:light:system"
            "windows-11:dark:system"
          )
          for entry in "${themes[@]}"; do
            IFS=':' read -r theme variant icon_set <<< "$entry"
            echo "Capturing: $theme $variant (icons: $icon_set)"
            cargo run -p native-theme-iced --example showcase --release -- \
              --theme "$theme" --variant "$variant" --icon-set "$icon_set" \
              --tab buttons \
              --screenshot "docs/assets/iced-${{ matrix.os_name }}-${theme}-${variant}.png"
          done

      - name: Capture gpui screenshots (macOS)
        if: runner.os == 'macOS'
        run: |
          # gpui showcase self-captures with window decorations via screencapture -l
          themes=(
            "macos-sonoma:light:system:"
            "macos-sonoma:dark:system:"
          )
          for entry in "${themes[@]}"; do
            IFS=':' read -r theme variant icon_set icon_theme <<< "$entry"
            echo "Capturing gpui: $theme $variant"
            cargo run -p native-theme-gpui --example showcase --release -- \
              --theme "$theme" --variant "$variant" --icon-set "$icon_set" \
              --tab buttons \
              --screenshot "docs/assets/gpui-${{ matrix.os_name }}-${theme}-${variant}.png"
          done

      - name: Capture gpui screenshots (Windows)
        if: runner.os == 'Windows'
        shell: bash
        run: |
          # gpui showcase self-captures with window decorations via BitBlt
          themes=(
            "windows-11:light:system:"
            "windows-11:dark:system:"
          )
          for entry in "${themes[@]}"; do
            IFS=':' read -r theme variant icon_set icon_theme <<< "$entry"
            echo "Capturing gpui: $theme $variant"
            cargo run -p native-theme-gpui --example showcase --release -- \
              --theme "$theme" --variant "$variant" --icon-set "$icon_set" \
              --tab buttons \
              --screenshot "docs/assets/gpui-${{ matrix.os_name }}-${theme}-${variant}.png"
          done

      - name: Upload screenshots
        uses: actions/upload-artifact@v7
        with:
          name: screenshots-${{ matrix.os_name }}
          path: |
            docs/assets/iced-${{ matrix.os_name }}-*.png
            docs/assets/gpui-${{ matrix.os_name }}-*.png
