# Property Registry — machine-readable source of truth for widget fields.
#
# Derived from docs/platform-facts.md Chapter 2 (§2.1–§2.28).
# Property names match the platform-facts.md table first-column entries exactly.
#
# Each widget lists ALL fields from its platform-facts table. A Rust test
# should verify every field in this file exists in the corresponding struct,
# and every field in the struct exists in this file.
#
# Source: docs/platform-facts.md  (2026-04-06 snapshot)
# Struct: native-theme/src/model/

# ═══════════════════════════════════════════════════════════════════════════
# Structure Definitions
# ═══════════════════════════════════════════════════════════════════════════
#
# Composite types used in property definitions below. Scalar types:
#   "color"  — Rgba color value
#   "f32"    — 32-bit float (dimensions in logical pixels, ratios)
#   "u16"    — 16-bit unsigned int (CSS font-weight: 400=Normal, 700=Bold)
#   "bool"   — boolean
#   "string" — UTF-8 string
#   "enum"   — enumerated value (variants noted in comments)
#
# Sub-struct types (defined below, referenced by name in widgets):
#   "font"             — see _structures.Font
#   "border"           — see _structures.Border
#   "icon_sizes"       — see _structures.IconSizes
#   "text_scale_entry" — see _structures.TextScaleEntry

[_structures.Font]
# Font specification (§2 naming: family + size + weight + style + color).
# Widgets reference this as `font = "font"` or with a named prefix like
# `title_bar_font = "font"`, `body_font = "font"`, `item_font = "font"`, etc.
family                       = "string"
size                         = "f32"
weight                       = "u16"
style                        = "enum"   # Normal | Italic | Oblique
color                        = "color"

[_structures.Border]
# Frame/border sub-struct. Widgets reference as `border = "border"`.
# Not every widget populates every field — platform-facts documents
# which fields have actual platform values per widget.
color                        = "color"
corner_radius                = "f32"
corner_radius_lg             = "f32"    # defaults only
line_width                   = "f32"
opacity                      = "f32"    # defaults only
shadow_enabled               = "bool"
padding_horizontal           = "f32"
padding_vertical             = "f32"

[_structures.IconSizes]
# Platform icon size contexts, nested under defaults.
toolbar                      = "f32"
small                        = "f32"
large                        = "f32"
dialog                       = "f32"
panel                        = "f32"

[_structures.TextScaleEntry]
# One entry in the text scale ramp (§2.19).
size                         = "f32"
weight                       = "u16"
line_height                  = "f32"    # computed from defaults.line_height * size when None

# ═══════════════════════════════════════════════════════════════════════════
# §2.1 Global Defaults (ThemeDefaults)
# ═══════════════════════════════════════════════════════════════════════════

[defaults]
# §2.1.1 Base Font
font                         = "font"
line_height                  = "f32"
# §2.1.2 Monospace Font
mono_font                    = "font"
# §2.1.3 Base Colors (text_color = defaults.font.color — same value, kept for state-override reference)
background_color             = "color"
text_color                   = "color"
accent_color                 = "color"
accent_text_color            = "color"
surface_color                = "color"
muted_color                  = "color"
shadow_color                 = "color"
link_color                   = "color"
selection_background         = "color"
selection_text_color         = "color"
selection_inactive_background = "color"
text_selection_background    = "color"
text_selection_color         = "color"
disabled_text_color          = "color"
# §2.1.4 Status Colors
danger_color                 = "color"
danger_text_color            = "color"
warning_color                = "color"
warning_text_color           = "color"
success_color                = "color"
success_text_color           = "color"
info_color                   = "color"
info_text_color              = "color"
# §2.1.5 Focus Ring
focus_ring_color             = "color"
focus_ring_width             = "f32"
focus_ring_offset            = "f32"
# §2.1.6 Global Geometry (border.* fields are inside the Border sub-struct)
border                       = "border"
disabled_opacity             = "f32"
# §2.1.7 Accessibility
text_scaling_factor          = "f32"
reduce_motion                = "bool"
high_contrast                = "bool"
reduce_transparency          = "bool"
# §2.1.8 Icon Sizes
icon_sizes                   = "icon_sizes"
# NOTE: defaults.spacing (ThemeSpacing T-shirt scale xxs..xxl) exists in
# the code but is NOT a platform theme property. All platform spacing
# values are captured as direct per-widget fields in §2.3–§2.28, and as
# layout container defaults in [layout] (§2.20). The T-shirt scale should
# be removed from the code. See todo_v0.5.5.md Spacing Design Analysis.

# ═══════════════════════════════════════════════════════════════════════════
# §2.19 Text Scale (TextScale)
# ═══════════════════════════════════════════════════════════════════════════
# size and weight from §2.19 table.
# NOTE: Each entry also has a line_height in the code (computed from
# defaults.line_height * size when None). Listed in _structures above.

[text_scale]
caption                      = "text_scale_entry"
section_heading              = "text_scale_entry"
dialog_title                 = "text_scale_entry"
display                      = "text_scale_entry"

# ─── Top-level ThemeVariant fields (structural, not from §2.x tables) ────

[theme_variant]
icon_set                     = "enum"
icon_theme                   = "string"

# ═══════════════════════════════════════════════════════════════════════════
# §2.2 Window / Application Chrome
# ═══════════════════════════════════════════════════════════════════════════

[window]
background_color             = "color"
title_bar_background         = "color"
title_bar_font               = "font"
inactive_title_bar_background = "color"
inactive_title_bar_text_color = "color"
border                       = "border"

# ═══════════════════════════════════════════════════════════════════════════
# §2.3 Button
# ═══════════════════════════════════════════════════════════════════════════

[button]
background_color             = "color"
font                         = "font"
min_width                    = "f32"
min_height                   = "f32"
icon_text_gap                = "f32"
primary_background           = "color"
primary_text_color           = "color"
disabled_opacity             = "f32"
hover_background             = "color"
hover_text_color             = "color"
active_background            = "color"
active_text_color            = "color"
disabled_background          = "color"
disabled_text_color          = "color"
border                       = "border"

# ═══════════════════════════════════════════════════════════════════════════
# §2.4 Text Input
# ═══════════════════════════════════════════════════════════════════════════

[input]
background_color             = "color"
placeholder_color            = "color"
caret_color                  = "color"
selection_background         = "color"
selection_text_color         = "color"
font                         = "font"
min_height                   = "f32"
disabled_opacity             = "f32"
hover_border_color           = "color"
focus_border_color           = "color"
disabled_background          = "color"
disabled_text_color          = "color"
border                       = "border"

# ═══════════════════════════════════════════════════════════════════════════
# §2.5 Checkbox / Radio Button
# ═══════════════════════════════════════════════════════════════════════════

[checkbox]
background_color             = "color"
font                         = "font"
indicator_color              = "color"
indicator_width              = "f32"
label_gap                    = "f32"
checked_background           = "color"
disabled_opacity             = "f32"
hover_background             = "color"
disabled_background          = "color"
disabled_text_color          = "color"
unchecked_background         = "color"
unchecked_border_color       = "color"
border                       = "border"

# ═══════════════════════════════════════════════════════════════════════════
# §2.6 Menu
# ═══════════════════════════════════════════════════════════════════════════

[menu]
background_color             = "color"
separator_color              = "color"
font                         = "font"
row_height                   = "f32"
icon_text_gap                = "f32"
icon_size                    = "f32"
hover_background             = "color"
hover_text_color             = "color"
disabled_text_color          = "color"
border                       = "border"

# ═══════════════════════════════════════════════════════════════════════════
# §2.7 Tooltip
# ═══════════════════════════════════════════════════════════════════════════

[tooltip]
background_color             = "color"
font                         = "font"
max_width                    = "f32"
border                       = "border"

# ═══════════════════════════════════════════════════════════════════════════
# §2.8 Scrollbar
# ═══════════════════════════════════════════════════════════════════════════

[scrollbar]
track_color                  = "color"
thumb_color                  = "color"
thumb_hover_color            = "color"
groove_width                 = "f32"
min_thumb_length             = "f32"
thumb_width                  = "f32"
overlay_mode                 = "bool"
thumb_active_color           = "color"

# ═══════════════════════════════════════════════════════════════════════════
# §2.9 Slider
# ═══════════════════════════════════════════════════════════════════════════

[slider]
fill_color                   = "color"
track_color                  = "color"
thumb_color                  = "color"
track_height                 = "f32"
thumb_diameter               = "f32"
tick_mark_length             = "f32"
disabled_opacity             = "f32"
thumb_hover_color            = "color"
disabled_fill_color          = "color"
disabled_track_color         = "color"
disabled_thumb_color         = "color"

# ═══════════════════════════════════════════════════════════════════════════
# §2.10 Progress Bar
# ═══════════════════════════════════════════════════════════════════════════

[progress_bar]
fill_color                   = "color"
track_color                  = "color"
track_height                 = "f32"
min_width                    = "f32"
border                       = "border"

# ═══════════════════════════════════════════════════════════════════════════
# §2.11 Tab Bar
# ═══════════════════════════════════════════════════════════════════════════

[tab]
background_color             = "color"
active_background            = "color"
active_text_color            = "color"
bar_background               = "color"
min_width                    = "f32"
min_height                   = "f32"
hover_background             = "color"
hover_text_color             = "color"
font                         = "font"
border                       = "border"

# ═══════════════════════════════════════════════════════════════════════════
# §2.12 Sidebar
# ═══════════════════════════════════════════════════════════════════════════

[sidebar]
background_color             = "color"
font                         = "font"
selection_background         = "color"
selection_text_color         = "color"
hover_background             = "color"
border                       = "border"

# ═══════════════════════════════════════════════════════════════════════════
# §2.13 Toolbar
# ═══════════════════════════════════════════════════════════════════════════

[toolbar]
font                         = "font"
bar_height                   = "f32"
item_gap                     = "f32"
background_color             = "color"
icon_size                    = "f32"
border                       = "border"

# ═══════════════════════════════════════════════════════════════════════════
# §2.14 Status Bar
# ═══════════════════════════════════════════════════════════════════════════

[status_bar]
font                         = "font"
background_color             = "color"
border                       = "border"

# ═══════════════════════════════════════════════════════════════════════════
# §2.15 List / Table
# ═══════════════════════════════════════════════════════════════════════════

[list]
background_color             = "color"
item_font                    = "font"
alternate_row_background     = "color"
selection_background         = "color"
selection_text_color         = "color"
header_font                  = "font"
header_background            = "color"
grid_color                   = "color"
row_height                   = "f32"
hover_background             = "color"
hover_text_color             = "color"
disabled_text_color          = "color"
border                       = "border"

# ═══════════════════════════════════════════════════════════════════════════
# §2.16 Popover / Dropdown
# ═══════════════════════════════════════════════════════════════════════════

[popover]
background_color             = "color"
font                         = "font"
border                       = "border"

# ═══════════════════════════════════════════════════════════════════════════
# §2.17 Splitter
# ═══════════════════════════════════════════════════════════════════════════

[splitter]
divider_width                = "f32"
divider_color                = "color"
hover_color                  = "color"

# ═══════════════════════════════════════════════════════════════════════════
# §2.18 Separator
# ═══════════════════════════════════════════════════════════════════════════

[separator]
line_color                   = "color"
line_width                   = "f32"

# ═══════════════════════════════════════════════════════════════════════════
# §2.20 Layout Container Defaults
# ═══════════════════════════════════════════════════════════════════════════
#
# Default spacing for toolkit layout containers (QLayout, NSStackView,
# GtkBox, StackPanel). These are the values a layout manager uses when
# the developer does not specify explicit spacing.
#
# NOT YET IMPLEMENTED — struct and presets do not exist yet.
# Currently only documented in platform-facts.md §2.20.
# No platform exposes these as user-configurable settings; all values
# are compile-time constants or design guidelines.

[layout]
widget_gap                   = "f32"
container_margin             = "f32"
window_margin                = "f32"
section_gap                  = "f32"

# ═══════════════════════════════════════════════════════════════════════════
# §2.21 Switch / Toggle
# ═══════════════════════════════════════════════════════════════════════════

[switch]
track_width                  = "f32"
track_height                 = "f32"
thumb_diameter               = "f32"
track_radius                 = "f32"
checked_background           = "color"
unchecked_background         = "color"
thumb_background             = "color"
disabled_opacity             = "f32"
hover_checked_background     = "color"
hover_unchecked_background   = "color"
disabled_checked_background  = "color"
disabled_unchecked_background = "color"
disabled_thumb_color         = "color"

# ═══════════════════════════════════════════════════════════════════════════
# §2.22 Dialog
# ═══════════════════════════════════════════════════════════════════════════

[dialog]
background_color             = "color"
body_font                    = "font"
min_width                    = "f32"
max_width                    = "f32"
min_height                   = "f32"
max_height                   = "f32"
button_gap                   = "f32"
button_order                 = "enum"   # PrimaryRight | PrimaryLeft | KdeLayout | GnomeLayout
title_font                   = "font"
icon_size                    = "f32"
border                       = "border"

# ═══════════════════════════════════════════════════════════════════════════
# §2.23 Spinner / Progress Ring
# ═══════════════════════════════════════════════════════════════════════════

[spinner]
diameter                     = "f32"
min_diameter                 = "f32"
stroke_width                 = "f32"
fill_color                   = "color"

# ═══════════════════════════════════════════════════════════════════════════
# §2.24 ComboBox / Dropdown Trigger
# ═══════════════════════════════════════════════════════════════════════════

[combo_box]
background_color             = "color"
font                         = "font"
min_height                   = "f32"
min_width                    = "f32"
arrow_icon_size              = "f32"
arrow_area_width             = "f32"
disabled_opacity             = "f32"
hover_background             = "color"
disabled_background          = "color"
disabled_text_color          = "color"
border                       = "border"

# ═══════════════════════════════════════════════════════════════════════════
# §2.25 Segmented Control
# ═══════════════════════════════════════════════════════════════════════════

[segmented_control]
background_color             = "color"
font                         = "font"
active_background            = "color"
active_text_color            = "color"
segment_height               = "f32"
separator_width              = "f32"
disabled_opacity             = "f32"
hover_background             = "color"
border                       = "border"

# ═══════════════════════════════════════════════════════════════════════════
# §2.26 Card / Container
# ═══════════════════════════════════════════════════════════════════════════

[card]
background_color             = "color"
border                       = "border"

# ═══════════════════════════════════════════════════════════════════════════
# §2.27 Expander / Disclosure
# ═══════════════════════════════════════════════════════════════════════════

[expander]
font                         = "font"
header_height                = "f32"
arrow_icon_size              = "f32"
hover_background             = "color"
arrow_color                  = "color"
border                       = "border"

# ═══════════════════════════════════════════════════════════════════════════
# §2.28 Link
# ═══════════════════════════════════════════════════════════════════════════

[link]
font                         = "font"
visited_text_color           = "color"
underline_enabled            = "bool"
background_color             = "color"
hover_background             = "color"
hover_text_color             = "color"
active_text_color            = "color"
disabled_text_color          = "color"
