---
phase: 15-publishing-prep
plan: 04
type: execute
wave: 2
depends_on: ["15-01", "15-02"]
files_modified: []
autonomous: false
requirements:
  - PUB-07
  - PUB-08
user_setup:
  - service: crates.io
    why: "Publishing crates requires crates.io authentication"
    env_vars:
      - name: CARGO_REGISTRY_TOKEN
        source: "https://crates.io/settings/tokens -- create a token with 'publish-new' and 'publish-update' scopes"
    dashboard_config:
      - task: "Login with cargo login or set CARGO_REGISTRY_TOKEN"
        location: "https://crates.io/settings/tokens"
  - service: github
    why: "Repository URL in Cargo.toml must point to an existing GitHub repo"
    dashboard_config:
      - task: "Create GitHub repository and push code"
        location: "https://github.com/new"

must_haves:
  truths:
    - "native-theme 0.2.0 is published and visible on crates.io"
    - "native-theme-iced 0.1.0 is published and visible on crates.io"
    - "Git tag v0.2.0 exists"
  artifacts: []
  key_links:
    - from: "cargo publish -p native-theme"
      to: "crates.io"
      via: "crate registry upload"
      pattern: "published"
    - from: "cargo publish -p native-theme-iced"
      to: "crates.io native-theme dependency"
      via: "registry dependency resolution"
      pattern: "published"
---

<objective>
Publish native-theme and native-theme-iced to crates.io.

Purpose: PUB-07 and PUB-08 require both crates to be published to crates.io. The core crate must be published first (so the iced connector can resolve its dependency from the registry). This plan requires human action for crates.io authentication and GitHub repository setup.
Output: Both crates published to crates.io, git tag v0.2.0 created.
</objective>

<execution_context>
@/home/tibi/.claude/get-shit-done/workflows/execute-plan.md
@/home/tibi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-publishing-prep/15-RESEARCH.md
@.planning/phases/15-publishing-prep/15-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Set up GitHub remote and crates.io authentication</name>
  <files></files>
  <action>
User must complete these prerequisite steps before publishing can proceed:

1. Create GitHub repository (if not already done): go to https://github.com/new, create repo (e.g., `native-theme`), add remote with `git remote add origin git@github.com:YOUR_USER/native-theme.git`, push with `git push -u origin master`.

2. Update repository URL in root `Cargo.toml` `[workspace.package]` if placeholder was used -- set `repository` and `homepage` to match actual GitHub URL.

3. Authenticate with crates.io: visit https://crates.io/settings/tokens, create a token with 'publish-new' and 'publish-update' scopes, run `cargo login`.

4. Verify dry-runs still pass: `cargo publish --dry-run -p native-theme` and `cargo publish --dry-run -p native-theme-iced`.

Type "ready" when GitHub remote is configured, Cargo.toml URLs are correct, and cargo login is done.
  </action>
  <verify>
    <automated>cd /home/tibi/Rust/native-theme && git remote -v | head -2 && cargo publish --dry-run -p native-theme 2>&1 | tail -3</automated>
  </verify>
  <done>GitHub remote is configured, Cargo.toml repository URLs point to a real GitHub repo, and cargo login authentication is complete.</done>
</task>

<task type="auto">
  <name>Task 2: Publish crates to crates.io and tag release</name>
  <files></files>
  <action>
1. Run final verification:
   - `cargo test -p native-theme`
   - `cargo test --doc -p native-theme`
   - `cargo test -p native-theme-iced`
   - `cargo publish --dry-run -p native-theme`
   - `cargo publish --dry-run -p native-theme-iced`
   - `cargo doc -p native-theme --no-deps` (check zero warnings)

2. Publish the core crate first:
   - `cargo publish -p native-theme`
   - Wait 30-60 seconds for crates.io index propagation

3. Publish the iced connector:
   - `cargo publish -p native-theme-iced`
   - If it fails with "failed to select a version", wait another 30 seconds and retry

4. Create git tag:
   - `git tag v0.2.0 -m "Release v0.2.0"`

5. Verify publications:
   - Check that `cargo search native-theme` shows both crates
  </action>
  <verify>
    <automated>cargo search native-theme 2>&1 | head -5</automated>
  </verify>
  <done>native-theme 0.2.0 and native-theme-iced 0.1.0 are both published on crates.io. Git tag v0.2.0 exists.</done>
</task>

</tasks>

<verification>
- `cargo search native-theme` shows both crates
- `git tag -l v0.2.0` returns v0.2.0
- https://crates.io/crates/native-theme shows version 0.2.0
- https://crates.io/crates/native-theme-iced shows version 0.1.0
</verification>

<success_criteria>
Both crates are live on crates.io. Any Rust developer can add `native-theme = "0.2"` to their Cargo.toml and it resolves from the registry.
</success_criteria>

<output>
After completion, create `.planning/phases/15-publishing-prep/15-04-SUMMARY.md`
</output>
