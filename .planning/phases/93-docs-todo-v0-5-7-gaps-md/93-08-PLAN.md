---
phase: 93-docs-todo-v0-5-7-gaps-md
plan: 08
type: execute
wave: 5
depends_on: [93-06, 93-07]
files_modified:
  - pre-release-check.sh
  - RELEASING.md
  - .planning/phases/93-docs-todo-v0-5-7-gaps-md/93-VERIFICATION.md
autonomous: true
gap_closure: true
requirements: [G11-followup]
tags: [release, pre-release-check, docs, v057-polish, gap-closure]

must_haves:
  truths:
    - "`./pre-release-check.sh` exits 0 with its green success banner at HEAD, no human prerequisite required."
    - "`pre-release-check.sh` lines 321-323 use `--no-verify` on the three `cargo package` invocations, with an inline comment block explaining the first-publication bootstrap rationale and the exact condition for removing `--no-verify` later."
    - "`RELEASING.md` exists at the repo root documenting: (a) the ordered publish sequence `native-theme-derive → native-theme-build → native-theme → native-theme-iced → native-theme-gpui`; (b) why the order matters (tarball verify of each `cargo publish` resolves workspace-internal deps from crates.io, so earlier crates must be indexed first); (c) the condition for removing `--no-verify` from the release script after the first successful publish cycle lands on crates.io; (d) the fact that `cargo publish` performs the full tarball-compile-against-registry verification that `pre-release-check.sh --no-verify` skips — so the real gate is `cargo publish` itself, in order."
    - "`93-VERIFICATION.md` is corrected so the `human_verification` section no longer claims the user must `cargo publish -p native-theme-derive` before the script can pass. Correction is APPEND-ONLY per the 'agents must append, never rewrite' rule — append an `## Update 2026-04-19 (Plan 93-08)` section at the file tail + a targeted edit to the frontmatter `status:` field from `human_needed` to `passed` (one-line field edit, not a rewrite). No existing narrative paragraphs are modified."
    - "The new `RELEASING.md` is referenced from at least one pre-existing doc so it is discoverable (e.g. a one-line pointer added to the end of `CHANGELOG.md` v0.5.7 section or to `README.md` — whichever the existing doc layout naturally supports; APPEND-ONLY in either case)."
    - "Zero regressions: `cargo test -p native-theme --all-features --lib` still reports 791 passed; `cargo test -p native-theme --all-features --doc` still reports ≥50 passed / 0 failed; `cargo clippy -p native-theme --all-targets -- -D warnings` still exits 0."
    - "One atomic commit, message `docs(93-08): fix pre-release-check bootstrap + document ordered-publish workflow`, no `Co-Authored-By` trailer."
  artifacts:
    - path: "pre-release-check.sh"
      provides: "Lines 321-323 updated to `cargo package --no-verify ...` with a comment block immediately above explaining the bootstrap rationale and when to remove `--no-verify`."
      contains: "--no-verify"
    - path: "RELEASING.md"
      provides: "New root-level doc describing the ordered publish workflow, the first-publication bootstrap, and the condition for removing --no-verify post-bootstrap."
      contains: "cargo publish -p native-theme-derive"
    - path: ".planning/phases/93-docs-todo-v0-5-7-gaps-md/93-VERIFICATION.md"
      provides: "status: passed in the frontmatter; appended Update section citing Plan 93-08 and the corrected understanding that the script is the gate, not prior publication."
      contains: "Update 2026-04-19 (Plan 93-08)"
  key_links:
    - from: "pre-release-check.sh line 321 comment"
      to: "RELEASING.md bootstrap section"
      via: "one-line pointer in the comment"
      pattern: "RELEASING.md"
    - from: "RELEASING.md"
      to: "pre-release-check.sh"
      via: "explicit citation of the script's per-crate test/clippy/doc steps as the real pre-publication validation"
      pattern: "pre-release-check.sh"
---

<objective>
Close the release-gate gap identified by the phase-93 re-verification: `./pre-release-check.sh` was red at line 321 because `cargo package -p native-theme-derive -p native-theme -p native-theme-build --allow-dirty` failed to verify tarball compilation — the tarball's stripped `Cargo.toml` declares `native-theme-derive = "0.5.7"` and cargo resolves that via crates.io, where `native-theme-derive` has never been published. This makes tarball verify architecturally impossible on the first-ever publication of a workspace with internal path deps.

The previous verification (VERIFICATION.md as committed in `081c6cf`/`0e87107`) classified this as `human_needed` with the guidance "publish `native-theme-derive` 0.5.7 first, then re-run the script." That guidance is wrong: `cargo publish` itself requires the same tarball verify to pass — so "publish first" is circular, AND publishing untested code violates the user's explicit rule. The actual fix is at the release-script level.

Correct fix (documented in-conversation 2026-04-19):

- The cargo-package tarball verify step compiles each tarball as if downloaded from crates.io. For a first-publication bootstrap, workspace-internal deps aren't on crates.io yet, so this simulation cannot succeed. The standard industry workaround is `cargo package --no-verify` (which still validates metadata, file list, and tarball creation, but skips the download-and-compile-against-registry step that's impossible to bootstrap).
- The REAL tarball-verify happens during `cargo publish` itself: when run in dependency order, each `cargo publish` compiles the tarball against crates.io where the PRIOR crates are now indexed. So the full publish-grade verification exists; it just cannot happen before any crate is published.
- Once `native-theme-derive 0.5.7` is on crates.io (after the first successful publish cycle), `--no-verify` can be removed from the script, restoring tarball-verify for future releases.

This plan makes `./pre-release-check.sh` green at HEAD (the phase's chartered must_have per Plan 93-06 success criterion #8), documents the ordered publish workflow so the user has a clear runbook, and corrects the inaccurate human_verification claim in VERIFICATION.md.

Scope: one script change (three lines + comment block), one new markdown doc (`RELEASING.md` ~50-80 lines), one APPEND-ONLY VERIFICATION.md update + single-field frontmatter status correction. One atomic commit.

Why not just revert Phase 93-05's `ThemeFields` derive: that's rejecting a working refactor for a release-workflow limitation. The fix belongs in the release script.

Why not Option A (publish first): the user explicitly rejected this — publishing requires passing tests, not vice versa, and the unpublished derive crate is a symptom of a never-bootstrapped workspace, not a defect in the derive itself.

Why not Option C (local registry): deeper analysis showed it requires either `cargo vendor` of the entire dep tree (heavyweight: every dep vendored, 100MB+ per refresh) or an overlay-registry cargo feature that doesn't exist (RFC rust-lang/cargo#9227 pending). Neither gives meaningfully more confidence than Option B + documented publish order.

**Wave assignment:** Wave 5. Depends on 93-06 and 93-07 (both already committed). No parallel siblings.
</objective>

<execution_context>
@/home/tibi/.claude/get-shit-done/workflows/execute-plan.md
@/home/tibi/.claude/get-shit-done/templates/summary.md
@/home/tibi/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/93-docs-todo-v0-5-7-gaps-md/93-VERIFICATION.md
@.planning/phases/93-docs-todo-v0-5-7-gaps-md/93-06-SUMMARY.md
@.planning/phases/93-docs-todo-v0-5-7-gaps-md/93-07-SUMMARY.md
@pre-release-check.sh
@Cargo.toml
</context>

<tasks>

<task type="auto">
  <name>Task 93-08.01: Add --no-verify to pre-release-check.sh package-validation lines with bootstrap-rationale comment</name>
  <files>pre-release-check.sh</files>
  <action>
Edit lines 320-323 of `pre-release-check.sh`. Original:

```bash
# Validate packages before publishing (cargo package resolves workspace path deps locally)
run_check "Validating packages (core)" cargo package -p native-theme-derive -p native-theme -p native-theme-build --allow-dirty
run_check_soft "Validating package (iced connector)" cargo package -p native-theme-derive -p native-theme -p native-theme-iced --allow-dirty
run_check_soft "Validating package (gpui connector)" cargo package -p native-theme-derive -p native-theme -p native-theme-gpui --allow-dirty
```

Replace with:

```bash
# Validate packages before publishing.
#
# --no-verify rationale: cargo package verification compiles each tarball as if it
# were downloaded from crates.io. For first-ever publication of this workspace, the
# internal proc-macro crate native-theme-derive is not yet on the registry, so
# tarball verification cannot resolve the workspace-internal dep. The real tarball
# compilation check happens during `cargo publish` itself, which this script does
# not run. See RELEASING.md for the ordered publish workflow (derive -> build ->
# core -> connectors).
#
# Once native-theme-derive 0.5.7 is published to crates.io, remove --no-verify
# from these three lines to restore full tarball-compile verification for
# subsequent releases.
run_check "Validating packages (core)" cargo package --no-verify -p native-theme-derive -p native-theme -p native-theme-build --allow-dirty
run_check_soft "Validating package (iced connector)" cargo package --no-verify -p native-theme-derive -p native-theme -p native-theme-iced --allow-dirty
run_check_soft "Validating package (gpui connector)" cargo package --no-verify -p native-theme-derive -p native-theme -p native-theme-gpui --allow-dirty
```

Rationale summary: the three `cargo package` invocations with `-p A -p B -p C` multi-crate mode already coordinate path-dep resolution during packaging (tarball metadata is correctly stripped and sibling versions resolve). What they can't do is tarball-verification — extracted tarballs are compiled in isolation against the registry, and the registry doesn't have the unpublished derive. `--no-verify` skips only the verification-compile sub-step; tarball creation, metadata validation, and file-list checks all still run.
  </action>
  <verify>
1. `grep -n "no-verify" pre-release-check.sh` — expected 3 matches (lines 333, 334, 335 or thereabouts after the comment block insertion).
2. `grep -n "RELEASING.md" pre-release-check.sh` — expected 1 match in the new comment block.
3. Run `./pre-release-check.sh 2>&1 | tail -5` — expected end with "All pre-release checks passed successfully!" (or equivalent green banner). Exit code 0.
4. `./pre-release-check.sh 2>&1 | grep -c "\[0;31m"` — expected 0 (no red/error markers reach the banner).
  </verify>
  <done>
`pre-release-check.sh` lines for package validation carry `--no-verify`, the comment block explains why and how to remove it later, and running the script end-to-end produces the green success banner.
  </done>
</task>

<task type="auto">
  <name>Task 93-08.02: Create RELEASING.md documenting ordered-publish workflow and bootstrap condition</name>
  <files>RELEASING.md</files>
  <action>
Create a new file `RELEASING.md` at the repo root. Content:

```markdown
# Releasing `native-theme`

This workspace publishes five crates that depend on each other. Publication is not order-independent — `native-theme` depends on `native-theme-derive` (proc-macro) and `native-theme-build` (codegen helpers), and the connectors (`native-theme-iced`, `native-theme-gpui`) depend on `native-theme`. Publishing has to happen in dependency order.

## Pre-publication checks

Run the pre-release script from the repo root:

```bash
./pre-release-check.sh
```

The script runs, per crate:

- `cargo check`
- `cargo fmt --check` (autofixes if possible)
- `cargo clippy --all-targets -D warnings`
- `cargo test`
- Example builds (for crates that have an `examples/` directory)
- `cargo doc --no-deps`

It also runs `cargo package --no-verify` on the five crates to confirm each tarball can be built, metadata is valid, and the file list is correct. `native-theme-gpui` is treated as a soft check (warns but doesn't block) because of the upstream `naga` 27.0.3 / `codespan-reporting` 0.12.0 incompatibility documented in `docs/todo_v0.5.7_gaps.md` §G11.

### Why `--no-verify`?

`cargo package` verification compiles each tarball as if it were downloaded from crates.io. On the first-ever publication of a workspace with internal path deps, the depended-on crate (`native-theme-derive`) is not yet indexed, so that compilation can't succeed — cargo searches crates.io, doesn't find the crate, errors out. `--no-verify` skips only the verification-compile step; metadata and tarball creation are still validated.

**The real tarball-verification happens during `cargo publish` itself** (see below). Once the first publish cycle seats `native-theme-derive 0.5.7` on crates.io, `--no-verify` can be removed from the three lines at the bottom of `pre-release-check.sh` so subsequent releases get full tarball-verify.

## Publish order

For a cold-start release (nothing on crates.io yet), publish in this order, one at a time, waiting for the index to settle between each:

```bash
# 1. Proc-macro crate (no workspace-internal deps)
cargo publish -p native-theme-derive

# Wait ~30-60 seconds for the crates.io sparse index to update.

# 2. Codegen helpers (depends on native-theme-derive)
cargo publish -p native-theme-build

# Wait.

# 3. Core crate (depends on native-theme-derive AND native-theme-build)
cargo publish -p native-theme

# Wait.

# 4. Iced connector (depends on native-theme)
cargo publish -p native-theme-iced

# Wait.

# 5. GPUI connector (depends on native-theme; soft-gated)
cargo publish -p native-theme-gpui
```

Each `cargo publish` invocation performs the tarball-compile-against-registry check that `pre-release-check.sh --no-verify` skips. Because prior crates are indexed by the time the next `cargo publish` runs, those verifications succeed. `cargo publish` is the real release-grade validation of the workspace.

If any step fails: crates.io does not allow unpublishing, but versions can be yanked. Publish a `0.5.7+` patch or bump to `0.5.8` rather than trying to recover.

## Post-bootstrap cleanup

After the first successful publish cycle (i.e. `native-theme-derive 0.5.7` is live on crates.io), remove `--no-verify` from the three lines near the bottom of `pre-release-check.sh`:

```bash
# Before (bootstrap):
run_check "Validating packages (core)" cargo package --no-verify -p native-theme-derive -p native-theme -p native-theme-build --allow-dirty

# After (post-bootstrap):
run_check "Validating packages (core)" cargo package -p native-theme-derive -p native-theme -p native-theme-build --allow-dirty
```

From `0.5.8` onwards, the script will perform full tarball-verify against the live registry because the prior version of `native-theme-derive` is now resolvable from crates.io.

## Version bumps

Workspace version is declared once in the root `Cargo.toml` under `[workspace.package]`. All five crates inherit it via `version.workspace = true`. Update `version` in `[workspace.package]`, commit, then run the publish sequence above.

The `CHANGELOG.md` and per-crate `README.md` badges should be updated in the same commit as the version bump.

## Tagging

After all five `cargo publish` commands succeed:

```bash
git tag v0.5.7
git push origin v0.5.7
```

Push only the specific tag — never `git push --tags` (user rule).
```

No emojis in the file content (project rule).
  </action>
  <verify>
1. `test -f RELEASING.md` — file exists.
2. `grep -c "cargo publish -p native-theme-derive" RELEASING.md` — ≥1 match.
3. `grep -c "no-verify" RELEASING.md` — ≥2 matches (bootstrap rationale + post-bootstrap cleanup).
4. `grep -c "pre-release-check.sh" RELEASING.md` — ≥3 matches (script is cross-referenced throughout).
5. `grep -c "naga" RELEASING.md` — ≥1 match (gpui soft-check justification cites the G11 principled deviation).
6. `wc -l RELEASING.md` — between 50 and 150 lines (concise but complete).
7. `grep -E "^## " RELEASING.md | wc -l` — at least 5 section headings.
  </verify>
  <done>
`RELEASING.md` exists at the repo root with the ordered publish sequence, `--no-verify` bootstrap rationale, post-bootstrap cleanup instructions, and tagging guidance.
  </done>
</task>

<task type="auto">
  <name>Task 93-08.03: Append Update section to 93-VERIFICATION.md correcting the inaccurate human_verification guidance; targeted frontmatter status edit</name>
  <files>.planning/phases/93-docs-todo-v0-5-7-gaps-md/93-VERIFICATION.md</files>
  <action>
Two targeted edits to `93-VERIFICATION.md` — no existing narrative is rewritten, per the 'append-only' rule.

**EDIT 1 — Frontmatter status field.** Find line 4: `status: human_needed`. Replace with: `status: passed`. This is a single-field edit to reflect the resolved state, not a rewrite of the verification story. Also update the re_verification block — find `previous_status: gaps_found` (line 13) and append just below it (after `previous_score:` line 14) a new line `superseded_by: plan-93-08` for audit-trail linkage.

**EDIT 2 — Append a new `## Update 2026-04-19 (Plan 93-08)` section at the end of the file.** The file currently ends around line 181. Add (two blank lines then the new section):

```markdown


## Update 2026-04-19 (Plan 93-08)

The `human_verification` entry above described an inaccurate remedial action:
publishing `native-theme-derive` 0.5.7 (and `native-theme-build` 0.5.7) to
crates.io as a prerequisite to `./pre-release-check.sh` passing. That
description was generated before the root cause was fully understood and
contained two errors:

1. **Publishing untested code is a rule violation.** The user rule
   "NEVER bypass human checkpoints" combined with standard engineering
   practice means the release script must go green *before* publication, not
   as a consequence of it. Any "publish first, re-run later" procedure
   reverses the gate.
2. **Publication would not have been possible anyway.** `cargo publish`
   performs the same tarball-compile-against-registry verification that was
   blocking `cargo package` at line 321. On the very first publication of a
   workspace with internal path deps, that verification is impossible to
   pass for crates after the root — the earlier crates must be indexed on
   crates.io before later ones can be tarball-verified against them. This
   is a cargo architectural constraint, not a project defect (see
   rust-lang/cargo issues #9227 and related for the community's
   overlay-registry discussion).

**Actual fix:** Plan 93-08 added `--no-verify` to the three `cargo package`
invocations in `pre-release-check.sh` (lines 333-335 after the comment
block) with an inline rationale and removal condition. This is the
industry-standard bootstrap pattern for Rust workspaces on first-ever
publication. The real tarball-verification happens during the ordered
`cargo publish` sequence itself (each invocation verifies against a
crates.io that now has the prior crate indexed). `RELEASING.md` at the repo
root documents the publish order and the post-bootstrap cleanup
(removing `--no-verify` once `native-theme-derive 0.5.7` is live).

**Result after Plan 93-08:** `./pre-release-check.sh` exits 0 with the
green success banner at HEAD, no human prerequisite required. The
frontmatter `status:` is updated from `human_needed` to `passed`. The
original `human_verification` block is kept above for audit trail.

**References:**

- `pre-release-check.sh` lines 320-335 (comment block + three `--no-verify`
  `cargo package` invocations).
- `RELEASING.md` (new; publish order + post-bootstrap cleanup).
- `.planning/phases/93-docs-todo-v0-5-7-gaps-md/93-08-PLAN.md` (this plan).
- `.planning/phases/93-docs-todo-v0-5-7-gaps-md/93-08-SUMMARY.md` (plan
  completion report).
```

No modification to any existing paragraph. The `human_verification:` frontmatter block above stays as-is so the audit trail shows what the original verifier said, even though its remedial claim is now superseded.
  </action>
  <verify>
1. `grep -n "^status:" .planning/phases/93-docs-todo-v0-5-7-gaps-md/93-VERIFICATION.md | head -1` — expected `4:status: passed`.
2. `grep -n "superseded_by: plan-93-08" .planning/phases/93-docs-todo-v0-5-7-gaps-md/93-VERIFICATION.md` — expected 1 match in the re_verification block.
3. `grep -n "^## Update 2026-04-19" .planning/phases/93-docs-todo-v0-5-7-gaps-md/93-VERIFICATION.md` — expected 1 match at the file tail.
4. `git diff .planning/phases/93-docs-todo-v0-5-7-gaps-md/93-VERIFICATION.md | grep -c '^-'` — expected 2 lines of deletion markers ONLY (the two single-line frontmatter field edits). No narrative paragraphs deleted.
5. `grep -n "human_verification:" .planning/phases/93-docs-todo-v0-5-7-gaps-md/93-VERIFICATION.md` — expected 1 match (the original block is preserved).
  </verify>
  <done>
VERIFICATION.md frontmatter reflects the resolved state (`status: passed`, `superseded_by: plan-93-08`), and a trailing Update section documents the correction. No existing narrative paragraph is modified.
  </done>
</task>

<task type="auto">
  <name>Task 93-08.04: Add one-line RELEASING.md cross-reference to README.md; create atomic commit for the four files</name>
  <files>README.md</files>
  <action>
Append a one-line pointer at the end of `README.md` making `RELEASING.md` discoverable. Exact wording depends on the existing README layout — read the file first, then pick whichever of these fits the heading rhythm:

- If the README has a "Contributing" / "Development" / "Release" section heading, append within that section a bullet: `- Release process: see [RELEASING.md](RELEASING.md) for the ordered publish workflow.`
- If no such heading exists, append at the very end a new two-line block:
  ```markdown

  ## Release

  See [RELEASING.md](RELEASING.md) for the ordered publish workflow, the `--no-verify` bootstrap rationale for first-ever publication, and the post-bootstrap cleanup condition.
  ```

APPEND-ONLY — do not modify any existing README paragraph.

After this task, create ONE atomic commit covering all four files:

```
docs(93-08): fix pre-release-check bootstrap + document ordered-publish workflow

Phase 93 re-verification flagged ./pre-release-check.sh as red at line 321
because `cargo package -p native-theme-derive -p native-theme -p native-theme-build
--allow-dirty` failed tarball verification — the tarball's stripped Cargo.toml
declares `native-theme-derive = "0.5.7"` and cargo resolves that via crates.io,
where native-theme-derive has never been published. The original VERIFICATION.md
classified this as human_needed with guidance to publish native-theme-derive
first; that guidance reversed the release gate (publishing requires passing
tests, not the other way around) and would have been architecturally blocked
anyway (cargo publish runs the same tarball verify that was failing).

Root cause: cargo package verification compiles each tarball as if downloaded
from crates.io. On first-ever publication of a workspace with internal path
deps, the depended-on crate isn't indexed yet, so the simulation can't succeed.
This is a cargo architectural constraint; overlay-registry RFCs have been
pending for years.

Fix: pre-release-check.sh lines 320-335 gain --no-verify on the three cargo
package invocations with an inline comment block explaining the bootstrap
rationale and how to remove it after the first publish cycle lands on
crates.io. RELEASING.md is added documenting the ordered publish sequence
(derive -> build -> core -> iced -> gpui), why the order matters, and the
post-bootstrap cleanup condition. VERIFICATION.md frontmatter is updated
(status: human_needed -> passed; superseded_by: plan-93-08) with an
append-only Update section explaining why the original human_verification
claim was wrong.

Files:
- pre-release-check.sh (lines 320-335: comment block + --no-verify on 3 lines)
- RELEASING.md (new; ordered publish workflow + bootstrap cleanup)
- .planning/phases/93-docs-todo-v0-5-7-gaps-md/93-VERIFICATION.md
  (frontmatter edits + appended Update section; narrative preserved)
- README.md (one-line pointer to RELEASING.md)

./pre-release-check.sh now exits 0 with the green success banner at HEAD.
No human prerequisite required.

Refs: docs/todo_v0.5.7_gaps.md G11 (upstream naga / codespan-reporting
      deviation); cargo issue #9227 (overlay-registry RFC)
```

No `Co-Authored-By` trailer per user memory rule.

After the commit, ALSO run `./pre-release-check.sh 2>&1 | tail -5` as the final verify step and paste the last few lines into the SUMMARY to prove the banner is green.
  </action>
  <verify>
1. `grep -c "RELEASING.md" README.md` — ≥1 match.
2. `git diff README.md | grep -c '^-'` — expected 0 (APPEND-ONLY).
3. After commit: `git log -1 --format=%s` — expected `docs(93-08): fix pre-release-check bootstrap + document ordered-publish workflow`.
4. After commit: `git log -1 --format=%B | grep -c "Co-Authored-By"` — expected 0.
5. After commit: `git log -1 --name-only | wc -l` — expected to list four files: `pre-release-check.sh`, `RELEASING.md`, `.planning/phases/93-docs-todo-v0-5-7-gaps-md/93-VERIFICATION.md`, `README.md`.
6. Final script run: `./pre-release-check.sh 2>&1 | tail -3` ends with the success banner.
  </verify>
  <done>
README.md has a discoverable pointer to RELEASING.md; one atomic commit covers all four files with the plan's commit message; `./pre-release-check.sh` runs green end-to-end.
  </done>
</task>

</tasks>

<verification>
**Plan-level verification** — Phase 93 release-gate criterion (Plan 93-06 success criterion #8) is met:

| Check | Expected | Command |
|-------|----------|---------|
| Script green end-to-end | success banner, exit 0 | `./pre-release-check.sh` |
| No red markers in script output | 0 | `./pre-release-check.sh 2>&1 \| grep -c '\[0;31m'` |
| --no-verify added 3 times in script | 3 matches | `grep -c 'no-verify' pre-release-check.sh` |
| RELEASING.md exists and is substantive | 50-150 lines, ≥5 headings | `wc -l RELEASING.md; grep -c '^## ' RELEASING.md` |
| Publish order documented | 5 cargo publish commands in sequence | `grep -c 'cargo publish -p native-theme' RELEASING.md` ≥5 |
| VERIFICATION.md status corrected | passed | `grep '^status:' .planning/phases/93-docs-todo-v0-5-7-gaps-md/93-VERIFICATION.md` |
| VERIFICATION.md appended, not rewritten | minimal deletions | `git diff --stat .planning/phases/93-docs-todo-v0-5-7-gaps-md/93-VERIFICATION.md` — should be mostly additions |
| README.md pointer to RELEASING.md | 1 match | `grep -c 'RELEASING.md' README.md` |
| One atomic commit for four files | 4 file names in commit | `git log -1 --name-only \| tail -5` |
| No Co-Authored-By | 0 | `git log -1 --format=%B \| grep -c 'Co-Authored-By'` |

**Negative checks:**
- No Rust file modifications (`git log -1 --name-only | grep -c '\.rs$'` returns 0).
- No Cargo.toml or Cargo.lock changes.
- No existing README paragraph deleted (`git diff README.md | grep -c '^-'` returns 0).
- No existing VERIFICATION.md narrative paragraph deleted (`git diff .planning/phases/93-docs-todo-v0-5-7-gaps-md/93-VERIFICATION.md | grep '^-' | grep -v '^---' | wc -l` returns ≤2 for the two targeted frontmatter field edits).

**Regression checks:**
- `cargo test -p native-theme --all-features --lib` still 791 passed.
- `cargo test -p native-theme --all-features --doc` still ≥50 passed / 0 failed.
- `cargo clippy -p native-theme --all-targets -- -D warnings` still exit 0.
</verification>

<success_criteria>
- [ ] `pre-release-check.sh` lines 320-335 carry the comment block + `--no-verify` on all three `cargo package` invocations.
- [ ] `RELEASING.md` exists at repo root, 50-150 lines, documents ordered publish workflow + bootstrap cleanup.
- [ ] `93-VERIFICATION.md` frontmatter reflects `status: passed` and `superseded_by: plan-93-08`; appended Update section explains the correction; no existing narrative modified.
- [ ] `README.md` has a one-line discoverable pointer to `RELEASING.md`.
- [ ] `./pre-release-check.sh` exits 0 with the green success banner at HEAD.
- [ ] No regressions: 791 lib tests + 50 doctests + 79 integration tests still passing; clippy still clean.
- [ ] ONE atomic commit containing all four files; commit message per template; no `Co-Authored-By` trailer.
- [ ] SUMMARY.md at `.planning/phases/93-docs-todo-v0-5-7-gaps-md/93-08-SUMMARY.md` with the final pre-release-check.sh tail output pasted as proof.
- [ ] STATE.md updated with 93-08 completion and the decisions recorded in the v0.5.7 Decisions section (the `--no-verify` bootstrap rationale + RELEASING.md creation).
</success_criteria>

<output>
After completion, create `.planning/phases/93-docs-todo-v0-5-7-gaps-md/93-08-SUMMARY.md`.
</output>
