---
phase: 75-non-exhaustive-compile-gate-iconset-default
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - native-theme/src/detect.rs
  - native-theme/src/pipeline.rs
  - native-theme/src/watch/mod.rs
autonomous: true

must_haves:
  truths:
    - "LinuxDesktop enum has #[non_exhaustive] attribute"
    - "LinuxDesktop has Hyprland, Sway, River, Niri, CosmicDe variants"
    - "detect_linux_de() maps XDG_CURRENT_DESKTOP values to the new variants"
    - "Matching LinuxDesktop without a wildcard arm produces a non-exhaustive compile error"
    - "All new compositor variants map to the adwaita preset in linux_preset_for_de()"
    - "watch/mod.rs match arms handle the new variants correctly"
  artifacts:
    - path: "native-theme/src/detect.rs"
      provides: "LinuxDesktop enum with #[non_exhaustive] and new Wayland compositor variants"
      contains: "#[non_exhaustive]"
    - path: "native-theme/src/pipeline.rs"
      provides: "Preset mapping and pipeline dispatch for new LinuxDesktop variants"
      contains: "Hyprland"
  key_links:
    - from: "native-theme/src/detect.rs"
      to: "native-theme/src/pipeline.rs"
      via: "LinuxDesktop variants used in match arms"
      pattern: "LinuxDesktop::(Hyprland|Sway|River|Niri|CosmicDe)"
---

<objective>
Add `#[non_exhaustive]` to `LinuxDesktop` and introduce five new Wayland compositor variants (Hyprland, Sway, River, Niri, CosmicDe).

Purpose: Future-proof the enum so adding new desktop environments is non-breaking, and recognize current Wayland compositors that previously mapped to `Unknown`.
Output: Updated `detect.rs`, `pipeline.rs`, and `watch/mod.rs` with new variants and correct dispatch.
</objective>

<execution_context>
@/home/tibi/.claude/get-shit-done/workflows/execute-plan.md
@/home/tibi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@native-theme/src/detect.rs
@native-theme/src/pipeline.rs
@native-theme/src/watch/mod.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add #[non_exhaustive] and new variants to LinuxDesktop</name>
  <files>native-theme/src/detect.rs</files>
  <action>
  1. Add `#[non_exhaustive]` attribute to the `LinuxDesktop` enum (before the `#[derive]` line at line 5).

  2. Add five new variants before `Unknown` (with doc comments):
     - `Hyprland` — Hyprland Wayland compositor
     - `Sway` — Sway Wayland compositor (i3-compatible)
     - `River` — River Wayland compositor
     - `Niri` — Niri scrollable Wayland compositor
     - `CosmicDe` — COSMIC desktop environment (System76)

  3. Update `detect_linux_de()` to recognize the new XDG_CURRENT_DESKTOP values:
     - `"Hyprland"` → `LinuxDesktop::Hyprland`
     - `"sway"` → `LinuxDesktop::Sway`
     - `"river"` → `LinuxDesktop::River`
     - `"niri"` → `LinuxDesktop::Niri`
     - `"COSMIC"` → `LinuxDesktop::CosmicDe`

     Add these AFTER the existing DEs but BEFORE the `_ => {}` wildcard. COSMIC must be checked before the wildcard (it sets `XDG_CURRENT_DESKTOP=COSMIC`). Hyprland sets it to `"Hyprland"`. Sway uses lowercase `"sway"`. River uses lowercase `"river"`. Niri uses lowercase `"niri"`.
  </action>
  <verify>
  `cargo check -p native-theme` compiles.
  `cargo test -p native-theme detect_hyprland` passes (added in Task 2).
  </verify>
  <done>
  `LinuxDesktop` has `#[non_exhaustive]` and five new Wayland compositor variants. `detect_linux_de()` maps their XDG_CURRENT_DESKTOP strings to the correct variants.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update pipeline dispatch, watch dispatch, and tests</name>
  <files>
  native-theme/src/pipeline.rs
  native-theme/src/watch/mod.rs
  </files>
  <action>
  **pipeline.rs — linux_preset_for_de():**
  The function currently uses `LinuxDesktop::Kde => "kde-breeze-live"` and `_ => "adwaita-live"`. The wildcard already handles new variants correctly (maps to adwaita). No change needed here since `#[non_exhaustive]` forces the wildcard arm.

  **pipeline.rs — from_linux():**
  The match at line 296-321 explicitly lists variants. Add the five new compositor variants alongside the existing GTK-based DEs. They all use adwaita preset:
  ```
  LinuxDesktop::Xfce | LinuxDesktop::Cinnamon | LinuxDesktop::Mate | LinuxDesktop::LxQt
  | LinuxDesktop::Hyprland | LinuxDesktop::Sway | LinuxDesktop::River
  | LinuxDesktop::Niri | LinuxDesktop::CosmicDe => {
      run_pipeline(ThemeSpec::preset("adwaita")?, preset, is_dark)
  }
  ```

  **pipeline.rs — from_system_async_inner():**
  Same change at the async match (line 376-442). Add the five new variants to the same arm as Xfce/Cinnamon/Mate/LxQt.

  **pipeline.rs — tests:**
  Update the three existing tests that check `detect_linux_de("Hyprland") == LinuxDesktop::Unknown` etc. (lines 571-583):
  - `detect_hyprland_returns_unknown` → rename to `detect_hyprland` and assert `LinuxDesktop::Hyprland`
  - `detect_sway_returns_unknown` → rename to `detect_sway` and assert `LinuxDesktop::Sway`
  - `detect_cosmic_returns_unknown` → rename to `detect_cosmic` and assert `LinuxDesktop::CosmicDe`

  Add three more tests:
  - `detect_river` — `assert_eq!(detect_linux_de("river"), LinuxDesktop::River)`
  - `detect_niri` — `assert_eq!(detect_linux_de("niri"), LinuxDesktop::Niri)`
  - `detect_cosmic_full_desktop` — `assert_eq!(detect_linux_de("COSMIC:Freedesktop"), LinuxDesktop::CosmicDe)` (tests colon-separated value)

  Also update the test comment `LNXDE-03: Hyprland, Sway, COSMIC map to Unknown` to say they map to their own variants.

  **watch/mod.rs — on_theme_change():**
  The match on `de` at lines 193-205 uses `_` wildcard already. No change needed since the wildcard arm handles new variants. However, verify the existing match works correctly with the new `#[non_exhaustive]` on the enum — the `_` arm already handles unknown variants.
  </action>
  <verify>
  `cargo test -p native-theme -- detect_hyprland detect_sway detect_cosmic detect_river detect_niri` — all pass.
  `cargo test -p native-theme -- from_linux` — pipeline tests pass.
  `cargo clippy -p native-theme -- -D warnings` — no warnings.
  </verify>
  <done>
  All match arms in `pipeline.rs` handle the new LinuxDesktop variants. Tests verify detection and preset mapping for all five new Wayland compositors.
  </done>
</task>

</tasks>

<verification>
1. `cargo check -p native-theme` — compiles cleanly
2. `cargo test -p native-theme` — all tests pass (including new detection tests)
3. `cargo clippy -p native-theme -- -D warnings` — no warnings
4. Grep for `LinuxDesktop::Unknown` in pipeline.rs tests — the three Hyprland/Sway/COSMIC tests should now assert their own variants, not Unknown
5. Verify `#[non_exhaustive]` is present on LinuxDesktop enum
</verification>

<success_criteria>
- LinuxDesktop has `#[non_exhaustive]` and five new variants: Hyprland, Sway, River, Niri, CosmicDe
- detect_linux_de() maps XDG_CURRENT_DESKTOP strings to the correct new variants
- All pipeline match arms handle the new variants (map to adwaita preset)
- All existing tests pass; new tests verify detection of each compositor
</success_criteria>

<output>
After completion, create `.planning/phases/75-non-exhaustive-compile-gate-iconset-default/75-01-SUMMARY.md`
</output>
