---
phase: 75-non-exhaustive-compile-gate-iconset-default
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - native-theme/src/model/icons.rs
autonomous: true

must_haves:
  truths:
    - "IconSet::default() no longer exists -- calling it produces a compile error"
    - "Removing Default from IconSet does not break serde deserialization (icon_set is Option<IconSet>, defaults to None)"
    - "system_icon_set() remains available as the correct platform-aware replacement"
    - "on_theme_change is compile-gated behind #[cfg(feature = watch)] -- already satisfied, verified"
  artifacts:
    - path: "native-theme/src/model/icons.rs"
      provides: "IconSet enum without Default derive"
  key_links:
    - from: "native-theme/src/model/icons.rs"
      to: "native-theme/src/model/mod.rs"
      via: "icon_set: Option<IconSet> field defaults to None via Option, not IconSet::default()"
      pattern: "Option<IconSet>"
---

<objective>
Remove IconSet::default() (ICON-05) and verify the watch feature compile-gate (WATCH-03).

Purpose: IconSet::default() returns Freedesktop on all platforms, which is misleading on macOS/Windows. The design doc recommends removing `#[derive(Default)]` entirely. For WATCH-03, the existing `#[cfg(feature = "watch")]` gates already satisfy the compile-time error requirement -- this plan verifies that fact.
Output: Updated `model/icons.rs` with Default removed from IconSet.
</objective>

<execution_context>
@/home/tibi/.claude/get-shit-done/workflows/execute-plan.md
@/home/tibi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@native-theme/src/model/icons.rs
@native-theme/src/lib.rs
@native-theme/src/watch/mod.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Default from IconSet and verify watch gating</name>
  <files>native-theme/src/model/icons.rs</files>
  <action>
  **ICON-05: Remove IconSet::default()**

  In `native-theme/src/model/icons.rs`:

  1. Remove `Default` from the derive macro on the `IconSet` enum (line 274). Change:
     `#[derive(Debug, Clone, Copy, Default, PartialEq, Eq, Hash, Serialize, Deserialize)]`
     to:
     `#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]`

  2. Remove the `#[default]` attribute from the `Freedesktop` variant (line 289).

  3. Replace the doc comment on the `Freedesktop` variant (lines 283-288). The current 4-line comment references `IconSet::default()` which will no longer exist. Replace with a single line:
     `/// freedesktop Icon Naming Specification (Linux).`

  No call sites in the crate use `IconSet::default()`. The field `icon_set: Option<IconSet>` in `model/mod.rs` uses `#[serde(default)]` on the `Option`, which defaults to `None` -- this does NOT require `Default` on `IconSet`.

  **WATCH-03: Verify compile gating (no code changes)**

  The existing code already satisfies WATCH-03:
  - `lib.rs:116-117`: `#[cfg(feature = "watch")] pub mod watch;`
  - `lib.rs:185-186`: `#[cfg(feature = "watch")] pub use watch::{ThemeChangeEvent, ThemeWatcher, on_theme_change};`

  Without the `watch` feature, the entire module and all re-exports are absent. Calling `on_theme_change()` produces a compile error ("cannot find function"). This is exactly what the design doc section 22 Option B prescribes.

  Verify by confirming:
  - `grep -n 'cfg(feature = "watch")' native-theme/src/lib.rs` shows both the module declaration and the re-export line are gated.
  </action>
  <verify>
  `cargo check -p native-theme` -- compiles cleanly.
  `cargo test -p native-theme -- icon_set` -- all icon_set tests pass.
  `cargo test -p native-theme -- icon_set_default_is_none` -- passes (tests ThemeVariant::default().icon_set is None, not IconSet::default()).
  `cargo clippy -p native-theme -- -D warnings` -- no warnings.
  `grep 'cfg(feature = "watch")' native-theme/src/lib.rs` -- shows gating on module and re-exports.
  </verify>
  <done>
  IconSet no longer implements Default. Calling IconSet::default() produces a compile error. Serde deserialization is unaffected. WATCH-03 verified: on_theme_change/ThemeChangeEvent/ThemeWatcher are compile-gated behind the watch feature.
  </done>
</task>

</tasks>

<verification>
1. `cargo check -p native-theme` -- compiles cleanly
2. `cargo test -p native-theme` -- all tests pass
3. `cargo clippy -p native-theme -- -D warnings` -- no warnings
4. Confirm `IconSet` derive line does NOT contain `Default`
5. Confirm no `#[default]` attribute on any IconSet variant
6. Confirm `on_theme_change` re-export is gated: `#[cfg(feature = "watch")]`
</verification>

<success_criteria>
- IconSet does not implement Default (no derive, no #[default] variant attribute)
- All existing tests pass unchanged
- system_icon_set() is unaffected and remains the platform-correct API
- WATCH-03 confirmed: watch types/functions are compile-gated
</success_criteria>

<output>
After completion, create `.planning/phases/75-non-exhaustive-compile-gate-iconset-default/75-02-SUMMARY.md`
</output>
