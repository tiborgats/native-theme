---
phase: 14-toolkit-connectors
plan: 04
type: execute
wave: 2
depends_on: ["14-03"]
files_modified:
  - connectors/native-theme-gpui/examples/showcase.rs
  - connectors/native-theme-gpui/Cargo.toml
autonomous: false
requirements:
  - CONN-04
  - CONN-09

must_haves:
  truths:
    - "showcase.rs compiles showing a window with gpui-component widgets styled by native-theme"
    - "Theme selector dropdown lists all 17 presets plus OS Theme option"
    - "Changing theme selection updates all widget styles"
    - "Widget gallery demonstrates at least Button, Input, Checkbox, Slider components"
  artifacts:
    - path: "connectors/native-theme-gpui/examples/showcase.rs"
      provides: "gpui-component widget gallery with native-theme integration"
      min_lines: 80
  key_links:
    - from: "connectors/native-theme-gpui/examples/showcase.rs"
      to: "connectors/native-theme-gpui/src/lib.rs"
      via: "native_theme_gpui::to_theme() call for theme application"
      pattern: "native_theme_gpui::to_theme"
    - from: "connectors/native-theme-gpui/examples/showcase.rs"
      to: "native_theme::NativeTheme"
      via: "NativeTheme::preset() and NativeTheme::list_presets()"
      pattern: "NativeTheme::(preset|list_presets)"
---

<objective>
Create a gpui-component example application demonstrating the native-theme-gpui connector with a widget gallery and theme selector dropdown.

Purpose: Proves the gpui connector works end-to-end and serves as documentation for consumers.
Output: A runnable `examples/showcase.rs` that displays a themed widget gallery with live theme switching.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-toolkit-connectors/14-RESEARCH.md
@.planning/phases/14-toolkit-connectors/14-03-SUMMARY.md

@connectors/native-theme-gpui/Cargo.toml
@connectors/native-theme-gpui/src/lib.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create gpui showcase.rs widget gallery with theme selector</name>
  <files>
    connectors/native-theme-gpui/examples/showcase.rs,
    connectors/native-theme-gpui/Cargo.toml
  </files>
  <action>
    1. Update `Cargo.toml` to ensure example can build. gpui may need additional features or dev-dependencies for windowing:
       ```toml
       [[example]]
       name = "showcase"
       ```
       If gpui needs specific features for examples (e.g., windowing backend), add them.

    2. Create `examples/showcase.rs` as a gpui application:

       **Application structure** (follow gpui-component patterns):
       - Use `gpui::Application::new()` to create app
       - Create a main window with `app.open_window()`
       - Build the widget gallery as a view

       **Theme selector:**
       - Use `gpui_component::Dropdown` (or equivalent picker widget) listing all presets + "OS Theme"
       - Each preset name from `NativeTheme::list_presets()` plus "OS Theme" entry
       - On selection: load NativeTheme::preset(), pick variant, call native_theme_gpui::to_theme(), apply to gpui-component's theme system

       **Widget gallery:**
       Layout using gpui v_flex/h_flex:
       - Title label: "native-theme-gpui Showcase"
       - Theme selector dropdown
       - Button section: primary Button, secondary Button, danger Button
       - Input section: TextInput (or StyledInput)
       - Checkbox section: Checkbox with label
       - Slider section: Slider component
       - Any other gpui-component widgets available (e.g., Switch, ProgressBar if available)

       **Theme application:**
       - gpui-component uses `ActiveTheme` trait and `Theme::global_mut()` or theme registry
       - Check gpui_component::theme docs for the correct way to apply a custom theme:
         - If Theme::apply() or similar method exists, use it
         - If the pattern is `cx.set_global(theme)`, use that
         - Research docs: "ActiveTheme trait, Theme::global_mut, ThemeRegistry"
       - When theme changes, re-apply and request repaint

    3. Handle platform restrictions:
       - gpui works on macOS and Linux (not Windows fully)
       - If the example requires platform-specific setup (like Vulkan on Linux), document in a comment at the top
       - If gpui requires features not available on the build machine, add cfg gates:
         ```rust
         #[cfg(any(target_os = "macos", target_os = "linux"))]
         fn main() { /* full app */ }

         #[cfg(not(any(target_os = "macos", target_os = "linux")))]
         fn main() { eprintln!("gpui showcase requires macOS or Linux"); }
         ```

    4. Ensure the example compiles: `cargo build -p native-theme-gpui --example showcase`
       If compilation fails due to system dependencies (Vulkan, Wayland, etc.), document the requirements and ensure `cargo check` passes at minimum.
  </action>
  <verify>
    <automated>cd /home/tibi/Rust/native-theme && (cargo build -p native-theme-gpui --example showcase 2>/dev/null || cargo check -p native-theme-gpui --example showcase 2>/dev/null || echo "Example requires platform-specific deps; cargo check -p native-theme-gpui passed: $(cargo check -p native-theme-gpui 2>&1 | tail -1)")</automated>
  </verify>
  <done>showcase.rs exists with widget gallery and theme selector. Compiles on supported platforms (macOS/Linux). Demonstrates gpui-component widgets styled by native-theme.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify showcase app (if platform supports gpui)</name>
  <files>connectors/native-theme-gpui/examples/showcase.rs</files>
  <action>Human visual verification of the running gpui showcase application. Run `cargo run -p native-theme-gpui --example showcase` (on macOS or Linux with GPU support) and verify the widget gallery renders correctly with theme switching. If platform does not support gpui, review source code and verify `cargo check` passes.</action>
  <verify>Visual inspection confirms widgets render and theme switching works, or code review confirms correctness if platform unavailable</verify>
  <done>User has visually confirmed the showcase app works correctly, or confirmed code review is satisfactory</done>
  <what-built>gpui-component widget gallery showcase with theme selector and native-theme integration</what-built>
  <how-to-verify>
    1. If on macOS or Linux with GPU support:
       Run: `cargo run -p native-theme-gpui --example showcase`
    2. A window should appear with:
       - A title "native-theme-gpui Showcase"
       - A theme selector dropdown
       - Buttons, input, checkbox, slider widgets
    3. Change the theme using the dropdown -- widget colors should update
    4. If gpui cannot run on your platform, verify:
       - `cargo check -p native-theme-gpui --example showcase` passes
       - Review showcase.rs source code for completeness
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (or "skip" if platform doesn't support gpui)</resume-signal>
</task>

</tasks>

<verification>
1. `cargo build -p native-theme-gpui --example showcase` (or `cargo check`) -- compiles
2. Visual inspection confirms widgets and theme switching (on supported platforms)
</verification>

<success_criteria>
- showcase.rs compiles on supported platforms
- Contains widget gallery with multiple gpui-component widgets
- Theme selector dropdown lists all 17 presets + OS Theme
- Theme switching updates widget appearance
</success_criteria>

<output>
After completion, create `.planning/phases/14-toolkit-connectors/14-04-SUMMARY.md`
</output>
