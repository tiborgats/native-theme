---
phase: 25.1-icon-gaps-and-fallback-removal
plan: 02
subsystem: icons
tags: [fallback-removal, freedesktop, sf-symbols, segoe, material, icon-loading]

# Dependency graph
requires:
  - phase: 25.1-01
    provides: "All icon mapping gaps filled (42/42 for freedesktop, material, lucide; known gaps documented for SF/Segoe)"
provides:
  - "Platform loaders return None instead of Material SVG fallback"
  - "load_icon wildcard returns None (no cross-set fallback)"
  - "Clean separation: each icon set is self-contained"
affects: [custom-icon-roles, codegen, connectors]

# Tech tracking
tech-stack:
  added: []
  patterns: ["? operator chains for Option propagation in loaders", "no cross-set fallback anywhere in the icon loading chain"]

key-files:
  created: []
  modified:
    - native-theme/src/freedesktop.rs
    - native-theme/src/sficons.rs
    - native-theme/src/winicons.rs
    - native-theme/src/lib.rs

key-decisions:
  - "Platform loaders use ? chains for clean None propagation instead of if-let with fallback"
  - "winicons uses #[cfg(not(target_os = windows))] let _ = role; None pattern for cross-platform compilation"
  - "Tests for unmapped roles use genuinely unmapped roles (FolderOpen for SF, StatusLoading for Segoe)"

patterns-established:
  - "No cross-set fallback: if icon_name returns None for a set, the loader returns None"
  - "? chain pattern: icon_name(set, role)? -> find_icon(name)? -> read(path).ok()? -> Some(IconData)"

requirements-completed: [FB-01, FB-02, FB-03, FB-04]

# Metrics
duration: 3min
completed: 2026-03-17
---

# Phase 25.1 Plan 02: Fallback Removal Summary

**Removed cross-set Material SVG fallback from all 4 locations (3 platform loaders + load_icon wildcard), ensuring each icon set is self-contained**

## Performance

- **Duration:** 3 min
- **Started:** 2026-03-17T09:46:12Z
- **Completed:** 2026-03-17T09:49:49Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments
- Removed `bundled_icon_svg` import and Material fallback from freedesktop.rs, sficons.rs, winicons.rs
- Replaced load_icon wildcard arm with `_ => None` (no cross-set fallback)
- Rewrote all affected tests to validate new no-fallback behavior
- Updated all doc comments to remove fallback chain references
- Full test suite passes (238 tests + 24 doctests, 0 failures)

## Task Commits

Each task was committed atomically:

1. **Task 1: Remove Material fallback from 3 platform loaders** - `c4dc318` (fix)
2. **Task 2: Remove wildcard Material fallback from load_icon** - `49e1514` (fix)

## Files Created/Modified
- `native-theme/src/freedesktop.rs` - Removed bundled_icon_svg import, rewrote load_freedesktop_icon with ? chains, updated doc comments and tests
- `native-theme/src/sficons.rs` - Removed bundled_icon_svg import, rewrote load_sf_icon with ? chains, updated doc comments and tests
- `native-theme/src/winicons.rs` - Removed bundled_icon_svg import, replaced Material fallback with None, updated doc comments and tests
- `native-theme/src/lib.rs` - Replaced wildcard Material fallback with `_ => None`, updated doc comments and test assertions/comments

## Decisions Made
- Platform loaders use ? chains for clean None propagation instead of nested if-let with Material fallback
- winicons uses `#[cfg(not(target_os = "windows"))] let _ = role; None` pattern for cross-platform compilation
- Tests for unmapped roles use genuinely unmapped roles: FolderOpen (SF Symbols), StatusLoading (Segoe)
- Notification test in freedesktop.rs verifies no panic without asserting Some/None (theme-dependent)

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Phase 25.1 is now complete (both plans executed)
- All icon loading follows the no-cross-set-fallback principle established in Phase 22
- Ready for any future icon work or v0.3.3 release

---
*Phase: 25.1-icon-gaps-and-fallback-removal*
*Completed: 2026-03-17*
