---
phase: 53-preset-completeness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - native-theme/src/presets/adwaita.toml
  - native-theme/src/presets/kde-breeze.toml
autonomous: true
requirements: [PRESET-02, PRESET-03]

must_haves:
  truths:
    - "adwaita.toml light+dark have all 51 interactive state color fields with values from platform-facts.md GNOME column"
    - "kde-breeze.toml light+dark have all 51 interactive state color fields with values from platform-facts.md KDE column"
    - "kde-breeze text_scale values match platform-facts.md section 2.19 KDE column (caption=8.2, section_heading=12.0/700, dialog_title=13.5/700, display=20.0/700)"
    - "Both presets pass resolve() -> validate() pipeline"
  artifacts:
    - path: "native-theme/src/presets/adwaita.toml"
      provides: "Complete adwaita preset with all interactive state colors"
    - path: "native-theme/src/presets/kde-breeze.toml"
      provides: "Complete kde-breeze preset with corrected text_scale and all interactive state colors"
  key_links:
    - from: "native-theme/src/presets/adwaita.toml"
      to: "docs/platform-facts.md"
      via: "GNOME column values for each widget"
    - from: "native-theme/src/presets/kde-breeze.toml"
      to: "docs/platform-facts.md"
      via: "KDE column values for each widget"
---

<objective>
Add explicit interactive state color values to the adwaita and kde-breeze presets, and correct kde-breeze text_scale values. All values sourced from docs/platform-facts.md.

Purpose: Eliminate inheritance fallbacks for state colors and fix ratio-derived text_scale values for the two Linux platform presets.
Output: Two complete preset TOML files with all interactive state color fields populated.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-preset-completeness/53-RESEARCH.md
@docs/platform-facts.md
@docs/inheritance-rules.toml
@native-theme/src/presets/adwaita.toml
@native-theme/src/presets/kde-breeze.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add interactive state colors to adwaita preset</name>
  <files>native-theme/src/presets/adwaita.toml</files>
  <action>
Add ALL interactive state color fields to both light and dark variants of adwaita.toml. Every value MUST come from platform-facts.md GNOME column. The adwaita text_scale is already correct -- do NOT change it.

For each widget section below, add the listed fields inline within the existing `[light.widget]` and `[dark.widget]` sections. Values come from the GNOME column in platform-facts.md.

**Fields to add (26 inherited + 25 soft_option = 51 total per variant):**

INHERITED FIELDS (currently filled by resolve() inheritance -- make explicit):
1. button.hover_background -- platform-facts.md section 2.3 GNOME: Adwaita CSS `.button:hover` bg
2. button.hover_text_color -- section 2.3 GNOME: `font.color` (no change)
3. button.active_text_color -- section 2.3: same as font.color
4. button.disabled_text_color -- use defaults.disabled_text_color value from same preset
5. input.disabled_text_color -- use defaults.disabled_text_color value
6. checkbox.disabled_text_color -- use defaults.disabled_text_color value
7. menu.hover_background -- section 2.6 GNOME: Adwaita CSS `:hover` modelbutton bg
8. menu.hover_text_color -- section 2.6 GNOME: `:hover` fg (no change) -- use menu.font.color value
9. menu.disabled_text_color -- use defaults.disabled_text_color value
10. scrollbar.thumb_hover_color -- section 2.8 GNOME: Adwaita CSS :hover (NOT defaults.muted_color -- see wrong_safety_nets in inheritance-rules.toml)
11. tab.active_background -- section 2.11 GNOME: defaults.background_color
12. tab.active_text_color -- section 2.11 GNOME: defaults.text_color
13. tab.hover_text_color -- use tab.font.color value (inherited from defaults.font.color)
14. sidebar.hover_background -- section 2.12 GNOME: Adwaita CSS row `:hover` bg
15. list.hover_background -- section 2.15 GNOME: Adwaita CSS row `:hover` bg
16. list.hover_text_color -- use list.item_font.color (defaults.font.color)
17. list.disabled_text_color -- use defaults.disabled_text_color value
18. splitter.hover_color -- use splitter.divider_color value (widget-to-widget)
19. combo_box.disabled_text_color -- use defaults.disabled_text_color value
20. segmented_control.active_background -- section 2.25 GNOME: (none) -- use defaults.accent_color
21. segmented_control.active_text_color -- section 2.25 GNOME: (none) -- use defaults.accent_text_color
22. link.hover_text_color -- use link.font.color value (link_color)
23. link.active_text_color -- use link.font.color value (link_color)
24. link.disabled_text_color -- use defaults.disabled_text_color value
25. window.inactive_title_bar_background -- use window.title_bar_background value (or surface_color if no explicit title_bar_bg)
26. window.inactive_title_bar_text_color -- use window.title_bar_font.color value

SOFT_OPTION FIELDS (no inheritance, platform-specific -- look up each in platform-facts.md):
1. button.active_background -- section 2.3 GNOME: Adwaita CSS `.button:active` bg
2. button.disabled_background -- section 2.3 GNOME: Adwaita CSS `.button:disabled` bg
3. input.hover_border_color -- Adwaita CSS entry hover border
4. input.focus_border_color -- Adwaita CSS entry focus border (accent-derived)
5. input.disabled_background -- Adwaita CSS entry disabled bg
6. checkbox.hover_background -- Adwaita CSS check hover bg
7. checkbox.disabled_background -- Adwaita CSS check disabled bg
8. checkbox.unchecked_background -- Adwaita CSS unchecked indicator bg
9. checkbox.unchecked_border_color -- Adwaita CSS unchecked border
10. scrollbar.thumb_active_color -- Adwaita CSS scrollbar :active
11. slider.thumb_hover_color -- Adwaita scale slider thumb hover
12. slider.disabled_fill_color -- Adwaita scale disabled fill
13. slider.disabled_track_color -- Adwaita scale disabled track
14. slider.disabled_thumb_color -- Adwaita scale disabled thumb
15. tab.hover_background -- Adwaita notebook tab hover bg
16. switch.hover_checked_background -- Adwaita switch hover checked
17. switch.hover_unchecked_background -- Adwaita switch hover unchecked
18. switch.disabled_checked_background -- Adwaita switch disabled checked
19. switch.disabled_unchecked_background -- Adwaita switch disabled unchecked
20. switch.disabled_thumb_color -- Adwaita switch disabled thumb
21. combo_box.hover_background -- Adwaita combo hover bg
22. combo_box.disabled_background -- Adwaita combo disabled bg
23. segmented_control.hover_background -- GNOME has no native segmented control; use reasonable value from button hover pattern
24. expander.hover_background -- Adwaita expander row hover bg
25. expander.arrow_color -- Adwaita expander arrow color

READ docs/platform-facts.md sections 2.2-2.28 carefully for each GNOME value. For values marked as `(none)` in the GNOME column or where GNOME has no platform-specific value documented, use sensible Adwaita CSS-based values:
- Hover backgrounds: typically a subtle overlay on base background (e.g., 8-10% darker/lighter)
- Disabled backgrounds: typically desaturated/alpha version of base
- Disabled text: use defaults.disabled_text_color from the preset

For the dark variant: use DIFFERENT colors appropriate for dark mode. Do NOT copy light values to dark.

IMPORTANT: The adwaita preset uses sparse text_scale (only specifying overrides). Do NOT change text_scale. Also do NOT change any existing non-state-color fields.
  </action>
  <verify>
    <automated>cd /home/tibi/Rust/native-theme && cargo test -p native-theme --lib -- all_presets_resolve_validate 2>&1 | tail -5</automated>
  </verify>
  <done>adwaita.toml has all 51 interactive state color fields in both light and dark variants, all sourced from platform-facts.md GNOME column, and passes resolve/validate</done>
</task>

<task type="auto">
  <name>Task 2: Fix kde-breeze text_scale and add interactive state colors</name>
  <files>native-theme/src/presets/kde-breeze.toml</files>
  <action>
Two changes to kde-breeze.toml:

**Part A: Fix text_scale values (both light and dark variants)**

Current ratio-derived values need correction. Use platform-facts.md section 2.19 KDE column:

- caption: size=8.2 (KEEP -- already correct, from smallestReadableFont field 1)
- section_heading: size=12.0 (was 10.0 -- KDE Level 2 = body*1.20, and KDE body=10.0, so 10.0*1.2=12.0), weight=700
- dialog_title: size=13.5 (was 12.0 -- KDE Level 1 = body*1.35, so 10.0*1.35=13.5), weight=700
- display: size=20.0 (KEEP -- KDE has no equivalent, 20.0 is reasonable), weight=700

For both light and dark variants, update:
- `[*.text_scale.section_heading]` size from 10.0 to 12.0
- `[*.text_scale.dialog_title]` size from 12.0 to 13.5

Recalculate line_height for changed entries using KDE's line_height multiplier (1.36 ratio from existing caption: 8.2 * 1.36 = 11.2):
- section_heading: line_height = 12.0 * 1.36 = 16.3 (was 13.6)
- dialog_title: line_height = 13.5 * 1.36 = 18.4 (was 16.3)

**Part B: Add interactive state colors (both light and dark variants)**

Same 51 fields as Task 1, but using the KDE column from platform-facts.md:

INHERITED FIELDS -- same list as Task 1 but with KDE-specific values:
- button.hover_background -- section 2.3 KDE: `[Colors:Button] DecorationHover` blend
- menu.hover_background -- section 2.6 KDE: `[Colors:Selection] BackgroundNormal`
- menu.hover_text_color -- section 2.6 KDE: `[Colors:Selection] ForegroundNormal`
- scrollbar.thumb_hover_color -- section 2.8 KDE: Breeze src thumb hover (NOT defaults.muted_color)
- sidebar.hover_background -- section 2.12 KDE: `[Colors:View] DecorationHover` blend
- list.hover_background -- section 2.15 KDE: `[Colors:View] DecorationHover` blend
- segmented_control.active_background -- section 2.25 KDE: `[Colors:Selection] BackgroundNormal`
- segmented_control.active_text_color -- section 2.25 KDE: `[Colors:Selection] ForegroundNormal`
- window.inactive_title_bar_background -- section 2.2 KDE: `[WM] inactiveBackground`
- window.inactive_title_bar_text_color -- section 2.2 KDE: `[WM] inactiveForeground`
- All *_text_color inherited fields: use the appropriate KDE palette values
- All disabled_text_color fields: use defaults.disabled_text_color from the preset

SOFT_OPTION FIELDS -- platform-specific KDE values from platform-facts.md sections.

For the KDE Breeze Light palette, typical values:
- Light background: #fcfcfc (Window), #eff0f1 (Button), #fcfcfc (View)
- Light foreground: #232627 (Window), #232627 (Button)
- Dark background: #31363b (Window), #31363b (Button), #232629 (View)
- Dark foreground: #eff0f1 (Window), #eff0f1 (Button)
- Accent: #3daee9
- Selection bg: #3daee9, Selection fg: #fcfcfc
- Disabled: #bcc0bf (light), #4d545b (dark)

Use these palette values to derive state colors. For hover states, KDE typically uses DecorationHover blend. For disabled states, use the disabled_text_color or apply disabled_opacity.

READ docs/platform-facts.md for each widget's KDE column for specific values. Do NOT invent values.
  </action>
  <verify>
    <automated>cd /home/tibi/Rust/native-theme && cargo test -p native-theme --lib -- all_presets_resolve_validate 2>&1 | tail -5</automated>
  </verify>
  <done>kde-breeze.toml has corrected text_scale (section_heading=12.0, dialog_title=13.5) and all 51 interactive state color fields in both variants, all from platform-facts.md KDE column</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries -- pure TOML data entry, no user input processing.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-01 | I (Information Disclosure) | preset TOML | accept | Color values are not sensitive data |
</threat_model>

<verification>
- `cargo test -p native-theme --lib -- all_presets_resolve_validate` passes
- Both adwaita.toml and kde-breeze.toml have symmetric light/dark interactive state color fields
- kde-breeze text_scale.section_heading.size = 12.0 (not 10.0)
- kde-breeze text_scale.dialog_title.size = 13.5 (not 12.0)
</verification>

<success_criteria>
- adwaita and kde-breeze presets have all 51 interactive state color fields explicitly set in both variants
- kde-breeze text_scale matches platform-facts.md section 2.19 KDE column
- All preset tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/53-preset-completeness/53-01-SUMMARY.md`
</output>
