---
phase: 53-preset-completeness
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - native-theme/src/presets/nord.toml
  - native-theme/src/presets/dracula.toml
  - native-theme/src/presets/gruvbox.toml
  - native-theme/src/presets/solarized.toml
  - native-theme/src/presets/tokyo-night.toml
  - native-theme/src/presets/one-dark.toml
autonomous: true
requirements: [PRESET-02, PRESET-03]

must_haves:
  truths:
    - "All 6 community presets text_scale uses GNOME values: caption=9/400, section_heading=11/700, dialog_title=15/800, display=20/800"
    - "All 6 community presets have all 51 interactive state color fields in both variants"
    - "State color derivation is consistent across all 6 presets (same overlay/blend patterns)"
    - "All 6 presets pass resolve() -> validate() pipeline"
  artifacts:
    - path: "native-theme/src/presets/nord.toml"
      provides: "Complete Nord preset with GNOME text_scale and palette-derived state colors"
    - path: "native-theme/src/presets/dracula.toml"
      provides: "Complete Dracula preset with GNOME text_scale and palette-derived state colors"
    - path: "native-theme/src/presets/gruvbox.toml"
      provides: "Complete Gruvbox preset with GNOME text_scale and palette-derived state colors"
    - path: "native-theme/src/presets/solarized.toml"
      provides: "Complete Solarized preset with GNOME text_scale and palette-derived state colors"
    - path: "native-theme/src/presets/tokyo-night.toml"
      provides: "Complete Tokyo Night preset with GNOME text_scale and palette-derived state colors"
    - path: "native-theme/src/presets/one-dark.toml"
      provides: "Complete One Dark preset with GNOME text_scale and palette-derived state colors"
  key_links:
    - from: "*.toml"
      to: "docs/platform-facts.md section 2.19"
      via: "GNOME column for text_scale values"
---

<objective>
Fix text_scale values and add interactive state colors to the remaining 6 community presets: nord, dracula, gruvbox, solarized, tokyo-night, one-dark.

Purpose: Eliminate ratio-derived text_scale values and add explicit interactive state colors for all remaining community presets.
Output: Six complete preset TOML files with corrected text_scale and all interactive state color fields populated.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-preset-completeness/53-RESEARCH.md
@docs/platform-facts.md
@docs/inheritance-rules.toml
@native-theme/src/presets/nord.toml
@native-theme/src/presets/dracula.toml
@native-theme/src/presets/gruvbox.toml
@native-theme/src/presets/solarized.toml
@native-theme/src/presets/tokyo-night.toml
@native-theme/src/presets/one-dark.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix text_scale for all 6 community presets</name>
  <files>
    native-theme/src/presets/nord.toml,
    native-theme/src/presets/dracula.toml,
    native-theme/src/presets/gruvbox.toml,
    native-theme/src/presets/solarized.toml,
    native-theme/src/presets/tokyo-night.toml,
    native-theme/src/presets/one-dark.toml
  </files>
  <action>
Replace ratio-derived text_scale values in all 6 presets with GNOME/Adwaita values from platform-facts.md section 2.19. These are Linux-focused community color schemes, so GNOME text_scale is appropriate.

For ALL 6 presets, update both light and dark text_scale sections to:

```toml
[*.text_scale.caption]
size = 9.0
weight = 400

[*.text_scale.section_heading]
size = 11.0
weight = 700

[*.text_scale.dialog_title]
size = 15.0
weight = 800

[*.text_scale.display]
size = 20.0
weight = 800
```

Remove any explicit `line_height` entries from text_scale sections. Let them inherit from defaults.line_height (all community presets have line_height = 1.2 in defaults). The current line_height values (13.8, 16.8, 20.2, 33.6) were computed from the wrong ratio-derived sizes.

IMPORTANT: These are absolute sizes from the GNOME type ramp, NOT relative to the preset's defaults.font.size. Even though community presets have defaults.font.size=14.0, the text_scale sizes are 9.0, 11.0, 15.0, 20.0 (matching adwaita's CSS classes).

For each preset, verify the current text_scale values are indeed the ratio-derived ones before replacing:
- Expected WRONG values: caption≈11.5, section_heading≈14.0, dialog_title≈16.8, display≈28.0
  </action>
  <verify>
    <automated>cd /home/tibi/Rust/native-theme && cargo test -p native-theme --lib -- all_presets_resolve_validate 2>&1 | tail -5</automated>
  </verify>
  <done>All 6 community presets have GNOME text_scale values. No ratio-derived values (11.5, 14.0, 16.8, 28.0) remain in any text_scale section.</done>
</task>

<task type="auto">
  <name>Task 2: Add interactive state colors to all 6 community presets</name>
  <files>
    native-theme/src/presets/nord.toml,
    native-theme/src/presets/dracula.toml,
    native-theme/src/presets/gruvbox.toml,
    native-theme/src/presets/solarized.toml,
    native-theme/src/presets/tokyo-night.toml,
    native-theme/src/presets/one-dark.toml
  </files>
  <action>
Add ALL 51 interactive state color fields to both light and dark variants of all 6 presets. Derive all values from each preset's own palette.

**Consistent derivation pattern for ALL community presets:**

For the 26 INHERITED fields, copy the value that inheritance would have produced from the preset's own defaults section:
- button.hover_text_color = button.font.color (or defaults.font.color if no explicit button font color)
- button.active_text_color = button.font.color
- button.disabled_text_color = defaults.disabled_text_color
- input.disabled_text_color = defaults.disabled_text_color
- checkbox.disabled_text_color = defaults.disabled_text_color
- menu.hover_background = defaults.selection_background
- menu.hover_text_color = defaults.selection_text_color
- menu.disabled_text_color = defaults.disabled_text_color
- scrollbar.thumb_hover_color: derive a visible-but-subtle thumb hover color from the theme (brighter than muted_color since muted_color is a wrong safety net per inheritance-rules.toml)
- tab.active_background = defaults.background_color
- tab.active_text_color = defaults.text_color
- tab.hover_text_color = font.color (defaults.font.color if no explicit tab font)
- sidebar.hover_background = a subtle hover overlay on the sidebar's background
- list.hover_background = a subtle hover overlay on the list's background (or defaults.background_color)
- list.hover_text_color = list.item_font.color (defaults.font.color if no explicit)
- list.disabled_text_color = defaults.disabled_text_color
- splitter.hover_color = splitter.divider_color (or defaults.border.color)
- combo_box.disabled_text_color = defaults.disabled_text_color
- segmented_control.active_background = defaults.accent_color
- segmented_control.active_text_color = defaults.accent_text_color
- link.hover_text_color = link_color (defaults.link_color)
- link.active_text_color = link_color
- link.disabled_text_color = defaults.disabled_text_color
- window.inactive_title_bar_background = defaults.surface_color (title_bar_background source)
- window.inactive_title_bar_text_color = window.title_bar_font.color (defaults.font.color if no explicit)
- button.hover_background = a subtle hover overlay on button background

For the 25 SOFT_OPTION fields, derive from each preset's palette:
Use a consistent pattern across all 6 presets. The key rule: derive state colors from the preset's OWN color values. Pre-compute alpha overlays to solid hex values.

**Derivation patterns (apply to each preset using its own palette):**
- hover_background fields: blend 8-12% of accent or a lighter/darker surface variant over the widget's base bg. For light themes: darken slightly. For dark themes: lighten slightly.
- active_background (button): stronger version of hover (15-20% tonal shift)
- disabled_background fields: use a muted/desaturated version of the widget's base bg with reduced contrast
- Unchecked checkbox bg: use surface_color or a neutral variant
- Unchecked checkbox border: use border.color or muted_color
- Scrollbar thumb_active_color: slightly brighter/stronger than thumb_hover
- Slider disabled colors: desaturated versions of fill/track/thumb
- Switch hover/disabled states: adjusted versions of checked/unchecked backgrounds
- Arrow/expander colors: use muted_color or a contrasting text variant
- Tab hover_background: subtle overlay on tab background
- Focus/hover border: use accent_color or focus_ring_color

**Per-preset palettes (read from each preset's defaults section):**

Nord light: bg=#eceff4, text=#2e3440, surface=#e5e9f0, accent=#5e81ac, muted=#7b88a1, disabled_text=#b8bfcc
Nord dark: bg=#2e3440, text=#eceff4, surface=#3b4252, accent=#88c0d0, muted=#616e88, disabled_text=#5c6478

Dracula light: bg=#f8f8f2, text=#282a36, surface=#f0f0ec, accent=#bd93f9, muted=#6272a4
Dracula dark: bg=#282a36, text=#f8f8f2, surface=#44475a, accent=#bd93f9, muted=#6272a4

Gruvbox light: bg=#fbf1c7, text=#3c3836, surface=#f2e5bc, accent=#d65d0e (or #458588), muted=#928374
Gruvbox dark: bg=#282828, text=#ebdbb2, surface=#3c3836, accent=#fe8019 (or #83a598), muted=#928374

Solarized light: bg=#fdf6e3, text=#657b83, surface=#eee8d5, accent=#268bd2, muted=#93a1a1
Solarized dark: bg=#002b36, text=#839496, surface=#073642, accent=#268bd2, muted=#586e75

Tokyo Night light: bg=#d5d6db, text=#343b59, surface=#cbccd1, accent=#34548a, muted=#9699a3
Tokyo Night dark: bg=#1a1b26, text=#a9b1d6, surface=#24283b, accent=#7aa2f7, muted=#565f89

One Dark light: bg=#fafafa, text=#383a42, surface=#f0f0f0, accent=#4078f2, muted=#a0a1a7
One Dark dark: bg=#282c34, text=#abb2bf, surface=#21252b, accent=#61afef, muted=#5c6370

NOTE: Read the actual preset files to confirm these palette values before using them. The above are approximate. Use the ACTUAL values from the TOML files.

Process for each preset:
1. Read the preset's [light.defaults] and [dark.defaults] sections
2. For each widget section, add the 51 fields using the derivation patterns above
3. Verify light and dark variants have symmetric field counts

Run cargo test after all 6 presets are complete.
  </action>
  <verify>
    <automated>cd /home/tibi/Rust/native-theme && cargo test -p native-theme --lib -- all_presets_resolve_validate 2>&1 | tail -5</automated>
  </verify>
  <done>All 6 community presets have all 51 interactive state color fields in both variants, consistently derived from their own palettes. All tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries -- pure TOML data entry.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-04 | I (Information Disclosure) | preset TOML | accept | Color values are not sensitive data |
</threat_model>

<verification>
- `cargo test -p native-theme --lib -- all_presets_resolve_validate` passes for all 6 presets
- No ratio-derived text_scale values remain (no 11.5, 14.0, 16.8, 28.0 patterns in any text_scale section)
- All 6 presets have symmetric light/dark interactive state color fields
- State color derivation is consistent across all 6 presets (same overlay/blend approach)
</verification>

<success_criteria>
- All 6 community presets have GNOME text_scale values
- All 6 presets have all 51 interactive state color fields in both variants
- All values derived from each preset's own palette (no invented colors)
- All preset tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/53-preset-completeness/53-04-SUMMARY.md`
</output>
