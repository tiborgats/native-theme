---
phase: 53-preset-completeness
plan: 05
type: execute
wave: 2
depends_on: [53-01, 53-02, 53-03, 53-04]
files_modified: []
autonomous: true
requirements: [PRESET-02, PRESET-03]

must_haves:
  truths:
    - "All 16 presets pass resolve() -> validate() with zero inheritance fallbacks for state colors or text_scale"
    - "No ratio-derived text_scale values exist in any preset"
    - "pre-release-check.sh passes"
  artifacts: []
  key_links: []
---

<objective>
Verify all 16 presets are complete: correct text_scale values, all interactive state color fields present, zero inheritance fallbacks firing, and pre-release-check.sh passes.

Purpose: Final quality gate confirming SC1-SC4 are met before phase is marked complete.
Output: Verified phase completion.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-preset-completeness/53-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Comprehensive preset verification</name>
  <files></files>
  <action>
Run a comprehensive verification of all 16 presets to confirm Phase 53 success criteria are met.

**SC1 verification -- text_scale correctness:**
For each of the 16 presets, grep text_scale entries and verify:
- No preset has text_scale values matching the old ratio pattern: caption~11.5, section_heading~14.0, dialog_title~16.8, display~28.0
- 3 presets with correct values from start (adwaita, macos-sonoma, windows-11): unchanged
- kde-breeze: section_heading=12.0, dialog_title=13.5
- ios: caption=12.0, section_heading=15.0, dialog_title=22.0, display=34.0
- material: caption=11.0, section_heading=16.0, dialog_title=24.0, display=36.0
- All 10 community presets: caption=9.0, section_heading=11.0, dialog_title=15.0, display=20.0

Run: `grep -n "size = " native-theme/src/presets/*.toml | grep text_scale` and verify no ratio-derived values exist.

**SC2 verification -- interactive state color completeness:**
For each of the 16 presets, count the number of interactive state color fields present. Each preset should have the following fields in EACH variant (light and dark):

26 inherited fields: button.hover_background, button.hover_text_color, button.active_text_color, button.disabled_text_color, input.disabled_text_color, checkbox.disabled_text_color, menu.hover_background, menu.hover_text_color, menu.disabled_text_color, scrollbar.thumb_hover_color, tab.active_background, tab.active_text_color, tab.hover_text_color, sidebar.hover_background, list.hover_background, list.hover_text_color, list.disabled_text_color, splitter.hover_color, combo_box.disabled_text_color, segmented_control.active_background, segmented_control.active_text_color, link.hover_text_color, link.active_text_color, link.disabled_text_color, window.inactive_title_bar_background, window.inactive_title_bar_text_color

25 soft_option fields: button.active_background, button.disabled_background, input.hover_border_color, input.focus_border_color, input.disabled_background, checkbox.hover_background, checkbox.disabled_background, checkbox.unchecked_background (already existed), checkbox.unchecked_border_color, scrollbar.thumb_active_color, slider.thumb_hover_color, slider.disabled_fill_color, slider.disabled_track_color, slider.disabled_thumb_color, tab.hover_background, switch.hover_checked_background, switch.hover_unchecked_background, switch.disabled_checked_background, switch.disabled_unchecked_background, switch.disabled_thumb_color, combo_box.hover_background, combo_box.disabled_background, segmented_control.hover_background, expander.hover_background, expander.arrow_color

Note: Some fields like checkbox.unchecked_background, defaults.disabled_text_color, and sidebar.hover_background already existed before Phase 53. Count those as present.

Run a grep for each expected field across all presets to verify completeness.

**SC3 verification -- zero inheritance fallbacks:**
Run `cargo test -p native-theme --lib -- all_presets_resolve_validate` to confirm all presets pass resolve() -> validate().

The key test: for inherited fields, if a preset provides an explicit value, the `if field.is_none()` branch in resolve() won't fire. We can verify this indirectly: all tests pass, and all fields are explicitly present in the TOML.

**SC4 verification -- pre-release-check.sh:**
Run `./pre-release-check.sh` and confirm it passes.

If any verification fails, report the specific failures with preset name, field name, and expected vs actual values.
  </action>
  <verify>
    <automated>cd /home/tibi/Rust/native-theme && ./pre-release-check.sh 2>&1 | tail -10</automated>
  </verify>
  <done>All 16 presets verified: correct text_scale, all 51 interactive state color fields present in both variants, all tests pass, pre-release-check.sh passes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries -- read-only verification.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (none) | - | - | - | Verification-only plan, no new code |
</threat_model>

<verification>
- `cargo test -p native-theme` full test suite passes
- `./pre-release-check.sh` passes
- grep confirms no ratio-derived text_scale values
- grep confirms all 51 interactive state color fields present across all 16 presets
</verification>

<success_criteria>
- SC1: All 16 presets have correct text_scale values (verified by grep)
- SC2: All 16 presets have all interactive state color fields (verified by field count)
- SC3: Zero inheritance fallbacks for state colors or text_scale (verified by test pass + field presence)
- SC4: pre-release-check.sh passes
</success_criteria>

<output>
After completion, create `.planning/phases/53-preset-completeness/53-05-SUMMARY.md`
</output>
