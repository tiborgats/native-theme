---
phase: 32-documentation-and-release
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - native-theme/src/model/animated.rs
  - CHANGELOG.md
  - native-theme/README.md
autonomous: true
requirements: [DOC-01, DOC-02, DOC-03]

must_haves:
  truths:
    - "AnimatedIcon, TransformAnimation, Repeat, and first_frame() each have a rustdoc # Examples section with runnable code"
    - "CHANGELOG.md contains a v0.4.0 entry listing all Added/Changed/Removed items from phases 27-31"
    - "CHANGELOG.md contains a Migration from v0.3.x subsection with before/after code for StatusLoading replacement"
    - "native-theme/README.md has an Animated Icons section mentioning loading_indicator() and prefers_reduced_motion()"
  artifacts:
    - path: "native-theme/src/model/animated.rs"
      provides: "Rustdoc examples for AnimatedIcon, TransformAnimation, Repeat, first_frame()"
      contains: "# Examples"
    - path: "CHANGELOG.md"
      provides: "v0.4.0 release entry with migration guide"
      contains: "[0.4.0]"
    - path: "native-theme/README.md"
      provides: "Animated Icons section in core crate README"
      contains: "Animated Icons"
  key_links:
    - from: "native-theme/src/model/animated.rs"
      to: "cargo test --doc"
      via: "rustdoc test runner"
      pattern: "# Examples"
    - from: "CHANGELOG.md"
      to: "loading_indicator"
      via: "migration guide references correct API"
      pattern: "loading_indicator"
---

<objective>
Add rustdoc examples to all new animated icon types, write the v0.4.0 CHANGELOG entry with migration guide, and update the core README with an Animated Icons section.

Purpose: This is the final phase of the v0.4.0 Animated Icons milestone. All implementation is complete (phases 27-31). Consumers upgrading from v0.3.3 need documentation to discover the new animated icon API and migrate away from the removed StatusLoading variant.

Output: Documented animated.rs types with passing doctests, complete CHANGELOG v0.4.0 entry, migration guide, updated README.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-documentation-and-release/32-RESEARCH.md
@native-theme/src/model/animated.rs
@CHANGELOG.md
@native-theme/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add rustdoc examples to animated icon types</name>
  <files>native-theme/src/model/animated.rs</files>
  <action>
Add `# Examples` sections to the four items that are missing them in `native-theme/src/model/animated.rs`:

1. **`AnimatedIcon` enum** -- Add an `# Examples` section showing construction of both variants:
   - `Frames` variant with a Vec of `IconData::Svg(...)` frames, `frame_duration_ms: 83`, `repeat: Repeat::Infinite`
   - `Transform` variant with `IconData::Svg(...)` and `TransformAnimation::Spin { duration_ms: 1000 }`

2. **`TransformAnimation` enum** -- Add an `# Examples` section showing `Spin` construction:
   - `TransformAnimation::Spin { duration_ms: 1000 }`

3. **`Repeat` enum** -- Add an `# Examples` section showing `Infinite` construction:
   - `Repeat::Infinite` (brief, one-liner example)

4. **`first_frame()` method** -- Add an `# Examples` section showing:
   - Calling `first_frame()` on a `Frames` variant and asserting `Some`
   - Calling `first_frame()` on a `Transform` variant and asserting `Some`

All examples must be plain ```rust doc blocks (NOT `ignore` or `no_run`) because these types are available without feature gates. Use `IconData::Svg(b"<svg>...</svg>".to_vec())` for icon data construction.

Import path in examples: `use native_theme::{AnimatedIcon, IconData, Repeat, TransformAnimation};`

Do NOT modify any existing code or tests -- only add doc comments.
  </action>
  <verify>
    <automated>cd /home/tibi/Rust/native-theme && cargo test --doc -p native-theme 2>&1 | tail -5</automated>
  </verify>
  <done>All four items (AnimatedIcon, TransformAnimation, Repeat, first_frame) have `# Examples` doc sections, and `cargo test --doc -p native-theme` passes with the new doctests included.</done>
</task>

<task type="auto">
  <name>Task 2: Write CHANGELOG v0.4.0 entry, migration guide, and README animated icons section</name>
  <files>CHANGELOG.md, native-theme/README.md</files>
  <action>
**CHANGELOG.md** -- Add a `## [0.4.0] - 2026-03-18` section at the top (below the header, above the existing `[0.3.3]` entry). Follow the established Keep a Changelog format with backtick-quoted types and functions.

### Added section items (enumerate all v0.4.0 additions from phases 27-31):
- `AnimatedIcon` enum with `Frames` and `Transform` variants for animated icon data
- `TransformAnimation` enum with `Spin` variant for continuous rotation
- `Repeat` enum controlling animation looping behavior
- `AnimatedIcon::first_frame()` method returning a static fallback frame
- `loading_indicator(icon_set)` function dispatching to platform-appropriate spinner animations
- `prefers_reduced_motion()` function querying OS accessibility settings (Linux gsettings, macOS NSWorkspace, Windows UISettings)
- Bundled spinner animations for Material (12 arc frames), Lucide (spin transform), macOS (12 spoke frames), Windows (60 arc frames), and GNOME Adwaita (20 arc frames) styles
- Freedesktop sprite sheet parser for runtime `process-working.svg` animation loading
- gpui connector: `animated_frames_to_image_sources()` and `with_spin_animation()` for animation playback
- iced connector: `animated_frames_to_svg_handles()` and `spin_rotation_radians()` for animation playback

### Changed section:
- `IconRole::StatusLoading` renamed to `IconRole::StatusBusy` (static icon for busy state)

### Removed section:
- `IconRole::StatusLoading` variant (use `loading_indicator()` for animated loading indicators, or `IconRole::StatusBusy` for a static busy icon)

### Migration from v0.3.x subsection:
Add a `### Migration from v0.3.x` subsection after Removed. Include:

**Before (v0.3.x):** code block showing `load_icon(IconRole::StatusLoading, "material")` usage.

**After (v0.4.0):** code block showing:
1. `loading_indicator("material")` for animated spinners with match on Frames/Transform
2. `prefers_reduced_motion()` check with `first_frame()` fallback
3. `load_icon(IconRole::StatusBusy, "material")` for static busy icon

Use `rust,ignore` for migration code blocks (they reference removed variants).

Also add a comparison link at the bottom of the file: `[0.4.0]: https://github.com/tiborgats/native-theme/compare/v0.3.3...v0.4.0`

**native-theme/README.md** -- Add an "Animated Icons" section AFTER the "Custom Icon Roles" section and BEFORE the "Feature Flags" section. Keep it concise (similar length to the Custom Icon Roles section). Include:

1. Brief description: platform-native loading indicator animations
2. Code example showing `loading_indicator("material")` usage with match on variants
3. Mention `prefers_reduced_motion()` for accessibility
4. Note that toolkit connectors provide playback helpers

Use `rust,ignore` for code blocks (they require feature gates).
  </action>
  <verify>
    <automated>cd /home/tibi/Rust/native-theme && grep -c "\[0.4.0\]" CHANGELOG.md && grep -c "Animated Icons" native-theme/README.md && grep -c "Migration from v0.3.x" CHANGELOG.md && grep -c "prefers_reduced_motion" native-theme/README.md</automated>
  </verify>
  <done>CHANGELOG.md contains a complete v0.4.0 entry with Added/Changed/Removed sections and a Migration from v0.3.x subsection. native-theme/README.md contains an Animated Icons section mentioning both loading_indicator() and prefers_reduced_motion(). All four grep counts return at least 1.</done>
</task>

</tasks>

<verification>
```bash
# 1. All doc examples compile and pass
cargo test --doc -p native-theme

# 2. Docs build without warnings
cargo doc -p native-theme --no-deps 2>&1 | grep -i warning || echo "No warnings"

# 3. CHANGELOG has v0.4.0 entry
grep "\[0.4.0\]" CHANGELOG.md

# 4. README has animated icons section
grep "Animated Icons" native-theme/README.md

# 5. Migration guide present
grep "Migration from v0.3.x" CHANGELOG.md
```
</verification>

<success_criteria>
1. `cargo test --doc -p native-theme` passes with new doctests for AnimatedIcon, TransformAnimation, Repeat, and first_frame()
2. `cargo doc -p native-theme --no-deps` builds with zero warnings
3. CHANGELOG.md contains a well-formed v0.4.0 entry with all additions from phases 27-31, the StatusLoading breaking change, and a migration guide
4. native-theme/README.md contains an Animated Icons section between Custom Icon Roles and Feature Flags
</success_criteria>

<output>
After completion, create `.planning/phases/32-documentation-and-release/32-01-SUMMARY.md`
</output>
