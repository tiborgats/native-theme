---
phase: 12-widget-metrics
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - native-theme/src/kde/metrics.rs
  - native-theme/src/kde/mod.rs
  - native-theme/src/windows.rs
  - native-theme/src/macos.rs
  - native-theme/src/gnome/mod.rs
autonomous: true
requirements: [METRIC-04, METRIC-05, METRIC-06, METRIC-07]

must_haves:
  truths:
    - "KDE reader populates widget_metrics from breezemetrics.h constants"
    - "Windows reader populates widget_metrics from GetSystemMetricsForDpi + WinUI3 defaults"
    - "macOS reader populates widget_metrics from HIG defaults"
    - "GNOME reader populates widget_metrics from libadwaita defaults"
    - "All platform build_theme functions produce a theme with widget_metrics set"
  artifacts:
    - path: "native-theme/src/kde/metrics.rs"
      provides: "breeze_widget_metrics() -> WidgetMetrics"
      contains: "breeze_widget_metrics"
    - path: "native-theme/src/kde/mod.rs"
      provides: "widget_metrics wired into from_kde_content"
      contains: "widget_metrics"
    - path: "native-theme/src/windows.rs"
      provides: "read_widget_metrics(dpi) -> WidgetMetrics"
      contains: "read_widget_metrics"
    - path: "native-theme/src/macos.rs"
      provides: "macos_widget_metrics() -> WidgetMetrics"
      contains: "macos_widget_metrics"
    - path: "native-theme/src/gnome/mod.rs"
      provides: "widget_metrics populated in GNOME theme output"
      contains: "widget_metrics"
  key_links:
    - from: "native-theme/src/kde/metrics.rs"
      to: "native-theme/src/kde/mod.rs"
      via: "pub(crate) mod metrics; call in from_kde_content"
      pattern: "metrics::breeze_widget_metrics"
    - from: "native-theme/src/kde/mod.rs"
      to: "native-theme/src/model/widget_metrics.rs"
      via: "uses WidgetMetrics type"
      pattern: "WidgetMetrics"
    - from: "native-theme/src/windows.rs"
      to: "native-theme/src/model/widget_metrics.rs"
      via: "uses WidgetMetrics type in build_theme"
      pattern: "WidgetMetrics"
    - from: "native-theme/src/macos.rs"
      to: "native-theme/src/model/widget_metrics.rs"
      via: "uses WidgetMetrics type in build_theme"
      pattern: "WidgetMetrics"
---

<objective>
Populate widget metrics from all four platform readers: KDE (breezemetrics.h constants), Windows (GetSystemMetricsForDpi + WinUI3), macOS (HIG defaults), and GNOME (libadwaita defaults).

Purpose: Platform readers now produce per-widget sizing data, enabling toolkit connectors to render pixel-perfect native widgets.
Output: Four platform readers updated to populate widget_metrics on their ThemeVariant output.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-widget-metrics/12-RESEARCH.md
@.planning/phases/12-widget-metrics/12-01-SUMMARY.md

@native-theme/src/kde/mod.rs
@native-theme/src/windows.rs
@native-theme/src/macos.rs
@native-theme/src/gnome/mod.rs

<interfaces>
<!-- From Plan 01 output (WidgetMetrics types) -->

From native-theme/src/model/widget_metrics.rs (created by Plan 01):
```rust
pub struct WidgetMetrics {
    pub button: ButtonMetrics,
    pub checkbox: CheckboxMetrics,
    pub input: InputMetrics,
    pub scrollbar: ScrollbarMetrics,
    pub slider: SliderMetrics,
    pub progress_bar: ProgressBarMetrics,
    pub tab: TabMetrics,
    pub menu_item: MenuItemMetrics,
    pub tooltip: TooltipMetrics,
    pub list_item: ListItemMetrics,
    pub toolbar: ToolbarMetrics,
    pub splitter: SplitterMetrics,
}
// Each sub-struct has Option<f32> fields.
// All support Default::default() and ..Default::default() spread.
```

From native-theme/src/model/mod.rs (updated by Plan 01):
```rust
pub struct ThemeVariant {
    pub colors: ThemeColors,
    pub fonts: ThemeFonts,
    pub geometry: ThemeGeometry,
    pub spacing: ThemeSpacing,
    pub widget_metrics: Option<WidgetMetrics>,
}
```

From native-theme/src/kde/mod.rs (existing construction pattern):
```rust
let variant = crate::ThemeVariant {
    colors: theme_colors,
    fonts: theme_fonts,
    geometry: Default::default(),
    spacing: Default::default(),
};
```

From native-theme/src/macos.rs (existing construction):
```rust
fn build_theme(light_colors, dark_colors, fonts) -> NativeTheme {
    // Both variants populated, geometry/spacing default
}
```

From native-theme/src/windows.rs (existing construction):
```rust
fn build_theme(accent, fg, bg, accent_shades, fonts, spacing, geometry) -> NativeTheme {
    let variant = crate::ThemeVariant { colors, geometry, fonts, spacing };
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: KDE breeze metrics and macOS/GNOME hardcoded metrics</name>
  <files>native-theme/src/kde/metrics.rs, native-theme/src/kde/mod.rs, native-theme/src/macos.rs, native-theme/src/gnome/mod.rs</files>
  <behavior>
    - Test: breeze_widget_metrics() returns non-empty WidgetMetrics
    - Test: breeze button.min_width == Some(80.0), checkbox.indicator_size == Some(20.0), scrollbar.width == Some(21.0)
    - Test: from_kde_content produces theme with widget_metrics set (existing integration test pattern)
    - Test: macos_widget_metrics() returns non-empty WidgetMetrics with button.min_height == Some(22.0)
    - Test: macOS build_theme populates widget_metrics on both variants
    - Test: GNOME build_theme includes widget_metrics via adwaita preset (which will have widget_metrics after Plan 03, but here we add hardcoded fallback directly)
  </behavior>
  <action>
    **KDE (METRIC-04):**

    1. Create `native-theme/src/kde/metrics.rs` with `pub(crate) fn breeze_widget_metrics() -> crate::model::widget_metrics::WidgetMetrics`. Populate ALL 12 sub-structs with values from RESEARCH.md "KDE breezemetrics.h Mapping" section. Every value has a comment with the breezemetrics.h constant name.

    2. In `native-theme/src/kde/mod.rs`:
       - Add `pub mod metrics;` after `pub mod fonts;`
       - Update `from_kde_content()` to set `widget_metrics: Some(metrics::breeze_widget_metrics())` on the ThemeVariant constructor

    3. Add test in `kde/metrics.rs`: `breeze_metrics_not_empty`, `breeze_metrics_spot_check` (verify 3-4 specific values)

    4. Update existing `from_kde_content` integration test to assert widget_metrics is Some.

    **macOS (METRIC-06):**

    5. In `native-theme/src/macos.rs`, add `fn macos_widget_metrics() -> crate::model::widget_metrics::WidgetMetrics` (not pub, module-local). Populate all 12 sub-structs from RESEARCH.md "macOS HIG Defaults" section. Mark function `#[cfg_attr(not(feature = "macos"), allow(dead_code))]` like build_theme.

    6. Update `build_theme()` signature to accept widget_metrics parameter, or simpler: call `macos_widget_metrics()` inside build_theme directly. Wire `widget_metrics: Some(macos_widget_metrics())` into both light and dark ThemeVariant constructors.

    7. Update `from_macos()` if needed (it calls build_theme, so if build_theme handles it internally, no change needed).

    8. Add test: `macos_widget_metrics_spot_check` (button.min_height == Some(22.0), scrollbar.width == Some(15.0)). Update existing `build_theme_geometry_and_spacing_default` test to also assert widget_metrics is Some.

    **GNOME (METRIC-07):**

    9. In `native-theme/src/gnome/mod.rs`, add `fn adwaita_widget_metrics() -> crate::model::widget_metrics::WidgetMetrics` (module-local). Populate from RESEARCH.md "GNOME libadwaita Defaults" section.

    10. In `build_theme()`, after constructing the variant from base preset, set `variant.widget_metrics = Some(adwaita_widget_metrics());`. This ensures metrics are always present even if the base preset doesn't have them yet (though Plan 03 will add them to presets too).

    11. Add test: `adwaita_widget_metrics_spot_check` (button.min_height == Some(34.0)).
  </action>
  <verify>
    <automated>cd /home/tibi/Rust/native-theme && cargo test -p native-theme -- --nocapture 2>&1 | tail -30</automated>
  </verify>
  <done>KDE from_kde_content produces widget_metrics with breeze constants. macOS build_theme produces widget_metrics on both variants. GNOME build_theme includes adwaita widget metrics. All tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Windows reader widget metrics population</name>
  <files>native-theme/src/windows.rs</files>
  <behavior>
    - Test: winui3 fluent defaults populate button/checkbox/input/slider/progress_bar/tab/tooltip/list_item/toolbar/splitter metrics
    - Test: build_theme produces theme with widget_metrics set (existing test pattern)
    - Test: scrollbar/menu_item fields present in returned WidgetMetrics
  </behavior>
  <action>
    **Windows (METRIC-05):**

    1. Add `fn read_widget_metrics(dpi: u32) -> crate::model::widget_metrics::WidgetMetrics` to windows.rs. This function:
       - Calls `GetSystemMetricsForDpi` for scrollbar width (SM_CXVSCROLL), scrollbar thumb height (SM_CYVTHUMB), and menu item height (SM_CYMENU) -- these are the only widgets with system metrics
       - Fills remaining widgets (button, checkbox, input, slider, progress_bar, tab, tooltip, list_item, toolbar, splitter) with WinUI3 Fluent Design hardcoded defaults from RESEARCH.md
       - Gate the `GetSystemMetricsForDpi` calls behind `#[cfg(target_os = "windows")]`; on other platforms, use WinUI3 defaults for scrollbar/menu too (for testability)

    2. Wire into `build_theme()`: add `widget_metrics` parameter of type `crate::model::widget_metrics::WidgetMetrics`, set `widget_metrics: Some(widget_metrics)` on the ThemeVariant constructor.

    3. Update `from_windows()` to call `read_widget_metrics(dpi)` and pass to build_theme.

    4. Update ALL existing `build_theme` test calls to pass a WidgetMetrics argument. Since tests construct build_theme directly, they can pass `crate::model::widget_metrics::WidgetMetrics::default()` or the actual `read_widget_metrics(96)` result.

    5. The read_widget_metrics function on non-Windows (test platform) should return WinUI3 Fluent defaults only (no GetSystemMetricsForDpi calls). Use `#[cfg(target_os = "windows")]` to gate the unsafe system calls, with a cross-platform fallback.

    6. Add tests:
       - `widget_metrics_fluent_defaults`: verify button.min_height == Some(32.0), checkbox.indicator_size == Some(20.0)
       - Update existing `build_theme_dark_mode_populates_dark_variant_only` to verify widget_metrics is Some
  </action>
  <verify>
    <automated>cd /home/tibi/Rust/native-theme && cargo test -p native-theme windows -- --nocapture 2>&1 | tail -20</automated>
  </verify>
  <done>Windows build_theme produces widget_metrics with Fluent defaults and system metrics (when on Windows). All windows tests pass on Linux cross-compilation host.</done>
</task>

</tasks>

<verification>
1. `cargo test -p native-theme` -- all tests pass
2. `cargo test -p native-theme kde::metrics` -- KDE breeze metrics tests pass
3. `cargo test -p native-theme macos::tests` -- macOS tests pass (build_theme includes widget_metrics)
4. `cargo test -p native-theme windows::tests` -- Windows tests pass
5. `cargo build -p native-theme` -- no warnings
</verification>

<success_criteria>
- KDE reader populates all 12 widget metrics sub-structs from breezemetrics.h constants
- Windows reader uses GetSystemMetricsForDpi for scrollbar/menu, WinUI3 defaults for others
- macOS reader uses HIG defaults for all 12 widget types
- GNOME reader uses libadwaita defaults for all 12 widget types
- All existing and new tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/12-widget-metrics/12-02-SUMMARY.md`
</output>
