---
phase: 41-gpui-theme-preset-screenshots
plan: 02
type: execute
wave: 2
depends_on: ["41-01"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "4 gpui screenshot PNG files exist in docs/assets/"
    - "Screenshots show correctly themed gpui-component widgets"
    - "Screenshots are visually acceptable for the README"
  artifacts:
    - path: "docs/assets/gpui-linux-dracula-dark.png"
      provides: "Dracula dark theme gpui screenshot"
    - path: "docs/assets/gpui-linux-nord-light.png"
      provides: "Nord light theme gpui screenshot"
    - path: "docs/assets/gpui-linux-catppuccin-mocha-dark.png"
      provides: "Catppuccin Mocha dark theme gpui screenshot"
    - path: "docs/assets/gpui-linux-macos-sonoma-light.png"
      provides: "macOS Sonoma light theme gpui screenshot"
  key_links:
    - from: "connectors/native-theme-gpui/README.md"
      to: "docs/assets/gpui-linux-*.png"
      via: "img src references"
      pattern: "gpui-linux-.*\\.png"
---

<objective>
Run the gpui screenshot capture script and verify the resulting screenshots are correct.

Purpose: The capture requires spectacle on the user's KDE Wayland desktop -- the user must run the script and verify screenshots visually since they involve window focus and render timing.
Output: 4 verified gpui screenshot PNG files committed to docs/assets/
</objective>

<execution_context>
@/home/tibi/.claude/get-shit-done/workflows/execute-plan.md
@/home/tibi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@scripts/generate_gpui_screenshots.sh
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Run gpui screenshot capture script</name>
  <action>
Run the spectacle-based gpui screenshot capture script on your KDE Wayland desktop:

1. Close or minimize all other windows to avoid focus interference
2. Run: `bash scripts/generate_gpui_screenshots.sh`
3. Do NOT interact with the desktop during capture (spectacle captures the active window)
4. The script captures 4 screenshots with a 3-second delay between launch and capture
5. Expected output: 4 PNG files in `docs/assets/`:
   - `gpui-linux-dracula-dark.png`
   - `gpui-linux-nord-light.png`
   - `gpui-linux-catppuccin-mocha-dark.png`
   - `gpui-linux-macos-sonoma-light.png`
6. If any screenshot captures the wrong window or is blank, re-run the script
  </action>
  <verify>ls -la docs/assets/gpui-linux-*.png 2>/dev/null | wc -l should show 4 files</verify>
  <done>4 gpui screenshot PNG files exist in docs/assets/</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify screenshot quality and theme correctness</name>
  <action>
Visually verify the 4 captured gpui screenshots show correctly themed widgets:

1. Open the 4 PNG files in `docs/assets/`:
   - `gpui-linux-dracula-dark.png` -- should show dark purple/pink Dracula colors
   - `gpui-linux-nord-light.png` -- should show light blue/white Nord colors
   - `gpui-linux-catppuccin-mocha-dark.png` -- should show dark warm Catppuccin colors
   - `gpui-linux-macos-sonoma-light.png` -- should show clean light macOS-style colors
2. Each screenshot should show the gpui-component Buttons tab with various button styles
3. Verify the window content is fully rendered (no blank areas, no partial widgets)
4. Check that the theme colors look correct for each preset
5. Optionally preview the gpui README to verify images render: open `connectors/native-theme-gpui/README.md` in a markdown previewer
  </action>
  <verify>User visual confirmation that all 4 screenshots are correct</verify>
  <done>All 4 screenshots show correctly themed gpui-component Buttons tab, user approved</done>
</task>

</tasks>

<verification>
- All 4 PNG files exist in docs/assets/
- Each file is non-empty (> 1KB)
- User confirmed visual quality
</verification>

<success_criteria>
- 4 gpui screenshot PNG files exist and are visually correct
- User approved the screenshots
- Screenshots show correctly themed gpui-component widgets on the Buttons tab
</success_criteria>

<output>
After completion, create `.planning/phases/41-gpui-theme-preset-screenshots/41-02-SUMMARY.md`
</output>
