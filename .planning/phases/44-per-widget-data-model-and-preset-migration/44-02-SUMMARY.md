---
phase: 44-per-widget-data-model-and-preset-migration
plan: "02"
subsystem: native-theme/model
tags: [model, structs, serde, merge, widgets, defaults, per-widget]
dependency_graph:
  requires:
    - phase: 44-01
      provides: define_widget_pair! macro, impl_merge! optional_nested clause, FontSpec, IconSizes, DialogButtonOrder, ResolvedFontSpec
  provides:
    - ThemeDefaults with ~40 fields (34 Option<T> + 4 non-Option nested structs)
    - 25 XxxTheme structs generated by define_widget_pair! (Option-based, serde-ready)
    - 25 ResolvedXxx structs (concrete types, post-resolution)
    - All 50 types re-exported from model module
  affects:
    - 44-03 (ThemeVariant restructure uses ThemeDefaults + all 25 XxxTheme types)
    - 44-04 (TOML preset rewrites populate ThemeDefaults and widget fields)
    - 44-05 (resolution pipeline reads ThemeDefaults and widget structs)
tech-stack:
  added: []
  patterns:
    - ThemeDefaults uses non-Option nested structs (FontSpec, ThemeSpacing, IconSizes)
      with skip_serializing_if to suppress empty sections in TOML output
    - define_widget_pair! option clause handles any Option<T> including enums (DialogButtonOrder)
    - All 25 widget structs coexist alongside old ThemeVariant model with zero interference

key-files:
  created:
    - native-theme/src/model/defaults.rs
  modified:
    - native-theme/src/model/widgets/mod.rs
    - native-theme/src/model/mod.rs

key-decisions:
  - "ThemeDefaults non-Option nested structs (font, mono_font, spacing, icon_sizes) use
    #[serde(default, skip_serializing_if = \"T::is_empty\")] per-field — consistent with
    how ThemeVariant handles its sub-structs"
  - "All 25 widget ResolvedXxx types are named ResolvedWindow, ResolvedButton (not
    ResolvedWindowTheme) to avoid double-Theme suffix"

patterns-established:
  - "Widget struct naming: XxxTheme (Option-based input) / ResolvedXxx (concrete output)"
  - "ThemeDefaults nested structs: plain T (not Option<T>) with is_empty suppression"

duration: ~4min
completed: "2026-03-27"
---

# Phase 44 Plan 02: ThemeDefaults and 25 Widget Structs Summary

**ThemeDefaults struct with ~40 fields and all 25 per-widget XxxTheme/ResolvedXxx struct pairs generated by define_widget_pair!, coexisting with the existing model with zero breakage.**

## Performance

- **Duration:** ~4 min
- **Started:** 2026-03-27T00:00:00Z
- **Completed:** 2026-03-27
- **Tasks:** 2
- **Files modified:** 3 (1 created, 2 modified)

## Accomplishments

- ThemeDefaults with 34 Option<T> fields (colors, geometry, focus ring, accessibility) and 4 non-Option nested structs (font, mono_font, spacing, icon_sizes) with TOML suppression when empty
- All 25 widget structs using define_widget_pair!: Window, Button, Input, Checkbox, Menu, Tooltip, Scrollbar, Slider, ProgressBar, Tab, Sidebar, Toolbar, StatusBar, List, Popover, Splitter, Separator, Switch, Dialog, Spinner, ComboBox, SegmentedControl, Card, Expander, Link
- All 50 types (25 pairs) re-exported from the model module
- 56 new tests (18 ThemeDefaults + 38 widget); all 297 tests pass, 0 regressions

## Task Commits

Each task was committed atomically:

1. **Task 1: Create ThemeDefaults struct** - `5d442e2` (feat)
2. **Task 2: Create all 25 widget structs using define_widget_pair!** - `747cc81` (feat)

## Files Created/Modified

- `native-theme/src/model/defaults.rs` - ThemeDefaults struct with ~40 fields, impl_merge!, 18 tests
- `native-theme/src/model/widgets/mod.rs` - All 25 XxxTheme + 25 ResolvedXxx pairs, 38 tests
- `native-theme/src/model/mod.rs` - Added `pub mod defaults`, re-exports for ThemeDefaults and all 50 widget types

## Decisions Made

- ThemeDefaults non-Option nested structs use `#[serde(default, skip_serializing_if = "T::is_empty")]` per-field, consistent with how ThemeVariant handles its sub-structs. This suppresses empty `[font]`, `[mono_font]`, `[spacing]`, `[icon_sizes]` sections in TOML output.
- All 25 widget Resolved types are named `ResolvedWindow`, `ResolvedButton`, etc. (not `ResolvedWindowTheme`) to avoid a double "Theme" suffix in names.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## Known Stubs

None. All types are fully functional with merge, is_empty, and serde support.

## Next Phase Readiness

- ThemeDefaults and all 25 widget structs are ready for Plan 03 to wire into ThemeVariant
- The existing ThemeVariant (colors/fonts/geometry/spacing/widget_metrics) coexists unchanged — Plan 03 will restructure it
- `use native_theme::ThemeDefaults` compiles; `use native_theme::ButtonTheme` etc. compile

---
*Phase: 44-per-widget-data-model-and-preset-migration*
*Completed: 2026-03-27*
