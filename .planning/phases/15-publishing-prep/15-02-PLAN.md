---
phase: 15-publishing-prep
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - native-theme/src/color.rs
  - native-theme/src/model/mod.rs
  - native-theme/src/lib.rs
  - CHANGELOG.md
autonomous: true
requirements:
  - PUB-03
  - PUB-04

must_haves:
  truths:
    - "cargo test --doc -p native-theme passes with doc examples on Rgba, ThemeVariant, and NativeTheme struct"
    - "cargo doc -p native-theme --no-deps produces zero warnings"
    - "CHANGELOG.md exists at repo root covering all v0.2 changes in Keep a Changelog format"
  artifacts:
    - path: "native-theme/src/color.rs"
      provides: "Doc examples on Rgba struct"
      contains: "/// # Examples"
    - path: "native-theme/src/model/mod.rs"
      provides: "Doc examples on ThemeVariant and NativeTheme structs, fixed doc links"
      contains: "/// # Examples"
    - path: "native-theme/src/lib.rs"
      provides: "Fixed from_gnome doc link"
    - path: "CHANGELOG.md"
      provides: "v0.2 changelog in Keep a Changelog format"
      contains: "[0.2.0]"
      min_lines: 30
  key_links:
    - from: "native-theme/src/color.rs"
      to: "cargo test --doc"
      via: "rustdoc doc-test compilation"
      pattern: "/// # Examples"
    - from: "native-theme/src/model/mod.rs"
      to: "cargo doc"
      via: "intra-doc link resolution"
      pattern: "crate::Error"
---

<objective>
Add doc examples to the three key public types (Rgba, ThemeVariant, NativeTheme struct), fix all cargo doc warnings, and create CHANGELOG.md.

Purpose: PUB-04 requires doc examples that compile on NativeTheme, Rgba, and ThemeVariant. Currently Rgba and ThemeVariant have no `/// # Examples` blocks. Additionally, 5 cargo doc warnings exist from broken intra-doc links. CHANGELOG.md is required for a proper v0.2 release.
Output: Doc examples added, doc warnings fixed, CHANGELOG.md created.
</objective>

<execution_context>
@/home/tibi/.claude/get-shit-done/workflows/execute-plan.md
@/home/tibi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-publishing-prep/15-RESEARCH.md
@native-theme/src/color.rs
@native-theme/src/model/mod.rs
@native-theme/src/lib.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add doc examples to Rgba, ThemeVariant, NativeTheme and fix doc warnings</name>
  <files>native-theme/src/color.rs, native-theme/src/model/mod.rs, native-theme/src/lib.rs</files>
  <action>
1. Add `/// # Examples` block to the `Rgba` struct doc comment in `native-theme/src/color.rs` (currently has no examples). The example should demonstrate:
   - Creating an opaque color with `Rgba::rgb(0, 120, 215)` and asserting `a == 255`
   - Parsing from hex string: `let parsed: Rgba = "#3daee9".parse().unwrap();`
   - Converting to f32 array with `to_f32_array()`

2. Add `/// # Examples` block to the `ThemeVariant` struct doc comment in `native-theme/src/model/mod.rs` (currently has no examples). The example should demonstrate:
   - Creating a default variant and setting `colors.accent` and `fonts.family`
   - Asserting `!variant.is_empty()`

3. Add `/// # Examples` block to the `NativeTheme` struct doc comment in `native-theme/src/model/mod.rs`. Note: the `impl NativeTheme` methods already have examples. This is for the STRUCT itself. The example should demonstrate:
   - Loading a preset: `NativeTheme::preset("dracula").unwrap()`
   - Parsing from TOML with `NativeTheme::from_toml()`
   - Deep merge with `base.merge(&custom)`

4. Fix 5 broken intra-doc links in `native-theme/src/model/mod.rs`:
   - Line ~139: Change `[`Error::Unavailable`]` to `[`crate::Error::Unavailable`]`
   - Line ~153: Change `[`Error::Format`]` to `[`crate::Error::Format`]`
   - Line ~172: Change `[`Error::Unavailable`]` to `[`crate::Error::Unavailable`]`
   - Line ~196: Change `[`Error::Format`]` to `[`crate::Error::Format`]`

5. Fix broken intra-doc link in `native-theme/src/lib.rs`:
   - Line ~167: Change `[`from_gnome()`]` to `` `from_gnome()` `` (plain code formatting, since it's feature-gated). Add note: "(requires `portal-tokio` or `portal-async-io` feature)".

After all changes, run `cargo test --doc -p native-theme` and `cargo doc -p native-theme --no-deps 2>&1` to verify zero warnings and all doc-tests pass.
  </action>
  <verify>
    <automated>cd /home/tibi/Rust/native-theme && cargo test --doc -p native-theme 2>&1 | tail -5 && echo "---" && cargo doc -p native-theme --no-deps 2>&1 | grep -c "^warning:" | xargs -I{} bash -c 'if [ {} -eq 0 ]; then echo "ZERO doc warnings"; else echo "FAIL: {} doc warnings"; exit 1; fi'</automated>
  </verify>
  <done>All doc-tests pass (including new examples on Rgba, ThemeVariant, NativeTheme struct). `cargo doc -p native-theme --no-deps` produces zero warnings.</done>
</task>

<task type="auto">
  <name>Task 2: Create CHANGELOG.md in Keep a Changelog format</name>
  <files>CHANGELOG.md</files>
  <action>
Create `CHANGELOG.md` at repo root following Keep a Changelog 1.1.0 format. Structure:

```
# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [0.2.0] - 2026-03-09

### Added
- (list new features)

### Changed
- (list changes to existing functionality)

### Removed
- (list removed features)

## [0.1.0] - 2026-03-07

### Added
- (initial release features)
```

For v0.2.0 entries, derive from the ROADMAP phases 9-14:
- **Added**: macOS reader (`from_macos()`), `WidgetMetrics` with 12 per-widget sub-structs, `ThemeGeometry::radius_lg` and `shadow` fields, Linux D-Bus portal backend detection, portal overlay for KDE themes, `native-theme-iced` connector crate, `native-theme-gpui` connector crate, GitHub Actions CI pipeline with cross-platform matrix, Windows accent shade colors (AccentDark1-3, AccentLight1-3), Windows system font and DPI-aware geometry, GNOME font data population
- **Changed**: Restructured as Cargo workspace, flattened `ThemeColors` from nested sub-structs to 36 direct `Option<Rgba>` fields, moved preset API from free functions to `NativeTheme` associated methods (`preset()`, `from_toml()`, `from_file()`, `list_presets()`, `to_toml()`), primary/secondary color fields renamed with prefix (`primary_background`, etc.)
- **Removed**: `CoreColors`, `ActionColors`, `StatusColors`, `InteractiveColors`, `PanelColors`, `ComponentColors` nested sub-structs, free-standing `preset()`, `from_toml()`, `from_file()`, `list_presets()`, `to_toml()` functions (now methods on `NativeTheme`)

For v0.1.0 entries:
- **Added**: Initial release with `NativeTheme` data model (36 semantic color roles, fonts, geometry, spacing), TOML serialization, 17 bundled presets (platform + community), KDE reader, GNOME portal reader, Windows reader, cross-platform `from_system()` dispatch

Keep entries concise but informative. Use bullet points. Link version headings to git tags at bottom:
```
[0.2.0]: https://github.com/tiborgats/native-theme/compare/v0.1.0...v0.2.0
[0.1.0]: https://github.com/tiborgats/native-theme/releases/tag/v0.1.0
```
  </action>
  <verify>
    <automated>cd /home/tibi/Rust/native-theme && test -f CHANGELOG.md && grep -q "\[0.2.0\]" CHANGELOG.md && grep -q "\[0.1.0\]" CHANGELOG.md && grep -q "Keep a Changelog" CHANGELOG.md && echo "CHANGELOG.md valid"</automated>
  </verify>
  <done>CHANGELOG.md exists at repo root with v0.2.0 and v0.1.0 entries in Keep a Changelog 1.1.0 format.</done>
</task>

</tasks>

<verification>
- `cargo test --doc -p native-theme` passes with new doc examples on all three types
- `cargo doc -p native-theme --no-deps 2>&1 | grep -c warning` returns 0
- `CHANGELOG.md` contains both v0.2.0 and v0.1.0 sections
</verification>

<success_criteria>
NativeTheme, Rgba, and ThemeVariant all have `/// # Examples` doc blocks that compile. Zero cargo doc warnings. CHANGELOG.md exists with comprehensive v0.2 change documentation.
</success_criteria>

<output>
After completion, create `.planning/phases/15-publishing-prep/15-02-SUMMARY.md`
</output>
