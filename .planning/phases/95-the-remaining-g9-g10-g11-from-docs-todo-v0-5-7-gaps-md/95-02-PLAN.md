---
phase: 95-the-remaining-g9-g10-g11-from-docs-todo-v0-5-7-gaps-md
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/platform-facts.md
autonomous: true
gap_closure: true
requirements: [G10]
tags: [docs, platform-facts, windows, button-order, winui-3, modern-practice, gap-closure]

must_haves:
  truths:
    - "`docs/platform-facts.md:1481` no longer claims Windows uses 'primary leftmost'. The modern WinUI 3 / Fluent Design reality — 'primary rightmost' for modal ContentDialog — is the primary claim in the dialog table row. This matches the code in `native-theme/src/windows.rs:557`, `native-theme/src/presets/windows-11.toml`, and `native-theme/src/resolve/inheritance.rs::platform_button_order()` which all agree on `PrimaryRight`."
    - "`docs/platform-facts.md:1481` cites an EXACT URL/citation for the modern claim — either the WinUI 3 `ContentDialog` documentation or the Microsoft Style Guide page for Windows modal dialogs. If the live URL cannot be verified during plan execution, the plan must NOT invent a URL; the `verify` step must document the gap honestly and defer to the user (per user memory rule 'NEVER LIE, NEVER INVENT' and the `platform-fact` skill's no-invented-values rule)."
    - "A 'Historical note' paragraph is ADDED (append-only; existing prose untouched) to the dialog-table context section (around lines 1500-1510) that preserves the older Win7 Common Buttons guideline (primary-left) for legacy-application maintainers, with its existing citation (learn.microsoft.com/previous-versions) retained verbatim."
    - "The citation table entry at `docs/platform-facts.md:1803` ('Dialog button order: Windows primary leftmost') is updated to match — either by (a) EDITING the row in place if the existing row IS the historical citation being preserved (in which case the row text changes from 'Windows primary leftmost' to 'Windows primary rightmost (WinUI 3 modern)' with the modern URL replacing or supplementing the Win7 URL), OR (b) APPENDING a sibling row above the existing one with the modern citation, and annotating the existing row as 'Windows primary leftmost (Win7-era, historical note only)'. Planner leaves the form to the executor, with a preference for OPTION B to preserve existing row verbatim (maximises append-only compliance)."
    - "Grep scan confirms no other location in `docs/platform-facts.md` contradicts the modern claim. Search patterns: `primary leftmost`, `primary rightmost`, `primary-left`, `primary-right`, `Windows.*leftmost`, `Windows.*rightmost`, `button_order`. Any contradiction must be reconciled (append-only) or flagged in the SUMMARY."
    - "`cargo doc --no-deps` passes (platform-facts.md is not a rustdoc target, but the surrounding repo state must stay clean)."
    - "`./pre-release-check.sh` green banner (the canonical release gate per G11 principled deviation)."
  artifacts:
    - path: "docs/platform-facts.md"
      provides: "Dialog table row 1481 updated to reflect modern WinUI 3 ContentDialog practice (primary rightmost) with exact URL citation at ~line 1803. Historical note paragraph appended to the surrounding prose (around lines 1500-1510) preserving Win7-era guideline."
      contains: "WinUI 3"
  key_links:
    - from: "docs/platform-facts.md:1481 (Windows column of dialog table)"
      to: "modern WinUI 3 ContentDialog documentation URL"
      via: "exact citation anchor in the citation table (around line 1803)"
      pattern: "learn\\.microsoft\\.com"
    - from: "docs/platform-facts.md prose around line 1500-1510 (Historical note paragraph)"
      to: "existing Win7 Common Buttons guideline URL (line 1803)"
      via: "preserved citation"
      pattern: "previous-versions"
---

<objective>
Close G10 from `docs/todo_v0.5.7_gaps.md` (lines 510-542) — the Windows `button_order` platform-facts refresh. This is a DOCS-ONLY plan: no Rust source code, no presets, no inheritance rules are modified. `native-theme/src/windows.rs:557`, `native-theme/src/presets/windows-11.toml`, and `native-theme/src/resolve/inheritance.rs::platform_button_order()` already agree on `PrimaryRight` (verified from gaps-doc line 537-539). Only the canonical reference doc `docs/platform-facts.md` drifts from modern practice.

**What this plan does:**

1. Refreshes `docs/platform-facts.md:1481` (the Windows column of the dialog table) to cite modern WinUI 3 / Fluent Design sources: WinUI 3 `ContentDialog` documentation and the current Microsoft Style Guide.
2. Adds a "Historical note" paragraph to the surrounding prose preserving the older Win7 Common Buttons guideline (primary-left) for legacy-application maintainers.
3. Greps the entire file for any other "primary leftmost / rightmost / left / right" references and reconciles them against modern practice (append-only where possible).

**What this plan does NOT do:**

- Does NOT invent any URL. Per user memory rule "NEVER LIE, NEVER INVENT" + the `platform-fact` skill's explicit no-invented-values rule, every updated claim MUST carry an exact URL/citation. If `WebFetch` cannot verify a live Microsoft URL, the plan documents the gap honestly (in the SUMMARY) and defers to the user.
- Does NOT modify any Rust source file, preset TOML, or inheritance rule.
- Does NOT rewrite any existing prose. The two existing citations at `docs/platform-facts.md:1803` ("Windows primary leftmost" with Win7-era URL) and the existing dialog-table row at line 1481 are handled via APPEND-ONLY annotation OR SURGICAL IN-PLACE UPDATE that preserves the historical citation (see the must_haves.truths item 4 for the exact rule).

**User memory rules honoured:**

- "NEVER LIE, NEVER INVENT" + `platform-fact` skill no-invented-values — Task 01 includes a live URL-verification step via `WebFetch`. If the URL cannot be located, the task MUST stop and escalate to the user; it MUST NOT substitute a guessed URL.
- "Agents must append, never rewrite" — the Historical note paragraph is APPENDED. The existing Win7 citation is preserved; the modern citation either replaces only the outdated claim in the table cell (minimal surgical edit) or is added as a sibling row.
- "Read spec docs before design changes" — this plan modifies a spec doc (`platform-facts.md`); executor MUST re-read the target lines (1470-1510 and 1800-1810) before editing to confirm line numbers and adjacent content have not drifted.
- "No Co-Authored-By trailer" — commit template at the bottom omits it.
- "Run `./pre-release-check.sh` before committing" — included in verify step.

**Why G10 matters under "perfect v0.5.7":** `platform-facts.md` is the source-of-truth citation doc that all presets, inheritance rules, and tests are verified against. If the reference doc disagrees with the shipped code, any future audit (by a verifier agent or a human reviewer) would re-open the Windows button-order question and wonder which source is authoritative. G10 reconciles the doc with the code and preserves the historical context so nobody re-discovers the drift later.

**Deliverable shape:**

One atomic commit modifying only `docs/platform-facts.md`, two file-internal edit sites:
- Lines ~1481: dialog-table Windows column update + Historical note prose around 1500-1510.
- Lines ~1803: citation entry update/augmentation.

Plus any additional reconciliation edits discovered by the grep scan in Task 02 (same file, same commit).

**Wave assignment:** wave 1 — independent of Plans 95-01 (lib.rs + api-2 doc) and 95-03 (Phase 93 PLAN annotations). Zero file overlap; all three plans run in parallel.
</objective>

<execution_context>
@/home/tibi/Rust/native-theme/.claude/get-shit-done/workflows/execute-plan.md
@/home/tibi/Rust/native-theme/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@docs/todo_v0.5.7_gaps.md
@docs/todo_v0.5.7_native-theme-api-2.md
@docs/platform-facts.md
@.claude/skills/platform-fact/SKILL.md
@native-theme/src/windows.rs
@native-theme/src/presets/windows-11.toml
@native-theme/src/resolve/inheritance.rs
</context>

<tasks>

<task type="auto">
  <name>Task 95-02.01: Verify modern WinUI 3 ContentDialog + Microsoft Style Guide URLs are live and contain the 'primary rightmost' claim</name>
  <files>docs/platform-facts.md</files>
  <action>
**CRITICAL — this task MAY terminate with an escalation checkpoint if live URLs cannot be verified.**

Use the `WebFetch` tool to attempt to locate and verify these two target documents:

1. **WinUI 3 `ContentDialog` documentation.** Best-guess starting URL: `https://learn.microsoft.com/en-us/windows/apps/design/controls/dialogs-and-flyouts/dialogs` OR `https://learn.microsoft.com/en-us/windows/apps/design/controls/dialogs-and-flyouts/contentdialog` OR the Fluent Design overview.

2. **Microsoft Style Guide — Windows modal dialogs / buttons section.** Best-guess starting URL: `https://learn.microsoft.com/en-us/style-guide/a-z-word-list-term-collections/term-collections/ui-text-term-collection` OR the accessibility/dialog subsection of the Microsoft Writing Style Guide.

**WebFetch query prompt for URL 1:** "What does this page say about button order in WinUI 3 / Fluent Design dialogs? Specifically: is the primary (default/affirmative) button placed on the LEFT or the RIGHT in a modal dialog? Quote any relevant sentence and provide the section heading."

**WebFetch query prompt for URL 2:** "Does this page state Microsoft's current (2023+) recommendation for primary-button placement in Windows modal dialogs? Quote any relevant sentence."

**Expected outcome — two possibilities:**

**Case A — at least one URL is live AND explicitly states primary-right for WinUI 3 ContentDialog.** Record the URL, the exact quoted sentence, and the section heading. These become the citation in Task 02. Proceed to Task 02.

**Case B — neither URL is accessible, OR the page content does not explicitly state button-order direction.** STOP. Do NOT invent a URL. Do NOT interpolate/infer/substitute. Create an escalation note in the TASK output (not a file) with:
- The exact URLs attempted.
- The WebFetch response for each (status, summary of what was found).
- A proposed fallback: either (1) the gpui-component source repository showing ContentDialog rendering primary-right (if discoverable), or (2) an Apple-vs-Windows comparative research paper or industry blog that cites the modern MS position with its own reference chain. Propose these fallbacks for USER approval — do NOT unilaterally substitute.

If Case B triggers, this plan's execution PAUSES here and the user is asked for a decision: accept the fallback citation, provide a different URL, or defer G10 to a future milestone. This is an automation-first plan with a built-in escalation checkpoint for the specific case where the `platform-fact` skill's no-invented-values rule prevents automated resolution.

**Additional cross-check (always do, even if Case A succeeds):** Search for any existing Microsoft publication cited in the gpui-component repository or the iced repository that documents Windows dialog button order — these may have already gone through the same URL-verification and can be cited by extension. Use `Grep` over `~/.cargo/registry/src/` is NOT permitted (no automated registry scan); instead, rely on `WebFetch` and documented existing sources.

**Post-task output (required in the task's verify section below):**

If Case A: write to the Bash environment (not a file) a summary line containing: `G10 URL VERIFIED: <exact-url> | quote: "<quoted-sentence>"`. This summary is read by Task 02.

If Case B: write `G10 URL VERIFICATION FAILED` and the full escalation note. Task 02 is gated on Case A succeeding (or a user-provided URL in a revision).
  </action>
  <verify>
    <automated>echo 'G10 URL verification task — see action output. Case A requires a verified URL; Case B escalates to user.'</automated>
  </verify>
  <done>
Either (A) at least one live Microsoft URL is verified via `WebFetch` to contain the "primary rightmost" claim for WinUI 3 `ContentDialog`, with the exact URL and quoted sentence recorded in the task output, OR (B) the verification fails and an escalation note is generated listing the attempted URLs, the WebFetch responses, and proposed fallback citations for user approval. Under Case B, Task 02 does not execute until the user provides a decision.
  </done>
</task>

<task type="auto">
  <name>Task 95-02.02: Update docs/platform-facts.md:1481 dialog-table Windows column + add Historical note + grep-reconcile other primary-left/right references; atomic commit</name>
  <files>docs/platform-facts.md</files>
  <action>
**Prerequisite:** Task 01 must have completed with Case A (URL verified). If Task 01 ended in Case B, this task does not execute until the user provides a verified citation.

**Step 1 — Re-read anchors before editing.**

```
sed -n '1470,1510p' docs/platform-facts.md   # dialog table + prose context
sed -n '1800,1810p' docs/platform-facts.md   # citation rows
```

Confirm line numbers have not drifted. If they have, update all references in this action block to the actual line numbers found.

**Step 2 — Update the dialog-table row at line 1481.**

Current row (verified 2026-04-20):

```
| `button_order`        | primary rightmost             | primary leftmost                  | OK left of Cancel (right-aligned group; Help/Reset left-aligned) | cancel left, affirmative right     |
```

Per-column (|-delimited):
- Col 1: `button_order`
- Col 2: macOS (primary rightmost) — NO CHANGE
- Col 3: Windows (primary leftmost) — **CHANGE** to reflect WinUI 3 modern practice
- Col 4: KDE — NO CHANGE
- Col 5: GNOME — NO CHANGE

**Surgical edit**: change the Windows cell (Col 3) from `primary leftmost` to `primary rightmost (WinUI 3 ContentDialog) ✅`. The ✅ and `(WinUI 3 ContentDialog)` parenthetical signals to readers that this is the modern claim and matches the already-✅-verified pattern used elsewhere in the table (e.g. line 1483 title_font.size shows `13pt **(HIG)**`).

Alternative shape (if column width becomes problematic for markdown rendering): use the shorter form `primary rightmost (WinUI 3)`. Executor picks whichever renders cleanly. The SEMANTIC content — modern practice is primary-rightmost — is non-negotiable.

**Step 3 — Add Historical note paragraph to the prose around lines 1500-1510.**

The existing prose (verified 2026-04-20):

```
Button order convention differs significantly across platforms.
macOS primary action = rightmost. Windows primary = leftmost. KDE:
Help/Reset left-aligned, then stretch, then OK/Apply/Cancel right-aligned
(OK left of Cancel). GNOME: cancel left, affirmative right.
```

**Editing rule per user memory "Agents must append, never rewrite":** the paragraph above describes the OLD Windows state. Per the "append, never rewrite" rule, the paragraph should NOT be rewritten in place. Instead:

OPTION-APPEND (preferred): Leave the existing paragraph unchanged. IMMEDIATELY AFTER it (one blank line, then new paragraph), APPEND:

```markdown

**Historical note on Windows button order** (added 2026-04-20 per
`docs/todo_v0.5.7_gaps.md` §G10): the line above reading "Windows
primary = leftmost" reflects the older Microsoft Common Buttons
guideline from the Win7 era, preserved for maintainers of legacy
Windows applications that follow that convention. Modern WinUI 3
`ContentDialog` uses **primary rightmost** — the primary/affirmative
action sits to the right, with Cancel to its left. The modern
reference is `<VERIFIED-URL-FROM-TASK-01>`:

> <QUOTED-SENTENCE-FROM-TASK-01>

Native-theme's `windows.rs:557`, `presets/windows-11.toml`, and
`resolve/inheritance.rs::platform_button_order()` all track the
modern WinUI 3 practice and return `DialogButtonOrder::PrimaryRight`
on Windows. The dialog-table row above (line 1481) has been updated
to match. See `docs/todo_v0.5.7_gaps.md` §G10 for the full audit
and rationale.
```

**Note for executor:** substitute `<VERIFIED-URL-FROM-TASK-01>` and `<QUOTED-SENTENCE-FROM-TASK-01>` with the exact values produced by Task 01 Case A. Do NOT invent either value.

**Step 4 — Update/augment the citation row at line 1803.**

Current row (verified 2026-04-20):

```
| Dialog button order: Windows primary leftmost | ✅ [MS Command Buttons guideline](https://learn.microsoft.com/en-us/previous-versions/windows/desktop/bb246415(v=vs.85)): OK first, then Cancel, then Apply |
```

Per the must_haves item 4, planner recommends **Option B** (append sibling row, preserve existing row verbatim with an annotation). IMMEDIATELY AFTER line 1803 (before the next row at line 1804), INSERT a new row:

```
| Dialog button order: Windows primary rightmost (WinUI 3 modern) | ✅ [<citation-source-name>](<VERIFIED-URL-FROM-TASK-01>): "<QUOTED-SENTENCE-FROM-TASK-01>" |
```

And EDIT the existing row 1803's claim-side to annotate as historical (minimal surgical edit — ONLY adds parenthetical, does not remove the original citation):

```
| Dialog button order: Windows primary leftmost (Win7-era, historical note — see §G10) | ✅ [MS Command Buttons guideline](https://learn.microsoft.com/en-us/previous-versions/windows/desktop/bb246415(v=vs.85)): OK first, then Cancel, then Apply |
```

The edit preserves the existing URL verbatim; it adds only a parenthetical annotation on the claim side. This is the narrowest possible modification that remains defensible as "append-within-a-cell" rather than a rewrite. Executor verifies the git diff shows only this claim-side addition (no URL deletion, no citation-side content change).

**Step 5 — Grep scan for other primary-left/right references.**

Run:

```
grep -nE 'primary[- ]?(left|right)|primary leftmost|primary rightmost|Windows.*leftmost|Windows.*rightmost' docs/platform-facts.md
```

Expected hits (based on the pre-edit grep in the planning context):
- Line 888: `enums/strings (button_order, font.style, icon_set, icon_theme),` — NOT a button-order claim (just a field list). No action.
- Line 992: `- button_order — platform convention for dialog button arrangement (primary left vs right).` — ABSTRACT description, NOT a per-platform claim. No action.
- Line 1481: dialog table row — handled in Step 2.
- Line 1802: macOS row — NO CHANGE (macOS is correctly primary-right).
- Line 1803: Windows row — handled in Step 4.
- Line 1804: KDE row — NO CHANGE (doesn't say "primary-left/right" for Windows).
- Line 1805: GNOME row — NO CHANGE.

If the grep scan reveals NEW hits not in this list (e.g. a newly-added section that contradicts the modern claim), RECONCILE append-only by annotating the new hit with a §G10 cross-reference. Do not silently skip any hit.

**Step 6 — Atomic commit.**

One commit covering only `docs/platform-facts.md`:

```
docs(95-02): refresh Windows button_order to WinUI 3 modern practice (G10)

docs/platform-facts.md:1481 previously claimed "Windows primary
leftmost", citing an older Microsoft Common Buttons guideline from
the Win7 era. Modern WinUI 3 ContentDialog uses primary-rightmost,
which native-theme's code (windows.rs:557, windows-11.toml,
resolve/inheritance.rs::platform_button_order()) already tracks.
Only the reference doc had drifted.

Updates:
- Line 1481 (dialog table): Windows column switched from "primary
  leftmost" to "primary rightmost (WinUI 3 ContentDialog)".
- Lines ~1500-1510: "Historical note on Windows button order"
  paragraph APPENDED preserving the Win7-era guideline for legacy
  application maintainers, with a cross-reference to the modern
  claim.
- Line 1803 (citation row): existing "Windows primary leftmost"
  row annotated "(Win7-era, historical note — see §G10)" with
  URL preserved verbatim; sibling row added for modern WinUI 3
  citation with the verified URL.

Modern citation: <VERIFIED-URL-FROM-TASK-01>
Quoted: "<QUOTED-SENTENCE-FROM-TASK-01>"

Per user memory rule "NEVER LIE, NEVER INVENT" + platform-fact
skill no-invented-values, the modern URL was verified live via
WebFetch before this commit. Historical Win7 URL preserved
verbatim.

Closes G10 from docs/todo_v0.5.7_gaps.md:510-542. No code changes,
no preset changes, no inheritance-rule changes — the code and
presets already agreed on PrimaryRight; only the reference doc
drifted.
```

No `Co-Authored-By` trailer per user memory rule.

**Step 7 — Post-commit verification.**

```
git log -1 --format=%s                              # expected: docs(95-02): refresh Windows button_order to WinUI 3 modern practice (G10)
git log -1 --format=%B | grep -c "Co-Authored-By"   # expected: 0
git log -1 --name-only | tail -3                    # expected: docs/platform-facts.md (single file)
grep -c "primary rightmost (WinUI 3" docs/platform-facts.md   # expected: >= 2 (dialog table + citation row + historical note all use this phrasing)
grep -c "Historical note on Windows button order" docs/platform-facts.md  # expected: 1
grep -c "§G10" docs/platform-facts.md               # expected: >= 3 (cross-references)
grep -c "bb246415" docs/platform-facts.md           # expected: 1 (Win7 URL preserved verbatim)
grep -n "primary leftmost" docs/platform-facts.md   # expected: ONLY the historical note + citation row, annotated
cargo doc --no-deps -p native-theme --all-features 2>&1 | tail -10  # expected: no changes (rustdoc-unaffected)
./pre-release-check.sh 2>&1 | tail -3               # expected: "All pre-release checks passed successfully!"
```
  </action>
  <verify>
    <automated>cd /home/tibi/Rust/native-theme && grep -c "primary rightmost (WinUI 3" docs/platform-facts.md && grep -c "Historical note on Windows button order" docs/platform-facts.md && grep -c "§G10" docs/platform-facts.md && grep -c "bb246415" docs/platform-facts.md && grep -c "leftmost" docs/platform-facts.md && grep -nE "primary[- ]?(left|right)" docs/platform-facts.md | tail -15 && ./pre-release-check.sh 2>&1 | tail -5</automated>
  </verify>
  <done>
`docs/platform-facts.md:1481` dialog-table Windows column reads "primary rightmost (WinUI 3 ContentDialog)" (matching code reality). A "Historical note on Windows button order" paragraph is APPENDED around lines 1500-1510 preserving the Win7-era guideline verbatim for legacy maintainers with cross-references to §G10. The citation table at line 1803 has the Win7 row annotated as "(Win7-era, historical note — see §G10)" with URL preserved, and a sibling modern WinUI 3 citation row added. The grep scan across the whole file reports no unreconciled "primary leftmost" claims for Windows outside the two annotated historical references. One atomic commit covers only `docs/platform-facts.md` with the template message above and no Co-Authored-By trailer. `./pre-release-check.sh` green. Modern URL was verified live via Task 01's WebFetch — no invented citations.
  </done>
</task>

</tasks>

<verification>
**Plan-level verification** — G10 closed with evidence-cited modern practice and preserved historical context:

| Check | Expected | Command |
|-------|----------|---------|
| Modern claim in dialog table | >= 1 match | `grep -c "primary rightmost (WinUI 3" docs/platform-facts.md` |
| Historical note preserved | exactly 1 | `grep -c "Historical note on Windows button order" docs/platform-facts.md` |
| §G10 cross-references | >= 3 | `grep -c "§G10" docs/platform-facts.md` |
| Win7 URL verbatim | exactly 1 | `grep -c "bb246415" docs/platform-facts.md` |
| Modern URL cited | >= 1 | `grep -cE "microsoft\\.com.*dialog\|microsoft\\.com.*contentdialog\|fluent.*dialog" docs/platform-facts.md` (form depends on verified URL) |
| No unreconciled "primary leftmost" for Windows | all remaining hits annotated historical | `grep -n "primary leftmost" docs/platform-facts.md` — every hit must be inside a "historical" or "§G10" context |
| APPEND-only to prose | minimal non-header deletions | `git diff HEAD~1 HEAD -- docs/platform-facts.md \| grep -c '^-[^-]'` — expected <= 2 (only the Col-3 cell swap in the table row and the claim-side annotation in the citation row, both of which are surgical in-place minimal edits; all other content is APPENDED) |
| pre-release-check green | banner intact | `./pre-release-check.sh 2>&1 \| tail -3` |
| commit has no Co-Authored-By | 0 matches | `git log -1 --format=%B \| grep -c "Co-Authored-By"` |
| commit covers exactly 1 file | 1 path | `git log -1 --name-only \| tail -3` |

**Negative checks** (must NOT appear):
- No Rust file is modified: `git log -1 --name-only` shows ONLY `docs/platform-facts.md`.
- No preset TOML is modified.
- No `Cargo.toml` / `Cargo.lock` modification.
- No inheritance rule change.
- No URL was invented: every URL added in the commit must be reachable via HTTP 200 (best-effort verification via Task 01 WebFetch).
- The Win7 Common Buttons URL (`https://learn.microsoft.com/en-us/previous-versions/windows/desktop/bb246415(v=vs.85)`) is preserved verbatim — confirmed by `grep -c "bb246415"` returning exactly 1.
</verification>

<success_criteria>
- [ ] `docs/platform-facts.md:1481` dialog-table Windows column reads "primary rightmost" with a WinUI 3 qualifier (exact phrasing at executor discretion; semantic content non-negotiable).
- [ ] A "Historical note on Windows button order" paragraph exists in the prose around lines 1500-1510 (post-existing prose paragraph) that preserves the Win7 guideline verbatim with its citation and cross-references §G10.
- [ ] The citation table at line 1803 has the Win7 row annotated "(Win7-era, historical note — see §G10)" with URL preserved; a sibling modern WinUI 3 citation row added with a verified-live URL.
- [ ] Grep scan shows no unreconciled "primary leftmost" claims for Windows outside historical-note context.
- [ ] Modern citation URL was verified live via WebFetch in Task 01 (Case A) — no invented URL per user memory rule + `platform-fact` skill.
- [ ] `cargo doc --no-deps -p native-theme --all-features` unchanged (platform-facts.md is not a rustdoc target).
- [ ] `./pre-release-check.sh` green banner.
- [ ] One atomic commit modifies only `docs/platform-facts.md` with the template message and no Co-Authored-By trailer.
- [ ] Zero Rust code, preset TOML, or inheritance-rule changes.
</success_criteria>

<output>
After completion, create `.planning/phases/95-the-remaining-g9-g10-g11-from-docs-todo-v0-5-7-gaps-md/95-02-SUMMARY.md`. The SUMMARY must include:
- The exact verified URL(s) from Task 01 Case A.
- The quoted sentence(s) that establish modern primary-right practice.
- If Task 01 ended in Case B, the SUMMARY documents the escalation path taken and the user's decision.
</output>
