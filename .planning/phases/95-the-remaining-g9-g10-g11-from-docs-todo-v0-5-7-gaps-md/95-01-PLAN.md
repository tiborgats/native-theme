---
phase: 95-the-remaining-g9-g10-g11-from-docs-todo-v0-5-7-gaps-md
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - native-theme/src/lib.rs
  - docs/todo_v0.5.7_native-theme-api-2.md
autonomous: true
gap_closure: true
requirements: [G9]
tags: [docs, ownership-types, cow, arc-str, principled-deviation, v057-polish, gap-closure]

must_haves:
  truths:
    - "The `SystemTheme` struct's string fields (`name` at lib.rs:371 and `icon_theme` at lib.rs:387) carry rustdoc that documents why `Cow<'static, str>` was chosen over the `Arc<str>` recommendation from doc 2 §J.2/§K.3."
    - "The rationale is evidence-cited: bundled preset names are `'static` string literals (`Cow::Borrowed(static_lit)` is zero allocation, zero refcount), and each resolved theme carries exactly one `name`/`icon_theme` — no dedup benefit — unlike `ResolvedFontSpec::family` which remains `Arc<str>` because 26 widgets × connectors genuinely share font families."
    - "The rustdoc explicitly cross-references `docs/todo_v0.5.7_gaps.md` §G9 as the audit record, and confirms that `ResolvedFontSpec::family` is the sole field keeping `Arc<str>` (the one real dedup case)."
    - "`docs/todo_v0.5.7_native-theme-api-2.md` §J.2 ('B3 refinement: use Arc<str> or Cow<'static, str> for ReaderOutput::name') and §K.3 ('B3 ReaderOutput::name consistency with §31.2 ownership types') each gain an APPEND-ONLY post-script that explains why the uniform-`Arc<str>` recommendation was NOT adopted for the `name`/`icon_theme` axis — same audit reasoning (no dedup benefit, `'static` preset optimisation) — and cross-references §G9 in `docs/todo_v0.5.7_gaps.md` as the definitive record."
    - "Zero Rust source code changes: no struct fields reshape, no type signatures change, no function bodies change. Only rustdoc comments are added to `native-theme/src/lib.rs` near lines 371 and 387."
    - "`cargo doc --no-deps -p native-theme --all-features` succeeds with the new rustdoc (no broken intra-doc links, no warnings introduced)."
    - "`./pre-release-check.sh` green banner unchanged (docs-only changes cannot affect code gates, but the script IS the release gate per Phase 93-07 principled deviation G11)."
  artifacts:
    - path: "native-theme/src/lib.rs"
      provides: "Rustdoc paragraph ABOVE `pub name: Cow<'static, str>,` at line 371 and ABOVE `pub icon_theme: Cow<'static, str>,` at line 387 documenting the ownership-type trade-off per G9."
      contains: "Cow<'static, str>"
    - path: "docs/todo_v0.5.7_native-theme-api-2.md"
      provides: "Two APPEND-ONLY post-script paragraphs: one under §J.2 'B3 refinement' (around line 2642 after the J.2 Recommendation+Confidence block for this subsection) and one under §K.3 (around line 3105 after K.3's paragraph). Each post-script records the G9 principled deviation (no dedup benefit for name/icon_theme; keep Cow<'static, str>) and cross-references `docs/todo_v0.5.7_gaps.md:449-506`."
      contains: "G9"
  key_links:
    - from: "native-theme/src/lib.rs:371 (SystemTheme::name rustdoc)"
      to: "docs/todo_v0.5.7_gaps.md §G9"
      via: "cross-reference in rustdoc comment"
      pattern: "todo_v0\\.5\\.7_gaps\\.md.*G9"
    - from: "docs/todo_v0.5.7_native-theme-api-2.md §J.2 B3 refinement post-script"
      to: "docs/todo_v0.5.7_gaps.md §G9 principled deviation record"
      via: "explicit cross-reference at the end of the post-script"
      pattern: "G9"
    - from: "docs/todo_v0.5.7_native-theme-api-2.md §K.3 post-script"
      to: "docs/todo_v0.5.7_gaps.md §G9 principled deviation record"
      via: "explicit cross-reference at the end of the post-script"
      pattern: "G9"
---

<objective>
Close G9 from `docs/todo_v0.5.7_gaps.md` (lines 449-506) — the Arc<str> vs Cow<'static, str> ownership-type principled deviation for theme-name strings. This is a DOCS-ONLY plan: no Rust source code is modified. Two deliverables:

1. Rustdoc annotation on `SystemTheme::name` (`native-theme/src/lib.rs:371`) and `SystemTheme::icon_theme` (`native-theme/src/lib.rs:387`) explaining why `Cow<'static, str>` was chosen over the `Arc<str>` recommendation from doc 2 §J.2 / §K.3.

2. APPEND-ONLY post-scripts to `docs/todo_v0.5.7_native-theme-api-2.md` §J.2 (around line 2642) and §K.3 (around line 3105) recording the deviation rationale and cross-referencing `docs/todo_v0.5.7_gaps.md` §G9 as the definitive record.

**Audit evidence (from `docs/todo_v0.5.7_gaps.md:480-491`):**
- `SystemTheme::name`: exactly ONE name per resolved theme — no dedup benefit.
- `SystemTheme::icon_theme`: KDE has 2 (`breeze`/`breeze-dark`), others have 1 — no dedup benefit.
- `Theme::name` (`native-theme/src/model/mod.rs:253`): same audit applies.
- `ThemeDefaults::icon_theme` (`native-theme/src/model/defaults.rs:136`): same audit applies.
- `ResolvedFontSpec::family` (`native-theme/src/model/font.rs:241`): 26 widgets × connectors genuinely share font families — real dedup benefit — stays `Arc<str>`.

**The argument for the existing `Cow<'static, str>` choice (to include in the rustdoc):** bundled preset names are `&'static str` literals. `Cow::Borrowed(static_lit)` is zero allocation, zero refcount. `Arc<str>` requires at least one allocation per unique string at construction time. For fields that carry exactly one string per resolved theme, the allocation is pure cost with no dedup offset.

**User memory rules honoured:**
- "Agents must append, never rewrite" — all doc updates are APPEND-ONLY. No existing paragraph in either file is modified; rustdoc is ADDED above the two field declarations (which currently have one-line `/// Theme name (from reader or preset).` and `/// The name of the visual icon theme (e.g. "breeze", "Adwaita").` rustdocs — those one-liners are EXPANDED by prepending the ownership-type rationale; see §edit-rule-for-rustdoc below).
- "NEVER LIE, NEVER INVENT" — no URL is invented; no audit number is invented. All evidence comes from the gaps doc which cites exact file:line anchors.
- "No Co-Authored-By trailer" — the commit message template at the bottom of this plan omits it.
- "Read spec docs before design changes" — this plan IS the spec-docs update; the underlying code remains untouched.

**Edit rule for rustdoc** (reconciling "append, never rewrite" with rustdoc's line-above-field convention): the existing one-line rustdocs at lib.rs:370 (`/// Theme name (from reader or preset).`) and lib.rs:386 (`/// The name of the visual icon theme (e.g. "breeze", "Adwaita").`) are PRESERVED verbatim as the first line. Additional rustdoc lines are APPENDED BELOW the existing one-liner (still before the field declaration, per rustdoc grammar) to explain the ownership-type trade-off. Net effect: existing text is unchanged; new paragraphs are ADDED to the same rustdoc block. This is append-within-a-rustdoc-block and matches the letter and spirit of the append-only rule.

Purpose: make the deviation discoverable at the point of use (anyone reading `SystemTheme::name` in rustdoc sees the rationale inline) and preserve the audit record in the design docs (§J.2 + §K.3 stay honest about what was recommended vs what was shipped and why).

Output: two atomic commits (one per file) OR one atomic commit covering both files — the verify section and success criteria accept either shape.

**Wave assignment:** wave 1 — independent of Plans 95-02 (platform-facts.md) and 95-03 (Phase 93 PLAN annotations). Zero file overlap.
</objective>

<execution_context>
@/home/tibi/Rust/native-theme/.claude/get-shit-done/workflows/execute-plan.md
@/home/tibi/Rust/native-theme/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@docs/todo_v0.5.7_gaps.md
@docs/todo_v0.5.7_native-theme-api-2.md
@native-theme/src/lib.rs
@native-theme/src/model/mod.rs
@native-theme/src/model/defaults.rs
@native-theme/src/model/font.rs
</context>

<tasks>

<task type="auto">
  <name>Task 95-01.01: Expand rustdoc on SystemTheme::name and SystemTheme::icon_theme in native-theme/src/lib.rs to document the G9 principled deviation</name>
  <files>native-theme/src/lib.rs</files>
  <action>
Open `native-theme/src/lib.rs`. Locate the `pub struct SystemTheme { ... }` declaration (starts at line 369). Two fields get expanded rustdoc:

**Field 1 — `SystemTheme::name` at line 371 (field declaration `pub name: Cow<'static, str>,`):**

Currently the rustdoc above the field is a single line at line 370: `/// Theme name (from reader or preset).`

APPEND additional rustdoc lines below the existing one-liner (still before the field declaration), preserving the existing text verbatim as the first line. Target shape (exact text to insert):

```rust
    /// Theme name (from reader or preset).
    ///
    /// # Ownership type — principled deviation from doc 2 §J.2 / §K.3
    ///
    /// This field uses `Cow<'static, str>`, not `Arc<str>`. Doc 2 §J.2
    /// ("B3 refinement: use `Arc<str>` for `ReaderOutput::name`") and
    /// §K.3 recommend uniform `Arc<str>` across `name`, `icon_theme`,
    /// `ReaderOutput::name`, and `ResolvedFontSpec::family`. The audit in
    /// `docs/todo_v0.5.7_gaps.md` §G9 (lines 449-506) concluded that the
    /// uniform recommendation should be adopted ONLY for
    /// [`ResolvedFontSpec::family`](crate::model::font::ResolvedFontSpec)
    /// (where 26 widgets × connectors genuinely share font families), and
    /// should be REVERSED for `name` / `icon_theme` because:
    ///
    /// - Each resolved theme carries exactly ONE `name` — no dedup benefit.
    /// - Bundled preset names are `&'static str` literals; `Cow::Borrowed(static_lit)`
    ///   is zero allocation, zero refcount. `Arc<str>` would require at least one
    ///   allocation per unique string at construction time, paying allocation cost
    ///   for a dedup benefit that is structurally absent.
    ///
    /// The same reasoning applies symmetrically to
    /// [`SystemTheme::icon_theme`](Self::icon_theme),
    /// [`Theme::name`](crate::theme::Theme), and
    /// [`ThemeDefaults::icon_theme`](crate::model::defaults::ThemeDefaults).
    ///
    /// See `docs/todo_v0.5.7_gaps.md` §G9 for the full audit.
    pub name: Cow<'static, str>,
```

**Field 2 — `SystemTheme::icon_theme` at line 387 (field declaration `pub icon_theme: Cow<'static, str>,`):**

Currently the rustdoc above the field is a single line at line 386: `/// The name of the visual icon theme (e.g. "breeze", "Adwaita").`

APPEND additional rustdoc lines below the existing one-liner, preserving the existing text verbatim. Target shape (exact text to insert):

```rust
    /// The name of the visual icon theme (e.g. `"breeze"`, `"Adwaita"`).
    ///
    /// # Ownership type
    ///
    /// `Cow<'static, str>` is used here per the same principled deviation
    /// documented on [`SystemTheme::name`](Self::name) — see `docs/todo_v0.5.7_gaps.md`
    /// §G9. Each resolved theme carries a single icon-theme name (KDE has
    /// exactly two across light/dark variants — `"breeze"` / `"breeze-dark"`;
    /// other platforms have one), so the `Arc<str>` dedup benefit does not apply.
    pub icon_theme: Cow<'static, str>,
```

**CRITICAL — editing discipline:**

1. **Preserve existing one-liner rustdocs verbatim.** The first rustdoc line on each field must remain unchanged (`/// Theme name (from reader or preset).` and `/// The name of the visual icon theme (e.g. "breeze", "Adwaita").` — note the second one uses backticks in the expanded form to match Rust's doc-comment convention for code items; that is NOT a "rewrite", it is the CORRECT escaping; the semantic content is identical).

   **Actually, re-read the existing line first.** If lib.rs:386 currently uses plain double quotes (`"breeze"`, `"Adwaita"`) rather than backticks, preserve EXACTLY as-is and only add the new "# Ownership type" section below. The rule is: what exists, stays. New content is APPENDED.

2. **No `use` changes.** The existing `use std::borrow::Cow;` at top of file is already there (grep confirms); no new imports needed for rustdoc-only edits.

3. **No field reshape.** `pub name: Cow<'static, str>,` and `pub icon_theme: Cow<'static, str>,` stay exactly as written. No type change, no attribute change, no visibility change.

4. **No cross-doc-link must point at a type that does not exist.** The three cross-references in the rustdoc — `crate::model::font::ResolvedFontSpec`, `crate::theme::Theme`, `crate::model::defaults::ThemeDefaults` — must resolve under `cargo doc --no-deps -p native-theme --all-features`. Before committing: run `cargo doc --no-deps -p native-theme --all-features 2>&1 | grep -iE 'warning|error'` and confirm zero new warnings/errors introduced by the intra-doc links. If any link fails to resolve (e.g. the crate root re-exports `Theme` at a different path), demote it to a code-span (`` `ResolvedFontSpec` ``, `` `Theme` ``, `` `ThemeDefaults` ``) rather than inventing a wrong path. **Per user memory rule "NEVER LIE, NEVER INVENT" — if a path cannot be verified to resolve, use a non-linking code span.**

5. **Re-read lib.rs before editing** to confirm line numbers did not shift (STATE.md is current as of 2026-04-20; if Phase 94-03 left the shape unchanged, line 371 and line 387 should match). Use `grep -n "pub name: Cow" native-theme/src/lib.rs` and `grep -n "pub icon_theme: Cow" native-theme/src/lib.rs` to verify.

**Post-edit verification:**

```
grep -c "Principled deviation" native-theme/src/lib.rs   # expected >= 1
grep -c "§G9" native-theme/src/lib.rs                    # expected >= 2 (both fields reference it)
grep -c "todo_v0.5.7_gaps.md" native-theme/src/lib.rs    # expected >= 2
grep -n "pub name: Cow" native-theme/src/lib.rs          # expected exact line (structural check)
grep -n "pub icon_theme: Cow" native-theme/src/lib.rs    # expected exact line
cargo doc --no-deps -p native-theme --all-features 2>&1 | tail -20
```

**No commit yet.** Task 02 commits both files together (or commits them separately but within this plan's scope).
  </action>
  <verify>
    <automated>cd /home/tibi/Rust/native-theme && grep -c "§G9" native-theme/src/lib.rs && grep -c "Principled deviation from doc 2 §J.2" native-theme/src/lib.rs && grep -c "dedup benefit" native-theme/src/lib.rs && grep -n "pub name: Cow" native-theme/src/lib.rs && grep -n "pub icon_theme: Cow" native-theme/src/lib.rs && cargo doc --no-deps -p native-theme --all-features 2>&1 | tail -20</automated>
  </verify>
  <done>
`native-theme/src/lib.rs` has expanded rustdoc above `SystemTheme::name` (line ≈371) and `SystemTheme::icon_theme` (line ≈387) that documents the G9 principled deviation, cross-references `docs/todo_v0.5.7_gaps.md` §G9, and identifies `ResolvedFontSpec::family` as the sole Arc<str> retainer. Existing one-line rustdocs are preserved verbatim. `cargo doc --no-deps -p native-theme --all-features` produces zero new warnings or errors. Field declarations unchanged (types, visibility, names all intact).
  </done>
</task>

<task type="auto">
  <name>Task 95-01.02: Append G9 principled-deviation post-scripts to docs/todo_v0.5.7_native-theme-api-2.md §J.2 and §K.3; create atomic commit for both files</name>
  <files>docs/todo_v0.5.7_native-theme-api-2.md</files>
  <action>
Open `docs/todo_v0.5.7_native-theme-api-2.md`. Two post-scripts to append, each APPEND-ONLY (no existing paragraph is rewritten).

**Post-script 1 — insert after §J.2 B3 refinement block (around line 2642-2643, AFTER the "Confidence: high" line that ends the B3 refinement subsection):**

The existing §J.2 B3 refinement ends with "Confidence: high. This is the standard Rust answer for 'shared immutable string across many owners.'" at line 2641-2642. Immediately AFTER that line (two blank lines, then new content) insert:

```markdown

**Post-script 2026-04-20 — principled deviation per `docs/todo_v0.5.7_gaps.md` §G9:**

The uniform-`Arc<str>` recommendation above was NOT adopted for the
`SystemTheme::name` and `SystemTheme::icon_theme` axis. An audit
(recorded in `docs/todo_v0.5.7_gaps.md:449-506` as §G9) counted the
unique values these fields take in practice and concluded there is
**no dedup benefit**: each resolved theme carries exactly one `name`
(and KDE's two icon-theme names across light/dark are the maximum for
any platform — every other platform has one). Bundled preset names
are `&'static str` literals, so `Cow::Borrowed(static_lit)` costs
zero allocations and zero refcounts; `Arc::from(static_lit)` would
pay one allocation per unique string at construction time for a
dedup that structurally cannot fire.

The recommendation is retained for `ResolvedFontSpec::family` — the
one axis where dedup genuinely applies (26 widgets × connectors
share font families). See `docs/todo_v0.5.7_gaps.md` §G9 for the
audit detail and `native-theme/src/lib.rs` `SystemTheme::name`
rustdoc for the in-source record.

**Net result:** `name` / `icon_theme` stay `Cow<'static, str>`;
`family` stays `Arc<str>`. Mixed ownership is intentional, not
accidental inconsistency.

```

**Post-script 2 — insert after §K.3 (around line 3105, AFTER the "Adopt uniformly across the crate." sentence that ends K.3):**

K.3 ends around line 3104-3105 with "Adopt uniformly across the crate." Immediately AFTER that line (two blank lines, then new content) insert:

```markdown

**Post-script 2026-04-20 — principled deviation per `docs/todo_v0.5.7_gaps.md` §G9:**

"Adopt uniformly across the crate" was softened in the G9 audit
(recorded in `docs/todo_v0.5.7_gaps.md:449-506`). The uniform
`Arc<str>` path was adopted ONLY for `ResolvedFontSpec::family`
(real dedup across 26 widgets × connectors). For `SystemTheme::name`,
`SystemTheme::icon_theme`, `Theme::name`, and `ThemeDefaults::icon_theme`
the audit demonstrated no dedup benefit, and `Cow<'static, str>` was
retained to preserve the zero-allocation fast path on bundled
`&'static str` preset literals. See §J.2 B3 refinement post-script
above for the symmetrical record and `docs/todo_v0.5.7_gaps.md` §G9
for the definitive audit.

```

**Editing discipline per user memory rule "Agents must append, never rewrite":**

- Do NOT edit any existing text in `docs/todo_v0.5.7_native-theme-api-2.md`.
- The two post-scripts are PURE INSERTIONS at the specified anchor points.
- Verify APPEND-ONLY via `git diff docs/todo_v0.5.7_native-theme-api-2.md | grep -c '^-[^-]'` expected `0` (zero non-header deletion lines).

**Read before editing** — re-read lines 2600-2650 and 3090-3110 before making any edits to confirm the anchor line numbers (which can shift if prior edits reshaped the file). If anchors are off, GREP for the exact phrase (`"Confidence: high. This is the standard Rust answer"` for J.2; `"Adopt uniformly across the crate."` for K.3) and insert AFTER that exact phrase.

**Atomic commit** — after BOTH Task 01 (lib.rs) and this task's edits to the api-2 doc are complete, create ONE atomic commit covering both files:

```
docs(95-01): document G9 Arc<str>/Cow<'static, str> principled deviation

G9 closes the ownership-type uniformity recommendation from doc 2
§J.2 / §K.3. The audit in docs/todo_v0.5.7_gaps.md:449-506 shows
that Arc<str> provides no dedup benefit for SystemTheme::name,
SystemTheme::icon_theme, Theme::name, or ThemeDefaults::icon_theme
(each resolved theme carries exactly one such string; bundled
preset names are &'static str literals). Cow<'static, str> is
retained for these fields; Arc<str> is retained for
ResolvedFontSpec::family where dedup across 26 widgets × connectors
genuinely applies.

Files:
- native-theme/src/lib.rs: expand rustdoc on SystemTheme::name (line ~371)
  and SystemTheme::icon_theme (line ~387) to document the deviation
  with cross-reference to docs/todo_v0.5.7_gaps.md §G9.
- docs/todo_v0.5.7_native-theme-api-2.md: APPEND-ONLY post-scripts
  to §J.2 B3 refinement and §K.3 recording the deviation rationale
  and cross-referencing §G9.

Zero Rust code changes. Zero type signatures changed. Rustdoc-only
and design-doc-only.
```

No `Co-Authored-By` trailer per user memory rule.

**Verification after commit:**

```
git log -1 --format=%s                            # expected: docs(95-01): document G9 Arc<str>/Cow<'static, str> principled deviation
git log -1 --format=%B | grep -c "Co-Authored-By" # expected: 0
git log -1 --name-only | tail -5                  # expected: two files — native-theme/src/lib.rs and docs/todo_v0.5.7_native-theme-api-2.md
git diff HEAD~1 HEAD -- docs/todo_v0.5.7_native-theme-api-2.md | grep -c '^-[^-]' # expected: 0 (APPEND-ONLY)
grep -c "Post-script 2026-04-20" docs/todo_v0.5.7_native-theme-api-2.md # expected: 2
grep -c "§G9" docs/todo_v0.5.7_native-theme-api-2.md # expected: >= 2
```
  </action>
  <verify>
    <automated>cd /home/tibi/Rust/native-theme && grep -c "Post-script 2026-04-20" docs/todo_v0.5.7_native-theme-api-2.md && grep -c "§G9" docs/todo_v0.5.7_native-theme-api-2.md && grep -c "no dedup benefit" docs/todo_v0.5.7_native-theme-api-2.md && git diff HEAD -- docs/todo_v0.5.7_native-theme-api-2.md 2>&1 | grep -c '^-[^-]' || echo 'already-committed'</automated>
  </verify>
  <done>
`docs/todo_v0.5.7_native-theme-api-2.md` contains two APPEND-ONLY post-scripts (one under §J.2, one under §K.3) that document the G9 principled deviation and cross-reference `docs/todo_v0.5.7_gaps.md` §G9. No existing content is modified (git diff shows zero non-header deletion lines). One atomic commit covers both this file and `native-theme/src/lib.rs` from Task 01, with the commit message template above and no `Co-Authored-By` trailer. `./pre-release-check.sh` green banner unchanged.
  </done>
</task>

</tasks>

<verification>
**Plan-level verification** — G9 closed as principled deviation with in-source rustdoc + design-doc post-scripts:

| Check | Expected | Command |
|-------|----------|---------|
| lib.rs has G9 rustdoc on SystemTheme::name | >= 1 match | `grep -c "Principled deviation from doc 2 §J.2" native-theme/src/lib.rs` |
| lib.rs cross-references §G9 on both fields | >= 2 matches | `grep -c "§G9" native-theme/src/lib.rs` |
| lib.rs cross-references gaps doc | >= 2 matches | `grep -c "todo_v0.5.7_gaps.md" native-theme/src/lib.rs` |
| lib.rs field declarations unchanged | exact `pub name: Cow<'static, str>,` + `pub icon_theme: Cow<'static, str>,` | `grep -n "pub \(name\\|icon_theme\): Cow" native-theme/src/lib.rs` |
| api-2 doc has 2 post-scripts | exactly 2 matches | `grep -c "Post-script 2026-04-20" docs/todo_v0.5.7_native-theme-api-2.md` |
| api-2 doc references §G9 | >= 2 matches | `grep -c "§G9" docs/todo_v0.5.7_native-theme-api-2.md` |
| APPEND-ONLY to api-2 doc | 0 non-header deletions | `git diff HEAD~1 HEAD -- docs/todo_v0.5.7_native-theme-api-2.md \| grep -c '^-[^-]'` |
| cargo doc --no-deps clean | zero new warnings/errors | `cargo doc --no-deps -p native-theme --all-features 2>&1 \| tail -10` |
| pre-release-check green | banner intact | `./pre-release-check.sh 2>&1 \| tail -3` |
| commit has no Co-Authored-By | 0 matches | `git log -1 --format=%B \| grep -c "Co-Authored-By"` |
| commit covers exactly 2 files | 2 paths | `git log -1 --name-only \| tail -3` |

**Negative checks** (must NOT appear):
- No `Cow<'static, str>` → `Arc<str>` or `Arc<str>` → `Cow<'static, str>` type change in any struct field (grep `git diff HEAD~1 HEAD -- native-theme/` for any line matching `^[-+].*pub .*: \(Arc\\|Cow\)` — expected 0 non-rustdoc matches).
- No new `use std::sync::Arc;` line added (rustdoc-only change).
- No modification to `native-theme/src/model/mod.rs` or `native-theme/src/model/defaults.rs` (this plan annotates ONLY lib.rs; the sibling Theme::name and ThemeDefaults::icon_theme fields are cross-referenced but not touched — the author of G9 mapped the deviation primarily to the lib.rs SystemTheme facade, which is the user-visible entry point).
- No change to `.cargo/audit.toml`, `Cargo.toml` dependency list, or `Cargo.lock`.
</verification>

<success_criteria>
- [ ] `native-theme/src/lib.rs` has expanded rustdoc on `SystemTheme::name` (line ≈371) documenting the G9 principled deviation with cross-reference to `docs/todo_v0.5.7_gaps.md` §G9 and explanation of Arc<str> vs Cow<'static, str> trade-off.
- [ ] `native-theme/src/lib.rs` has expanded rustdoc on `SystemTheme::icon_theme` (line ≈387) documenting the same deviation.
- [ ] Existing one-line rustdocs on both fields are preserved verbatim as the first line of the expanded block (APPEND-within-rustdoc-block discipline).
- [ ] `docs/todo_v0.5.7_native-theme-api-2.md` §J.2 B3 refinement gains one APPEND-ONLY post-script titled "Post-script 2026-04-20 — principled deviation per `docs/todo_v0.5.7_gaps.md` §G9".
- [ ] `docs/todo_v0.5.7_native-theme-api-2.md` §K.3 gains one APPEND-ONLY post-script with the same title and cross-references §J.2's post-script.
- [ ] `cargo doc --no-deps -p native-theme --all-features` succeeds with zero new warnings/errors (intra-doc links resolve).
- [ ] `./pre-release-check.sh` green banner unchanged (docs-only change cannot affect code gates, but the script is the canonical release gate per G11).
- [ ] One atomic git commit covers exactly two files: `native-theme/src/lib.rs` and `docs/todo_v0.5.7_native-theme-api-2.md`.
- [ ] Commit message is `docs(95-01): document G9 Arc<str>/Cow<'static, str> principled deviation` with the body template above.
- [ ] No `Co-Authored-By` trailer in the commit message.
- [ ] Zero Rust code changes: no struct field reshape, no type signature change, no function body change, no import change.
</success_criteria>

<output>
After completion, create `.planning/phases/95-the-remaining-g9-g10-g11-from-docs-todo-v0-5-7-gaps-md/95-01-SUMMARY.md`
</output>
