---
phase: 95-the-remaining-g9-g10-g11-from-docs-todo-v0-5-7-gaps-md
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/93-docs-todo-v0-5-7-gaps-md/93-G11-DEVIATION.md
  - .planning/phases/93-docs-todo-v0-5-7-gaps-md/93-01-PLAN.md
  - .planning/phases/93-docs-todo-v0-5-7-gaps-md/93-02-PLAN.md
  - .planning/phases/93-docs-todo-v0-5-7-gaps-md/93-03-PLAN.md
  - .planning/phases/93-docs-todo-v0-5-7-gaps-md/93-04-PLAN.md
  - .planning/phases/93-docs-todo-v0-5-7-gaps-md/93-05-PLAN.md
  - RELEASING.md
autonomous: true
gap_closure: true
requirements: [G11]
tags: [docs, phase-93, principled-deviation, naga, gpui-component, release-gate, gap-closure, cross-plan-annotation]

must_haves:
  truths:
    - "A new durable cross-plan record `.planning/phases/93-docs-todo-v0-5-7-gaps-md/93-G11-DEVIATION.md` exists as a single-source-of-truth file listing ALL Phase 93 PLAN.md locations where `cargo test --workspace --all-features` is claimed as a must_have or verification criterion, with the principled deviation rationale (Options A/B/C/D analysis, Option D selected) and the exact re-evaluation trigger command — mirroring the content already present at `docs/todo_v0.5.7_gaps.md:546-614` but contextualised for Phase-93-plan readers rather than gap-doc readers."
    - "Each of the five Phase 93 PLAN files (93-01, 93-02, 93-03, 93-04, 93-05) that claims `cargo test --workspace --all-features` in its must_haves or verification section gains a short APPEND-ONLY annotation block at the END of the file (below the existing `<output>` block) that explicitly documents the deviation and points to both `docs/todo_v0.5.7_gaps.md` §G11 and the new `93-G11-DEVIATION.md` file. The annotation block is clearly labelled 'G11 annotation added 2026-04-20' so readers can distinguish append from original content."
    - "The existing frontmatter `must_haves.truths` claims and `verification` command lines that reference `cargo test --workspace --all-features` are PRESERVED VERBATIM. The annotation block does not edit, delete, or rewrite them — it adds context below them, per user memory rule 'Agents must append, never rewrite'."
    - "`RELEASING.md` gains a new section 'Known upstream tool-chain deviations (v0.5.7+)' that lists G11 as the canonical current deviation, cites `docs/todo_v0.5.7_gaps.md` §G11 as the detailed record, and gives the one-command re-evaluation trigger (`cargo update -p gpui-component && cargo test --workspace --all-features`). This makes G11 discoverable from the release maintainer's primary-reading doc, not just from the gaps doc."
    - "Zero source code changes. Zero preset changes. Zero Rust/TOML/Cargo modifications. Only `.md` files in `.planning/phases/93-.../` and `RELEASING.md` are touched."
    - "`./pre-release-check.sh` green banner unchanged (docs-only changes cannot affect code gates; the script IS the release gate per the G11 principled deviation this plan formalises)."
  artifacts:
    - path: ".planning/phases/93-docs-todo-v0-5-7-gaps-md/93-G11-DEVIATION.md"
      provides: "Single-source-of-truth cross-plan record for the Phase-93-wide --workspace deviation. Lists the five Phase 93 PLAN locations that claim --workspace, summarises the Options A/B/C/D analysis (one-line each), records Option D selection, gives the exact re-evaluation trigger command, and cross-references docs/todo_v0.5.7_gaps.md:546-614 as the definitive detailed record."
      contains: "G11"
    - path: ".planning/phases/93-docs-todo-v0-5-7-gaps-md/93-01-PLAN.md"
      provides: "Trailing APPEND-ONLY G11 annotation block below the existing <output> tag."
      contains: "G11 annotation added 2026-04-20"
    - path: ".planning/phases/93-docs-todo-v0-5-7-gaps-md/93-02-PLAN.md"
      provides: "Trailing APPEND-ONLY G11 annotation block below the existing <output> tag."
      contains: "G11 annotation added 2026-04-20"
    - path: ".planning/phases/93-docs-todo-v0-5-7-gaps-md/93-03-PLAN.md"
      provides: "Trailing APPEND-ONLY G11 annotation block below the existing <output> tag."
      contains: "G11 annotation added 2026-04-20"
    - path: ".planning/phases/93-docs-todo-v0-5-7-gaps-md/93-04-PLAN.md"
      provides: "Trailing APPEND-ONLY G11 annotation block below the existing <output> tag."
      contains: "G11 annotation added 2026-04-20"
    - path: ".planning/phases/93-docs-todo-v0-5-7-gaps-md/93-05-PLAN.md"
      provides: "Trailing APPEND-ONLY G11 annotation block below the existing <output> tag."
      contains: "G11 annotation added 2026-04-20"
    - path: "RELEASING.md"
      provides: "New section 'Known upstream tool-chain deviations (v0.5.7+)' listing G11 as the canonical current deviation with the re-evaluation trigger command. Inserted between existing sections — exact insertion point TBD at executor discretion but must be SEMANTICALLY adjacent to tool-chain content (e.g. between the '## Pre-publication checks' section and the '## Publish order' section, or after '## Post-bootstrap cleanup' — the executor picks which landing point reads most natural)."
      contains: "G11"
  key_links:
    - from: "`.planning/phases/93-docs-todo-v0-5-7-gaps-md/93-G11-DEVIATION.md`"
      to: "`docs/todo_v0.5.7_gaps.md:546-614` (definitive detailed record)"
      via: "explicit cross-reference at top of the file with line-number anchor"
      pattern: "todo_v0\\.5\\.7_gaps\\.md.*546"
    - from: "each 93-NN-PLAN.md annotation block"
      to: "`93-G11-DEVIATION.md`"
      via: "relative path reference in the annotation"
      pattern: "93-G11-DEVIATION\\.md"
    - from: "each 93-NN-PLAN.md annotation block"
      to: "`docs/todo_v0.5.7_gaps.md` §G11"
      via: "cross-reference by section anchor"
      pattern: "§G11"
    - from: "`RELEASING.md` 'Known upstream tool-chain deviations' section"
      to: "`docs/todo_v0.5.7_gaps.md` §G11 + `.planning/phases/93-.../93-G11-DEVIATION.md`"
      via: "dual cross-reference"
      pattern: "G11"
---

<objective>
Close G11 from `docs/todo_v0.5.7_gaps.md` (lines 546-614) — the formal Phase-93-wide annotation of the `cargo test --workspace --all-features` → per-crate realignment principled deviation, plus a durable-record entry in the release maintainer's primary-reading doc.

**Why G11 reappears in Phase 95 despite Phase 93-07 already documenting it:**

Phase 93-07 (commit `a6e8d4e docs(93-07): document naga/--workspace principled deviation (G11)`, 2026-04-19) landed the deviation record at `docs/todo_v0.5.7_gaps.md:546-614` and appended a cross-reference to `.planning/phases/93-docs-todo-v0-5-7-gaps-md/deferred-items.md`. Phase 93-VERIFICATION.md (2026-04-19) accepted the deviation via an `overrides_applied: 1` in the verification frontmatter, with `accepted_by: tiborgats` at `2026-04-19T18:28:22Z`.

**What Phase 93-07 did NOT do** (and what Phase 95 G11 closes):

1. The five individual Phase 93 PLAN files (93-01, 93-02, 93-03, 93-04, 93-05) each still contain must_haves or verification command lines that claim `cargo test --workspace --all-features` — grepped and confirmed:
   - `93-01-PLAN.md:23` → must_haves truth: "`cargo test --workspace --all-features` still passes"
   - `93-01-PLAN.md:269,291` → verification command lines
   - `93-02-PLAN.md:211,212` → verification command lines
   - `93-03-PLAN.md:308,330` → verification command lines
   - `93-04-PLAN.md:41` → must_haves truth; `:403` → verification
   - `93-05-PLAN.md:30` → must_haves truth; `:278,475` → verification
   These are now outdated in light of the accepted override, but there is no in-file annotation making the deviation discoverable to a reader of any individual plan. A verifier running plan-by-plan would see the raw `--workspace` claim and not know that the accepted deviation supersedes it.

2. There is no cross-plan single-source-of-truth record that lists "these five Phase 93 plans all claim --workspace; here is why the claim was accepted via deviation". A reader discovering plan 93-04 for the first time would need to cross-reference VERIFICATION.md, deferred-items.md, AND gaps.md §G11 to reconstruct the current state. That is excessive cognitive load for a self-documenting planning system.

3. `RELEASING.md` has no "Known deviations" section. When v0.5.8+ maintainers run the re-evaluation trigger command and get a green `--workspace`, they need to know there was ever a deviation. Currently they would only discover it by archaeological git-log spelunking.

**What Phase 95-03 adds:**

1. **A single cross-plan record** `.planning/phases/93-docs-todo-v0-5-7-gaps-md/93-G11-DEVIATION.md` that:
   - Lists every Phase 93 PLAN file + line number where `--workspace` is still claimed in must_haves/verification text.
   - Summarises Options A/B/C/D (one-line each; detail is in gaps.md §G11).
   - States Option D was selected and accepted by `tiborgats` on `2026-04-19`.
   - Gives the EXACT re-evaluation trigger command so any future maintainer (or agent) can run it without re-discovering.
   - Cross-references the definitive records (`docs/todo_v0.5.7_gaps.md:546-614` and Phase 93-VERIFICATION.md's `overrides` entry).

2. **APPEND-ONLY trailing annotations** on each of the five Phase 93 PLAN files. Each annotation is a short labelled block (< 15 lines) inserted BELOW the existing `<output>` XML tag that tells a plan-file reader: "The `--workspace` claims in this file are superseded by the accepted G11 deviation — see `93-G11-DEVIATION.md`."

3. **A new `RELEASING.md` section** "Known upstream tool-chain deviations (v0.5.7+)" listing G11 as the canonical current deviation with the exact re-evaluation-trigger command. This is the release maintainer's primary-reading doc (already structured with "Pre-publication checks", "Why --no-verify?", "Publish order", "Post-bootstrap cleanup", "Version bumps", "Tagging"), so G11 gains first-class visibility at the release-gate boundary.

**User memory rules honoured:**

- "Agents must append, never rewrite" — NOTHING in existing Phase 93 PLAN files is edited. Every annotation is APPENDED below `<output>`. The existing `must_haves.truths` and `verification` blocks stay verbatim. Verified via `git diff | grep -c '^-[^-]'` expected 0 per plan file touched.
- "NEVER LIE, NEVER INVENT" — every claim cites a specific file:line anchor (Phase 93-VERIFICATION.md frontmatter `overrides`, the exact `accepted_by`/`accepted_at` timestamps, the exact gaps.md line range). No URL is invented.
- "Never bypass human checkpoints" — Option D was ALREADY accepted via Phase 93 VERIFICATION.md's override block (`accepted_by: tiborgats`, `accepted_at: 2026-04-19T18:28:22Z`). This plan does NOT create a new decision; it formalises the already-accepted decision into discoverable places.
- "Run `./pre-release-check.sh` before committing" — included in verify step even though docs-only.
- "No Co-Authored-By trailer" — commit template at bottom omits it.

**Why a NEW file (`93-G11-DEVIATION.md`) and not reuse `deferred-items.md`:**

`deferred-items.md` at `.planning/phases/93-docs-todo-v0-5-7-gaps-md/deferred-items.md` was already updated by Phase 93-07 Plan with a "Resolved during Plan 93-07 (gap closure)" section. That file is a log of deferred-during-execution items; G11 is NOT deferred — it is an accepted principled deviation. The two categories differ semantically, and conflating them would muddy the audit trail. A separate file `93-G11-DEVIATION.md` is the cleanest home — discoverable by filename, specific by content. No user memory rule prohibits adding new files; the rule is "don't rewrite existing ones", which this plan honours by leaving `deferred-items.md` alone.

**Wave assignment:** wave 1 — independent of Plans 95-01 (lib.rs + api-2 doc) and 95-02 (platform-facts.md). Zero file overlap; all three plans run in parallel.

**Deliverable shape:** one atomic commit covering seven files:
- 1 new file (`93-G11-DEVIATION.md`)
- 5 existing Phase 93 PLAN files with APPEND-ONLY trailing annotations
- 1 existing `RELEASING.md` with a new section inserted

Single logical change = single commit. The commit message template below ties it together.
</objective>

<execution_context>
@/home/tibi/Rust/native-theme/.claude/get-shit-done/workflows/execute-plan.md
@/home/tibi/Rust/native-theme/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@docs/todo_v0.5.7_gaps.md
@.planning/phases/93-docs-todo-v0-5-7-gaps-md/93-VERIFICATION.md
@.planning/phases/93-docs-todo-v0-5-7-gaps-md/deferred-items.md
@.planning/phases/93-docs-todo-v0-5-7-gaps-md/93-07-PLAN.md
@.planning/phases/93-docs-todo-v0-5-7-gaps-md/93-07-SUMMARY.md
@.planning/phases/93-docs-todo-v0-5-7-gaps-md/93-01-PLAN.md
@.planning/phases/93-docs-todo-v0-5-7-gaps-md/93-02-PLAN.md
@.planning/phases/93-docs-todo-v0-5-7-gaps-md/93-03-PLAN.md
@.planning/phases/93-docs-todo-v0-5-7-gaps-md/93-04-PLAN.md
@.planning/phases/93-docs-todo-v0-5-7-gaps-md/93-05-PLAN.md
@RELEASING.md
@pre-release-check.sh
</context>

<tasks>

<task type="auto">
  <name>Task 95-03.01: Create 93-G11-DEVIATION.md cross-plan record + append G11 annotation blocks to five Phase 93 PLAN files</name>
  <files>.planning/phases/93-docs-todo-v0-5-7-gaps-md/93-G11-DEVIATION.md, .planning/phases/93-docs-todo-v0-5-7-gaps-md/93-01-PLAN.md, .planning/phases/93-docs-todo-v0-5-7-gaps-md/93-02-PLAN.md, .planning/phases/93-docs-todo-v0-5-7-gaps-md/93-03-PLAN.md, .planning/phases/93-docs-todo-v0-5-7-gaps-md/93-04-PLAN.md, .planning/phases/93-docs-todo-v0-5-7-gaps-md/93-05-PLAN.md</files>
  <action>
**Step 1 — Create the cross-plan record file.**

Write `.planning/phases/93-docs-todo-v0-5-7-gaps-md/93-G11-DEVIATION.md` with the following exact content:

```markdown
# Phase 93 — G11 Principled Deviation Record

**Created:** 2026-04-20 (Phase 95 Plan 03)
**Accepted:** 2026-04-19T18:28:22Z by `tiborgats` (recorded in `93-VERIFICATION.md` frontmatter `overrides` block)
**Deviation ID:** G11 (see `docs/todo_v0.5.7_gaps.md:546-614` for the definitive detailed record)

## What this file is

A single-source-of-truth annotation for the five Phase 93 PLAN files
that still contain `cargo test --workspace --all-features` as a
must_have truth or a verification command. Those raw claims predate
the discovery of the naga 27.0.3 / codespan-reporting 0.12.0 upstream
incompatibility (surfaced during Phase 93-01 execution, documented
during Phase 93-07).

Rather than rewriting the five PLAN files (which would violate the
user memory rule "Agents must append, never rewrite"), this file
serves as the canonical pointer. Each of the five PLAN files has a
short trailing annotation block directing readers here.

## The affected plans

| Plan | File | Claim locations | Resolution |
|------|------|-----------------|------------|
| 93-01 | `93-01-PLAN.md` | `:23` (must_haves truth), `:269`, `:291` (verification command lines) | Superseded by G11; see § below |
| 93-02 | `93-02-PLAN.md` | `:211`, `:212` (verification command lines) | Superseded by G11 |
| 93-03 | `93-03-PLAN.md` | `:308`, `:330` (verification command lines) | Superseded by G11 |
| 93-04 | `93-04-PLAN.md` | `:41` (must_haves truth), `:403` (verification command line) | Superseded by G11 |
| 93-05 | `93-05-PLAN.md` | `:30` (must_haves truth), `:278`, `:475` (verification command lines) | Superseded by G11 |

(Line numbers are snapshot as of 2026-04-20. Line drift is possible
if any of these files is ever edited append-only; the semantic
locations — frontmatter `must_haves.truths` + verification command
lines — are stable.)

## Why the deviation was accepted

**Error signature:**

```
error[E0277]: the trait bound `std::string::String: WriteColor` is not satisfied
   --> ~/.cargo/registry/src/index.crates.io-.../naga-27.0.3/src/front/wgsl/error.rs:113:20
```

Three errors total, all in `naga 27.0.3`, all the same trait-bound mismatch.

**Root cause:** `naga 27.0.3` (pulled transitively by `gpui-component v0.5.1` via `wgpu 27.0.0`) uses `codespan_reporting::term::emit` with `&mut String` as the writer. `codespan-reporting 0.12.0` removed the `impl WriteColor for String` that `0.11.x` provided. `naga 27.0.3` was not yet migrated. The workspace `Cargo.lock:1064-1067` pins `codespan-reporting 0.12.0`, which is correct for every other consumer.

**Options considered (one-line each; full analysis at `docs/todo_v0.5.7_gaps.md:546-614`):**

- **A — `cargo update -p naga --precise 27.0.4`:** Not viable. `naga 27.0.4` does not exist on crates.io (`cargo info naga@27.0.4` → "could not find"). Next release is `28.x`, rejected by `gpui-component v0.5.1`'s `27.x` pin.
- **B — narrow must_have to `-p native-theme`:** Weak framing. This is technically the result, but phrased as "scope narrowing" it reads as hiding the problem rather than honestly documenting the upstream state.
- **C — exclude `connectors/native-theme-gpui` from `[workspace] members`:** Worse. Breaks developer ergonomics (workspace-wide `cargo check` would miss the connector) and propagates the upstream defect into project layout.
- **D — document as principled deviation; align must_have with `./pre-release-check.sh` per-crate posture:** **SELECTED.** The pre-release script is THE release gate for this project since Phase 14-03 (2026-03-09); it runs per-crate tests and treats `native-theme-gpui` as soft via `run_check_soft` (lines 287-294). The `--workspace` claim in the Phase 93 plans was an aspirational generalisation predating the naga discovery.

## The honest acceptance criterion

The current per-crate gate (equivalent to the pre-existing `./pre-release-check.sh` behaviour):

```
cargo test -p native-theme --all-features
cargo test -p native-theme-derive
cargo test -p native-theme-build
cargo test -p native-theme-iced
cargo test -p native-theme-gpui    # soft — may fail on upstream naga issue
```

If all non-gpui crates pass AND the gpui crate either passes OR fails with the known naga signature, the release gate is green.

## Re-evaluation trigger

When `gpui-component` ships a release that either (a) bumps `naga` past 27.0.3 to a `codespan-reporting 0.12.0`-compatible version, or (b) pins `codespan-reporting 0.11.x` in its own `Cargo.toml`, run:

```bash
cargo update -p gpui-component
cargo test --workspace --all-features
```

If both succeed, this deviation is obsolete. Delete this file, delete the trailing annotation blocks from the five Phase 93 PLAN files (or flip them to "resolved"), and restore `--workspace` as the plan must_have in all five files. Track upstream state at:

- https://github.com/gfx-rs/wgpu/issues (naga is part of wgpu)
- https://github.com/zed-industries/zed (gpui-component's upstream)

## Cross-references

- **Definitive detailed record:** `docs/todo_v0.5.7_gaps.md:546-614` (G11 section).
- **Formal acceptance:** `.planning/phases/93-docs-todo-v0-5-7-gaps-md/93-VERIFICATION.md` frontmatter `overrides` block (accepted_by: `tiborgats`, 2026-04-19T18:28:22Z).
- **Original deferred-item note:** `.planning/phases/93-docs-todo-v0-5-7-gaps-md/deferred-items.md` "Resolved during Plan 93-07 (gap closure)" section.
- **Release-maintainer surface:** `RELEASING.md` "Known upstream tool-chain deviations (v0.5.7+)" section (added by Phase 95-03 Task 02).
- **Phase 93-07 plan:** `.planning/phases/93-docs-todo-v0-5-7-gaps-md/93-07-PLAN.md` (the plan that originally landed the deviation in `gaps.md`).
- **Phase 93-07 summary:** `.planning/phases/93-docs-todo-v0-5-7-gaps-md/93-07-SUMMARY.md`.

## Audit invariants

- Zero source code changes to close G11. This is a principled deviation, not a code bug.
- The upstream root cause lives in `gpui-component` / `naga` / `codespan-reporting`; `native-theme` has no path to resolve without forking `gpui-component`.
- The `./pre-release-check.sh` green banner (all 20+ test suites ok, zero ⚠ markers in hard checks, `native-theme v0.5.7 is ready for release.`) IS the release gate.
```

**Step 2 — Append trailing annotation block to each of the five Phase 93 PLAN files.**

For EACH of `93-01-PLAN.md`, `93-02-PLAN.md`, `93-03-PLAN.md`, `93-04-PLAN.md`, `93-05-PLAN.md`:

1. **Re-read the file first** to confirm the existing ending. All five plans end with an `<output>` tag followed by a closing instruction (roughly: `<output>\nAfter completion, create \`.planning/phases/93-docs-todo-v0-5-7-gaps-md/{plan}-SUMMARY.md\`\n</output>`).

2. **Append below the closing `</output>` tag** (two blank lines separator, then new content). Insert the following annotation block, with `{PLAN-ID}` substituted for the plan's 93-NN identifier:

```markdown


<!-- ============================================================
     G11 annotation added 2026-04-20 (Phase 95 Plan 03)
     APPEND-ONLY per user memory rule "Agents must append, never rewrite".
     The must_haves and verification references to `cargo test --workspace
     --all-features` in this file are SUPERSEDED by the accepted G11
     principled deviation.
     ============================================================ -->

## G11 deviation note (added 2026-04-20)

The `cargo test --workspace --all-features` claims in this plan file
(in `must_haves.truths` and/or `<verify>` command lines above) are
SUPERSEDED by the accepted G11 principled deviation.

**Summary:** `cargo test --workspace --all-features` fails to compile
due to upstream `naga 27.0.3` / `codespan-reporting 0.12.0`
incompatibility pulled transitively via `gpui-component v0.5.1`.
The deviation was analysed in detail during Phase 93-07
(Option D selected over Options A/B/C) and ACCEPTED via override
by `tiborgats` on `2026-04-19T18:28:22Z`.

**Current acceptance criterion:** per-crate tests via `./pre-release-check.sh`
(see `RELEASING.md` "Known upstream tool-chain deviations (v0.5.7+)"
section). The script tests each workspace crate individually; it
treats `native-theme-gpui` as soft via `run_check_soft`.

**For the full detail and re-evaluation trigger:** see
`.planning/phases/93-docs-todo-v0-5-7-gaps-md/93-G11-DEVIATION.md`
and `docs/todo_v0.5.7_gaps.md:546-614` (§G11).

**This annotation does NOT edit any existing content in this plan
file.** The original `--workspace` claims stay verbatim above; the
deviation is recorded here as a trailing note so future readers
can see both the original claim AND the accepted supersession.
```

**Editing discipline per user memory rule:**

- NOTHING above the `</output>` tag is modified. The existing frontmatter, objective, tasks, verification, success_criteria, and output blocks stay verbatim.
- The annotation is PURELY ADDITIVE at the end of the file, clearly labelled as added on 2026-04-20 by Phase 95 Plan 03.
- After each file edit, run `git diff -- <file> | grep -c '^-[^-]'` to verify zero non-header deletion lines.

**Step 3 — Bookkeeping: update STATE.md's progress tracker.**

No — STATE.md is updated by the `/gsd:execute-phase` workflow's SUMMARY step; this plan does not edit STATE.md directly. Skip.

**No commit yet.** Task 02 handles the RELEASING.md edit and produces ONE atomic commit for all seven files.
  </action>
  <verify>
    <automated>cd /home/tibi/Rust/native-theme && ls -la .planning/phases/93-docs-todo-v0-5-7-gaps-md/93-G11-DEVIATION.md && grep -c "G11 annotation added 2026-04-20" .planning/phases/93-docs-todo-v0-5-7-gaps-md/93-01-PLAN.md && grep -c "G11 annotation added 2026-04-20" .planning/phases/93-docs-todo-v0-5-7-gaps-md/93-02-PLAN.md && grep -c "G11 annotation added 2026-04-20" .planning/phases/93-docs-todo-v0-5-7-gaps-md/93-03-PLAN.md && grep -c "G11 annotation added 2026-04-20" .planning/phases/93-docs-todo-v0-5-7-gaps-md/93-04-PLAN.md && grep -c "G11 annotation added 2026-04-20" .planning/phases/93-docs-todo-v0-5-7-gaps-md/93-05-PLAN.md && grep -c "93-G11-DEVIATION.md" .planning/phases/93-docs-todo-v0-5-7-gaps-md/93-01-PLAN.md && for f in 93-01-PLAN.md 93-02-PLAN.md 93-03-PLAN.md 93-04-PLAN.md 93-05-PLAN.md; do echo "=== $f diff -/+ ==="; git diff -- ".planning/phases/93-docs-todo-v0-5-7-gaps-md/$f" 2>/dev/null | grep -cE '^[-+][^-+]' || echo '0'; done</automated>
  </verify>
  <done>
`.planning/phases/93-docs-todo-v0-5-7-gaps-md/93-G11-DEVIATION.md` exists and contains the full Options-A-B-C-D summary, the affected-plans table with exact line anchors, the current acceptance criterion, the re-evaluation trigger command, and cross-references to all five related records. Each of the five Phase 93 PLAN files (93-01 through 93-05) has a clearly-labelled "G11 annotation added 2026-04-20 (Phase 95 Plan 03)" APPEND-ONLY block inserted BELOW its existing `</output>` tag. Existing content in each file is preserved verbatim (git diff shows no non-header deletion lines). Ready for Task 02 to append RELEASING.md changes and create the atomic commit.
  </done>
</task>

<task type="auto">
  <name>Task 95-03.02: Add "Known upstream tool-chain deviations" section to RELEASING.md; create atomic commit for all seven files</name>
  <files>RELEASING.md</files>
  <action>
**Step 1 — Re-read RELEASING.md to confirm section structure.**

Based on the planning context grep, RELEASING.md has these sections in order:
- `# Releasing \`native-theme\``
- `## Pre-publication checks` (line 9)
- `### Why \`--no-verify\`?` (line 32)
- `## Publish order` (line 46)
- `## Post-bootstrap cleanup` (line 86)
- `## Version bumps` (line 104)
- `## Tagging` (line 114)

**Step 2 — Choose insertion point and write new section.**

**Recommended insertion point:** AFTER `## Post-bootstrap cleanup` (around line 103) and BEFORE `## Version bumps` (line 104). The "Known upstream tool-chain deviations" section logically follows Pre-publication-checks / Why-no-verify / Publish-order / Post-bootstrap-cleanup (all operational release steps) and precedes Version-bumps / Tagging (project-level state changes). It sits adjacent to the other tool-chain content. Executor may pick a different placement if the resulting reading flow is better.

**Content of the new section (exact text to insert):**

```markdown

## Known upstream tool-chain deviations (v0.5.7+)

This section lists current principled deviations from the
"cargo test --workspace --all-features" ideal that are in effect
because of upstream issues outside this project's control. Each
deviation has a specific re-evaluation trigger; when the upstream
state changes, run the trigger command and delete the deviation
entry from this section.

### G11 · `cargo test --workspace --all-features` → per-crate posture (active since 2026-04-19)

**Status:** active. Accepted `2026-04-19T18:28:22Z` by `tiborgats`.

**Why:** `cargo test --workspace --all-features` fails to compile
due to `naga 27.0.3` (transitively via `gpui-component v0.5.1`) being
written against `codespan-reporting 0.11.x`'s `impl WriteColor for String`,
which `codespan-reporting 0.12.0` (workspace `Cargo.lock:1064-1067`)
removed. `naga 27.0.4` does not exist on crates.io; `naga 28.x`
would require a `gpui-component` version bump that hasn't shipped.

**Current release gate (what this script actually enforces):**

```bash
./pre-release-check.sh
```

The script (lines 287-294) runs `cargo test -p <crate>` per workspace
crate and treats `native-theme-gpui` as soft via `run_check_soft`
(a failure becomes a warning, not a blocker). This per-crate
posture has been the release gate since Phase 14-03 (2026-03-09).

**Re-evaluation trigger command:**

```bash
cargo update -p gpui-component && cargo test --workspace --all-features
```

Run this when `gpui-component` ships a release that either (a) bumps
`naga` past 27.0.3 to a `codespan-reporting 0.12.0`-compatible version,
or (b) pins `codespan-reporting 0.11.x` in its own `Cargo.toml`.
If both succeed, this deviation is obsolete — delete this G11 entry.

Track upstream state at:

- `https://github.com/gfx-rs/wgpu/issues` (naga is part of wgpu)
- `https://github.com/zed-industries/zed` (gpui-component's upstream)

**Detailed records:**

- `docs/todo_v0.5.7_gaps.md:546-614` (§G11 — the definitive technical analysis of Options A/B/C/D).
- `.planning/phases/93-docs-todo-v0-5-7-gaps-md/93-G11-DEVIATION.md` (Phase-93-cross-plan annotation).
- `.planning/phases/93-docs-todo-v0-5-7-gaps-md/93-VERIFICATION.md` frontmatter `overrides` block (formal acceptance record).

```

**Step 3 — Atomic commit for all seven files.**

After all edits (Task 01's six files + this task's RELEASING.md), create ONE atomic commit:

```
docs(95-03): formalise G11 principled deviation across Phase 93 plans + RELEASING.md

Phase 93-07 (commit a6e8d4e) landed the G11 deviation record in
docs/todo_v0.5.7_gaps.md:546-614 and cross-referenced it from
deferred-items.md. Phase 93-VERIFICATION.md accepted the deviation
via an overrides_applied block on 2026-04-19T18:28:22Z.

However, the five individual Phase 93 PLAN files (93-01 through
93-05) still contain raw --workspace claims in must_haves and
verification command lines. A reader discovering any of those
plans for the first time would not know about the accepted
deviation without cross-referencing multiple other files.

This commit formalises the deviation in three discoverable places:

1. NEW: .planning/phases/93-docs-todo-v0-5-7-gaps-md/93-G11-DEVIATION.md
   — single-source-of-truth cross-plan record listing every affected
   plan line, the Options A/B/C/D summary, the current acceptance
   criterion, and the re-evaluation trigger command.

2. APPEND-ONLY annotation blocks at the END of each of the five
   affected Phase 93 PLAN files (93-01, 93-02, 93-03, 93-04, 93-05),
   clearly labelled "G11 annotation added 2026-04-20 (Phase 95 Plan 03)",
   directing readers to 93-G11-DEVIATION.md and gaps.md §G11.
   Existing --workspace claims above are preserved verbatim.

3. NEW section in RELEASING.md titled "Known upstream tool-chain
   deviations (v0.5.7+)" listing G11 as the active deviation with
   the re-evaluation trigger command. Makes G11 visible to release
   maintainers at their primary-reading surface.

Zero source code, preset, Cargo.toml, or Cargo.lock changes.
Zero rewrites of existing content (APPEND-ONLY discipline).
Closes G11 from docs/todo_v0.5.7_gaps.md — the final v0.5.7
remaining-gaps item.

Files:
- .planning/phases/93-docs-todo-v0-5-7-gaps-md/93-G11-DEVIATION.md (new)
- .planning/phases/93-docs-todo-v0-5-7-gaps-md/93-01-PLAN.md (append)
- .planning/phases/93-docs-todo-v0-5-7-gaps-md/93-02-PLAN.md (append)
- .planning/phases/93-docs-todo-v0-5-7-gaps-md/93-03-PLAN.md (append)
- .planning/phases/93-docs-todo-v0-5-7-gaps-md/93-04-PLAN.md (append)
- .planning/phases/93-docs-todo-v0-5-7-gaps-md/93-05-PLAN.md (append)
- RELEASING.md (insert new section)
```

No `Co-Authored-By` trailer per user memory rule.

**Step 4 — Post-commit verification.**

```
git log -1 --format=%s                              # expected: docs(95-03): formalise G11 principled deviation across Phase 93 plans + RELEASING.md
git log -1 --format=%B | grep -c "Co-Authored-By"   # expected: 0
git log -1 --name-only | tail -10                   # expected: 7 files total (1 new + 6 existing)
grep -c "Known upstream tool-chain deviations" RELEASING.md   # expected: 1
grep -c "G11" RELEASING.md                          # expected: >= 3
grep -c "93-G11-DEVIATION.md" RELEASING.md          # expected: 1
ls -la .planning/phases/93-docs-todo-v0-5-7-gaps-md/93-G11-DEVIATION.md  # expected: file exists
for f in 93-01-PLAN.md 93-02-PLAN.md 93-03-PLAN.md 93-04-PLAN.md 93-05-PLAN.md; do
  echo "--- $f ---"
  grep -c "G11 annotation added 2026-04-20" ".planning/phases/93-docs-todo-v0-5-7-gaps-md/$f"
  git diff HEAD~1 HEAD -- ".planning/phases/93-docs-todo-v0-5-7-gaps-md/$f" | grep -c '^-[^-]'   # expected: 0 per file (APPEND-ONLY)
done
./pre-release-check.sh 2>&1 | tail -3               # expected: green banner
```
  </action>
  <verify>
    <automated>cd /home/tibi/Rust/native-theme && grep -c "Known upstream tool-chain deviations" RELEASING.md && grep -c "G11" RELEASING.md && grep -c "93-G11-DEVIATION.md" RELEASING.md && ls .planning/phases/93-docs-todo-v0-5-7-gaps-md/93-G11-DEVIATION.md && for f in 93-01-PLAN.md 93-02-PLAN.md 93-03-PLAN.md 93-04-PLAN.md 93-05-PLAN.md; do echo "--- $f ---"; grep -c "G11 annotation added 2026-04-20" ".planning/phases/93-docs-todo-v0-5-7-gaps-md/$f"; done && ./pre-release-check.sh 2>&1 | tail -5</automated>
  </verify>
  <done>
`RELEASING.md` has a new section "Known upstream tool-chain deviations (v0.5.7+)" listing G11 as the active deviation with the re-evaluation-trigger command, cross-referencing `docs/todo_v0.5.7_gaps.md:546-614`, `93-G11-DEVIATION.md`, and `93-VERIFICATION.md`'s overrides block. One atomic commit covers all seven files (1 new + 6 existing, APPEND-ONLY on the existing ones) with the template message above and no `Co-Authored-By` trailer. `./pre-release-check.sh` green banner unchanged.
  </done>
</task>

</tasks>

<verification>
**Plan-level verification** — G11 formalised across Phase 93 plans + RELEASING.md:

| Check | Expected | Command |
|-------|----------|---------|
| New DEVIATION record exists | file present | `ls .planning/phases/93-docs-todo-v0-5-7-gaps-md/93-G11-DEVIATION.md` |
| DEVIATION record lists all 5 plans | all 5 IDs match | `grep -cE "93-0[1-5]-PLAN\\.md" .planning/phases/93-docs-todo-v0-5-7-gaps-md/93-G11-DEVIATION.md` — expected >= 5 |
| DEVIATION record has re-evaluation trigger | 1 match | `grep -c "cargo update -p gpui-component" .planning/phases/93-docs-todo-v0-5-7-gaps-md/93-G11-DEVIATION.md` |
| Each Phase 93 PLAN has annotation | 1 per file, 5 total | `for f in 93-01 93-02 93-03 93-04 93-05; do grep -c "G11 annotation added 2026-04-20" ".planning/phases/93-docs-todo-v0-5-7-gaps-md/$f-PLAN.md"; done` |
| Each Phase 93 PLAN cross-refs DEVIATION record | 1 per file | `for f in 93-01 93-02 93-03 93-04 93-05; do grep -c "93-G11-DEVIATION.md" ".planning/phases/93-docs-todo-v0-5-7-gaps-md/$f-PLAN.md"; done` |
| No content deleted from Phase 93 PLAN files | 0 non-header deletions per file | `for f in 93-01 93-02 93-03 93-04 93-05; do git diff HEAD~1 HEAD -- ".planning/phases/93-docs-todo-v0-5-7-gaps-md/$f-PLAN.md" \| grep -c '^-[^-]'; done` — expected 0 each |
| RELEASING.md has new section | 1 match | `grep -c "Known upstream tool-chain deviations" RELEASING.md` |
| RELEASING.md cross-refs gaps.md + DEVIATION record | present | `grep -c "93-G11-DEVIATION.md" RELEASING.md` + `grep -c "todo_v0.5.7_gaps.md:546" RELEASING.md` |
| RELEASING.md existing sections preserved | all present | `grep -cE "^## (Pre-publication checks\\|Publish order\\|Post-bootstrap cleanup\\|Version bumps\\|Tagging)" RELEASING.md` — expected 5 |
| pre-release-check green | banner intact | `./pre-release-check.sh 2>&1 \| tail -3` |
| commit has no Co-Authored-By | 0 matches | `git log -1 --format=%B \| grep -c "Co-Authored-By"` |
| commit covers exactly 7 files | 7 paths | `git log -1 --name-only \| tail -10` — expected 7 file paths (1 new + 6 modified) |

**Negative checks** (must NOT appear):
- No Rust source file is modified.
- No preset TOML is modified.
- No `Cargo.toml` / `Cargo.lock` modification.
- No existing content deletion in any of the five Phase 93 PLAN files (APPEND-ONLY enforced).
- No edit to `deferred-items.md` (Phase 93-07 already handled it; re-editing would violate append-only of the existing Phase 93-07 content).
- No edit to `93-VERIFICATION.md` (the override is ALREADY recorded there; re-editing would violate append-only).
- No edit to `.planning/STATE.md` (the /gsd:execute-phase SUMMARY step handles STATE.md updates).
</verification>

<success_criteria>
- [ ] `.planning/phases/93-docs-todo-v0-5-7-gaps-md/93-G11-DEVIATION.md` exists and contains: (a) the affected-plans table listing 93-01 through 93-05 with exact line anchors, (b) Options A/B/C/D summary with Option D selected, (c) the current acceptance criterion (per-crate gate), (d) the re-evaluation trigger command, (e) cross-references to `docs/todo_v0.5.7_gaps.md:546-614` + `93-VERIFICATION.md` + `deferred-items.md` + `RELEASING.md` + `93-07-PLAN.md` + `93-07-SUMMARY.md`.
- [ ] Each of `93-01-PLAN.md`, `93-02-PLAN.md`, `93-03-PLAN.md`, `93-04-PLAN.md`, `93-05-PLAN.md` has an APPEND-ONLY G11 annotation block at the END (below the existing `</output>` tag), clearly labelled "G11 annotation added 2026-04-20 (Phase 95 Plan 03)", cross-referencing both `93-G11-DEVIATION.md` and `docs/todo_v0.5.7_gaps.md` §G11.
- [ ] Existing content in the five Phase 93 PLAN files (frontmatter, objective, tasks, verification, success_criteria, output) is preserved VERBATIM. Git diff shows zero non-header deletion lines per file.
- [ ] `RELEASING.md` has a new section titled "Known upstream tool-chain deviations (v0.5.7+)" listing G11 as the active deviation, with the re-evaluation-trigger command, and cross-references to `docs/todo_v0.5.7_gaps.md:546-614` + `93-G11-DEVIATION.md` + `93-VERIFICATION.md`.
- [ ] `RELEASING.md`'s existing sections (Pre-publication checks, Publish order, Post-bootstrap cleanup, Version bumps, Tagging) are preserved.
- [ ] One atomic git commit covers exactly 7 files (1 new: `93-G11-DEVIATION.md`; 6 existing: the 5 Phase 93 PLAN files + `RELEASING.md`).
- [ ] Commit message is `docs(95-03): formalise G11 principled deviation across Phase 93 plans + RELEASING.md` with the body template above.
- [ ] No `Co-Authored-By` trailer.
- [ ] `./pre-release-check.sh` green banner unchanged.
- [ ] Zero Rust code, preset, Cargo.toml, or Cargo.lock changes.
</success_criteria>

<output>
After completion, create `.planning/phases/95-the-remaining-g9-g10-g11-from-docs-todo-v0-5-7-gaps-md/95-03-SUMMARY.md`. The SUMMARY must include:
- The exact list of 7 files in the atomic commit with a one-line rationale each.
- Confirmation that all five Phase 93 PLAN files preserved their existing content verbatim (APPEND-ONLY discipline honoured).
- Confirmation that `./pre-release-check.sh` remained green post-commit.
</output>
