---
phase: 02-core-presets
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/presets/default.toml
  - src/presets/kde-breeze.toml
  - src/presets/adwaita.toml
  - src/presets.rs
  - src/lib.rs
autonomous: true
requirements:
  - PRESET-01
  - PRESET-02

must_haves:
  truths:
    - "preset('default') returns a NativeTheme with both light and dark variants populated"
    - "preset('kde-breeze') returns a NativeTheme with KDE Breeze colors in both variants"
    - "preset('adwaita') returns a NativeTheme with GNOME Adwaita colors in both variants"
    - "list_presets() returns ['default', 'kde-breeze', 'adwaita']"
    - "from_toml() parses a valid TOML string into NativeTheme"
    - "from_file() loads a NativeTheme from a .toml file path"
    - "to_toml() serializes a NativeTheme to a valid TOML string"
  artifacts:
    - path: "src/presets/default.toml"
      provides: "Neutral toolkit-agnostic preset with light and dark variants"
      contains: "[light.colors.core]"
    - path: "src/presets/kde-breeze.toml"
      provides: "KDE Breeze preset with official color values"
      contains: "[light.colors.core]"
    - path: "src/presets/adwaita.toml"
      provides: "GNOME Adwaita preset with libadwaita CSS color values"
      contains: "[light.colors.core]"
    - path: "src/presets.rs"
      provides: "Preset registry module with 5 public API functions"
      exports: ["preset", "list_presets", "from_toml", "from_file", "to_toml"]
    - path: "src/lib.rs"
      provides: "Re-exports preset API functions at crate root"
      contains: "pub use presets"
  key_links:
    - from: "src/presets.rs"
      to: "src/presets/*.toml"
      via: "include_str!() compile-time embedding"
      pattern: "include_str!"
    - from: "src/presets.rs"
      to: "src/error.rs"
      via: "Error::Unavailable for unknown preset, Error::Format via From<toml::de::Error>"
      pattern: "Error::Unavailable"
    - from: "src/lib.rs"
      to: "src/presets.rs"
      via: "pub mod presets + pub use re-exports"
      pattern: "pub use presets"
---

<objective>
Create three bundled theme preset TOML files (default, kde-breeze, adwaita) with accurate color values for both light and dark variants, implement the preset loading API module with five public functions, and wire everything into lib.rs with crate-root re-exports.

Purpose: This is the core deliverable of Phase 2 -- users get bundled presets they can load by name and a complete TOML serialization API, all without any platform feature flags.

Output: Three TOML preset files under src/presets/, a src/presets.rs module with preset()/list_presets()/from_toml()/from_file()/to_toml(), and updated lib.rs with re-exports.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-presets/02-RESEARCH.md
@.planning/phases/01-data-model-foundation/01-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts from Phase 1 that this plan builds on. -->

From src/lib.rs:
```rust
pub type Result<T> = std::result::Result<T, Error>;
// Re-exports: NativeTheme, ThemeVariant, ThemeColors, CoreColors, ActionColors,
// StatusColors, InteractiveColors, PanelColors, ComponentColors,
// ThemeFonts, ThemeGeometry, ThemeSpacing, Rgba, Error
```

From src/error.rs:
```rust
pub enum Error {
    Unsupported,
    Unavailable(String),
    Format(String),
    Platform(Box<dyn std::error::Error + Send + Sync>),
}
// Has: From<toml::de::Error>, From<toml::ser::Error>, From<std::io::Error>
```

From src/model/mod.rs:
```rust
pub struct NativeTheme {
    pub name: String,
    pub light: Option<ThemeVariant>,
    pub dark: Option<ThemeVariant>,
}
pub struct ThemeVariant {
    pub colors: ThemeColors,
    pub fonts: ThemeFonts,
    pub geometry: ThemeGeometry,
    pub spacing: ThemeSpacing,
}
```

TOML section naming (must match struct field names exactly):
- Top level: name, [light], [dark]
- Under variant: [*.colors.core], [*.colors.primary], [*.colors.secondary], [*.colors.status], [*.colors.interactive], [*.colors.panel], [*.colors.component]
- Under variant: [*.fonts], [*.geometry], [*.spacing]

Color field names per sub-struct:
- CoreColors: accent, background, foreground, surface, border, muted, shadow
- ActionColors (primary/secondary): background, foreground
- StatusColors: danger, danger_foreground, warning, warning_foreground, success, success_foreground, info, info_foreground
- InteractiveColors: selection, selection_foreground, link, focus_ring
- PanelColors: sidebar, sidebar_foreground, tooltip, tooltip_foreground, popover, popover_foreground
- ComponentColors: button, button_foreground, input, input_foreground, disabled, separator, alternate_row

ThemeFonts: family, size, mono_family, mono_size
ThemeGeometry: radius, frame_width, disabled_opacity, border_opacity, scroll_width
ThemeSpacing: xxs, xs, s, m, l, xl, xxl
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create three TOML preset files with accurate platform colors</name>
  <files>src/presets/default.toml, src/presets/kde-breeze.toml, src/presets/adwaita.toml</files>
  <action>
Create the `src/presets/` directory and three TOML preset files. Each file must have `name`, `[light]` and `[dark]` sections with all color sub-structs, fonts, geometry, and spacing populated. TOML section headers MUST match the struct field names exactly (e.g., `[light.colors.core]` not `[light.colors.core_colors]`).

**src/presets/default.toml** -- Neutral, toolkit-agnostic theme:
- Name: "Default"
- Accent: medium blue (#4a90d9)
- Light: near-white backgrounds (#fafafa bg, #ffffff surface), dark gray foreground (#2e3436)
- Dark: dark gray backgrounds (#1e1e1e bg, #2d2d2d surface), near-white foreground (#f0f0f0)
- Fonts: family = "sans-serif", size = 10.0, mono_family = "monospace", mono_size = 10.0
- Geometry: radius = 6.0, frame_width = 1.0, disabled_opacity = 0.5, border_opacity = 0.15, scroll_width = 8.0
- Spacing: xxs = 2.0, xs = 4.0, s = 6.0, m = 12.0, l = 18.0, xl = 24.0, xxl = 36.0
- Fill ALL 36 color roles for both variants with reasonable neutral values. Do NOT leave any color sub-struct empty.

**src/presets/kde-breeze.toml** -- KDE Breeze colors from official KDE/breeze repository:
- Name: "KDE Breeze"
- Use the exact hex values from the research color reference tables (e.g., light accent = #3daee9, light bg = #eff0f1, light fg = #232629, etc.)
- Dark variant from BreezeDark.colors (e.g., dark bg = #232629, dark fg = #fcfcfc, dark surface = #1b1e20, etc.)
- Fonts: family = "Noto Sans", size = 10.0, mono_family = "Hack", mono_size = 10.0
- Geometry: radius = 4.0, frame_width = 1.0, disabled_opacity = 0.5, border_opacity = 0.2, scroll_width = 10.0
- Spacing: xxs = 2.0, xs = 4.0, s = 6.0, m = 8.0, l = 12.0, xl = 16.0, xxl = 24.0

**src/presets/adwaita.toml** -- GNOME Adwaita colors from libadwaita CSS variables:
- Name: "Adwaita"
- Light accent = #3584e4, light bg = #fafafb, light foreground = #2e3436 (pre-computed from Adwaita's rgba foreground, using traditional GTK value)
- Dark accent = #3584e4, dark bg = #222226 (official libadwaita dark_1), dark fg = #ffffff
- Pre-compute any alpha/opacity colors to solid hex values (see Pitfall 6 in research)
- Fonts: family = "Adwaita Sans", size = 11.0, mono_family = "Adwaita Mono", mono_size = 11.0
- Geometry: radius = 12.0, frame_width = 1.0, disabled_opacity = 0.5, border_opacity = 0.15, scroll_width = 8.0
- Spacing: xxs = 2.0, xs = 4.0, s = 6.0, m = 12.0, l = 18.0, xl = 24.0, xxl = 36.0

Hex colors MUST use lowercase letters and include the leading # (e.g., "#3daee9" not "3DAEE9"). Shadow and similar translucent colors use #RRGGBBAA format (e.g., "#00000040").
  </action>
  <verify>
    <automated>cd /home/tibi/Rust/native-theme && python3 -c "
import tomllib, pathlib, sys
for f in ['src/presets/default.toml','src/presets/kde-breeze.toml','src/presets/adwaita.toml']:
    p = pathlib.Path(f)
    if not p.exists(): sys.exit(f'{f} missing')
    d = tomllib.loads(p.read_text())
    assert 'name' in d, f'{f}: no name'
    assert 'light' in d, f'{f}: no light'
    assert 'dark' in d, f'{f}: no dark'
    for v in ['light','dark']:
        assert 'colors' in d[v], f'{f}: {v} missing colors'
        assert 'core' in d[v]['colors'], f'{f}: {v} missing colors.core'
        assert 'fonts' in d[v], f'{f}: {v} missing fonts'
        assert 'geometry' in d[v], f'{f}: {v} missing geometry'
        assert 'spacing' in d[v], f'{f}: {v} missing spacing'
print('All 3 TOML files valid with required structure')
"</automated>
  </verify>
  <done>Three TOML preset files exist under src/presets/ with name, light and dark variants each containing all color sub-structs (core, primary, secondary, status, interactive, panel, component), fonts, geometry, and spacing fully populated with accurate platform-specific values.</done>
</task>

<task type="auto">
  <name>Task 2: Create presets.rs module with API functions and wire into lib.rs</name>
  <files>src/presets.rs, src/lib.rs</files>
  <action>
Create `src/presets.rs` with the following structure (matching the research architecture pattern exactly):

1. Three `const` bindings using `include_str!()` to embed the TOML files:
   - `const DEFAULT_TOML: &str = include_str!("presets/default.toml");`
   - `const KDE_BREEZE_TOML: &str = include_str!("presets/kde-breeze.toml");`
   - `const ADWAITA_TOML: &str = include_str!("presets/adwaita.toml");`

2. A `const PRESET_NAMES: &[&str]` array listing all preset names: `["default", "kde-breeze", "adwaita"]`

3. Five public functions:
   - `pub fn preset(name: &str) -> Result<NativeTheme>` -- match on name, call from_toml on the embedded string, return Error::Unavailable for unknown names
   - `pub fn list_presets() -> &'static [&'static str]` -- return PRESET_NAMES
   - `pub fn from_toml(toml_str: &str) -> Result<NativeTheme>` -- deserialize via toml::from_str, use ? for error conversion
   - `pub fn from_file(path: impl AsRef<Path>) -> Result<NativeTheme>` -- read file with std::fs::read_to_string, then call from_toml
   - `pub fn to_toml(theme: &NativeTheme) -> Result<String>` -- serialize via toml::to_string_pretty, use ? for error conversion

4. Add a `#[cfg(test)] mod tests` block with unit tests:
   - Test that each preset name in list_presets() is loadable via preset()
   - Test that preset() with unknown name returns Error::Unavailable
   - Test from_toml() with a minimal valid TOML string
   - Test to_toml() produces valid TOML that round-trips
   - Test from_file() with a tempfile (use std::io::Write + tempfile pattern, or write to a temp path and clean up)

5. Update `src/lib.rs`:
   - Add `pub mod presets;` after the existing module declarations
   - Add re-exports: `pub use presets::{from_file, from_toml, list_presets, preset, to_toml};`

Use `crate::{Error, NativeTheme, Result}` imports. Use `std::path::Path` for from_file. Follow the exact function signatures from the research. Add doc comments with `///` on all public functions matching the research examples. Do NOT use lazy_static, OnceLock, or HashMap -- match statement is correct for 3 entries.
  </action>
  <verify>
    <automated>cd /home/tibi/Rust/native-theme && cargo test 2>&1</automated>
  </verify>
  <done>src/presets.rs exists with 5 public functions and include_str!() embeddings. lib.rs has `pub mod presets` and re-exports all 5 functions. `cargo test` passes (all existing tests + new preset unit tests). `cargo doc` generates docs for the new module without warnings.</done>
</task>

</tasks>

<verification>
```bash
# All tests pass (existing + new)
cargo test

# Presets module compiles and is accessible from crate root
cargo test --lib presets::tests

# Doc generation works without warnings
cargo doc --no-deps 2>&1 | grep -c "warning" | grep "^0$"

# Verify re-exports work -- these should be importable from crate root
echo 'use native_theme::{preset, list_presets, from_toml, from_file, to_toml};' > /tmp/check_imports.rs && echo "Re-exports verified"
```
</verification>

<success_criteria>
- Three TOML preset files exist with complete, accurate color values for both variants
- src/presets.rs implements all 5 API functions with proper error handling
- lib.rs re-exports all 5 functions at crate root
- cargo test passes with zero failures
- preset("default"), preset("kde-breeze"), preset("adwaita") each return valid NativeTheme with light and dark variants
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-presets/02-01-SUMMARY.md`
</output>
