---
phase: 81-feature-matrix-cleanup-and-unified-from-system
plan: 02
type: execute
wave: 2
depends_on: [81-01]
files_modified:
  - .github/workflows/ci.yml
  - native-theme/src/lib.rs
autonomous: true
requirements: [FEATURE-03]
must_haves:
  truths:
    - "CI matrix covers every feature combination that matters: no-features, kde-only, portal-only, linux, linux-kde, linux-portal, native, icons, plus macOS and Windows"
    - "No CI matrix entry references portal-tokio, portal-async-io, linux-async-io, or native-async-io"
    - "A sync-only consumer can call from_system() with only the kde feature without pulling any async runtime"
    - "All workspace crates compile and pass tests"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Updated CI test matrix without removed features"
      contains: "linux-kde"
    - path: "native-theme/src/lib.rs"
      provides: "Compile-time sync-consumer verification test"
      contains: "sync_consumer_no_async_runtime"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "native-theme/Cargo.toml"
      via: "CI matrix feature flags must match available Cargo features"
      pattern: "features.*linux-kde"
---

<objective>
Update CI matrix for the new feature graph and verify atomic correctness across all feature combinations.

Purpose: Ensure the feature-matrix cleanup (FEATURE-03) is fully verified -- CI catches any broken feature combination, and a compile-time test proves sync-only consumers don't pull an async runtime. This is the verification half of the atomic ship unit.

Output: Updated CI workflow, sync-consumer verification test, full workspace compilation proof.
</objective>

<execution_context>
@/home/tibi/Rust/native-theme/.claude/get-shit-done/workflows/execute-plan.md
@/home/tibi/Rust/native-theme/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/81-feature-matrix-cleanup-and-unified-from-system/81-01-SUMMARY.md
@.github/workflows/ci.yml
@native-theme/Cargo.toml

<interfaces>
<!-- Post-Plan-01 feature graph (from Cargo.toml after Plan 01): -->
```toml
kde = ["dep:configparser"]
portal = ["dep:ashpd", "ashpd/async-io", "dep:pollster"]
windows = ["dep:windows"]
macos = [...]
linux-kde = ["kde"]
linux-portal = ["portal"]
linux = ["linux-kde", "linux-portal"]
native = ["linux", "macos", "windows"]
watch = ["dep:notify", "dep:zbus"]
material-icons = []
lucide-icons = []
svg-rasterize = ["dep:resvg"]
system-icons = [...]
```

<!-- Current CI matrix entries that reference removed features: -->
```yaml
# These entries must be updated:
- { name: "Linux (portal-tokio)", flags: "--features portal-tokio" }      # REMOVE
- { name: "Linux (portal-async-io)", flags: "--features portal-async-io" }  # REMOVE
- { name: "Linux (linux-async-io)", flags: "--features linux-async-io" }    # REMOVE
- { name: "Linux (native-async-io)", flags: "--features native-async-io" }  # REMOVE
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CI matrix and add new feature combination entries</name>
  <files>.github/workflows/ci.yml</files>
  <action>
  Update the `test` job matrix in `.github/workflows/ci.yml`.

  Replace the current matrix entries with this updated set:

  ```yaml
  matrix:
    include:
      - { name: "Linux (no features)", os: ubuntu-latest, flags: "--no-default-features" }
      - { name: "Linux (kde)", os: ubuntu-latest, flags: "--features kde" }
      - { name: "Linux (portal)", os: ubuntu-latest, flags: "--features portal" }
      - { name: "Linux (linux-kde)", os: ubuntu-latest, flags: "--features linux-kde" }
      - { name: "Linux (linux-portal)", os: ubuntu-latest, flags: "--features linux-portal" }
      - { name: "Linux (linux)", os: ubuntu-latest, flags: "--features linux" }
      - { name: "Linux (native)", os: ubuntu-latest, flags: "--features native" }
      - { name: "Linux (icons)", os: ubuntu-latest, flags: "--features material-icons,lucide-icons,system-icons,svg-rasterize" }
      - { name: "Windows (no features)", os: windows-latest, flags: "--no-default-features" }
      - { name: "Windows (windows)", os: windows-latest, flags: "--features windows" }
      - { name: "macOS (no features)", os: macos-latest, flags: "--no-default-features" }
      - { name: "macOS (macos)", os: macos-latest, flags: "--features macos" }
  ```

  Changes from current:
  - REMOVED: "Linux (portal-tokio)" -- feature no longer exists
  - REMOVED: "Linux (portal-async-io)" -- feature no longer exists
  - REMOVED: "Linux (linux-async-io)" -- feature no longer exists
  - REMOVED: "Linux (native-async-io)" -- feature no longer exists
  - ADDED: "Linux (portal)" -- tests base portal feature (was only testable via portal-tokio before)
  - ADDED: "Linux (linux-kde)" -- tests new sub-aggregator
  - ADDED: "Linux (linux-portal)" -- tests new sub-aggregator
  - ADDED: "Linux (linux)" -- tests full linux aggregator (was implicitly tested via linux-async-io before)
  - ADDED: "Linux (native)" -- tests the cross-platform aggregator on Linux

  Total: 12 entries (was 11). Net gain of 1 entry but covers more meaningful combinations.
  </action>
  <verify>
    <automated>cd /home/tibi/Rust/native-theme && python3 -c "
import yaml, sys
with open('.github/workflows/ci.yml') as f:
    ci = yaml.safe_load(f)
matrix = ci['jobs']['test']['strategy']['matrix']['include']
names = [e['name'] for e in matrix]
# Verify removed features are gone
for bad in ['portal-tokio', 'portal-async-io', 'linux-async-io', 'native-async-io']:
    for n in names:
        if bad in n:
            print(f'FAIL: found removed feature in CI: {n}')
            sys.exit(1)
# Verify new entries exist
for required in ['linux-kde', 'linux-portal', 'Linux (linux)', 'Linux (portal)', 'Linux (native)']:
    found = any(required in n for n in names)
    if not found:
        print(f'FAIL: missing required entry: {required}')
        sys.exit(1)
print(f'OK: {len(matrix)} matrix entries, all checks pass')
print('Entries:', names)
" 2>&1</automated>
  </verify>
  <done>CI matrix has 12 entries covering no-features, kde, portal, linux-kde, linux-portal, linux, native, icons, Windows, macOS. No references to removed features.</done>
</task>

<task type="auto">
  <name>Task 2: Add sync-consumer compile-time verification and run full workspace check</name>
  <files>native-theme/src/lib.rs</files>
  <action>
  1. Add a compile-time test in the `system_theme_tests` module in `lib.rs` that proves a sync-only consumer path works. This test verifies SC-4 (sync consumer does not need an async runtime):

  ```rust
  /// Prove that the sync API compiles and works without any async runtime.
  /// This test runs on every `cargo test` invocation, acting as a compile-time
  /// gate that the sync path doesn't accidentally gain an async runtime dependency.
  #[test]
  fn sync_consumer_no_async_runtime() {
      // Build a SystemTheme via the pure pipeline (same path from_system uses).
      // If this compiles and runs, the sync API works without tokio/async-std.
      let reader = Theme::preset("adwaita").unwrap();
      let result = pipeline::run_pipeline(
          reader,
          "adwaita-live",
          ColorMode::Light,
          AccessibilityPreferences::default(),
          None,
      );
      assert!(result.is_ok(), "sync pipeline should succeed without async runtime");
  }
  ```

  Place this test inside the existing `system_theme_tests` module.

  2. Run the full verification suite:
  - `cargo check -p native-theme --no-default-features` (zero features)
  - `cargo check -p native-theme --features kde` (KDE only, no portal/async)
  - `cargo check -p native-theme --features portal` (portal with async-io + pollster)
  - `cargo check -p native-theme --features linux-kde` (new sub-aggregator)
  - `cargo check -p native-theme --features linux-portal` (new sub-aggregator)
  - `cargo check -p native-theme --features linux` (full linux)
  - `cargo check -p native-theme --features native` (all platforms)
  - `cargo test -p native-theme --features linux` (run tests)
  - `cargo test -p native-theme --no-default-features` (no features test)
  - `cargo check -p native-theme-gpui` (connector compiles)
  - `cargo check -p native-theme-iced` (connector compiles)
  - `cargo clippy -p native-theme --all-targets -- -D warnings` (no warnings)
  - `cargo fmt --all --check` (formatting)

  3. Run `./pre-release-check.sh` to verify the full workspace is clean (panic checks, clippy, tests, doc build).

  If any check fails, fix the issue before proceeding. Common issues:
  - Dead code warnings from removed `from_linux` references -- remove any stale imports
  - Clippy warnings about unused cfg attributes -- clean up
  - Feature gate errors -- verify cfg attributes match new feature names
  </action>
  <verify>
    <automated>cd /home/tibi/Rust/native-theme && cargo test -p native-theme --features linux -- sync_consumer_no_async_runtime --exact 2>&1 | tail -5 && cargo clippy -p native-theme --all-targets -- -D warnings 2>&1 | tail -5 && cargo check -p native-theme-gpui 2>&1 | tail -3 && cargo check -p native-theme-iced 2>&1 | tail -3</automated>
  </verify>
  <done>sync_consumer_no_async_runtime test passes, all feature combinations compile, clippy clean, both connectors compile, pre-release-check.sh passes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI matrix completeness | Missing feature combos could ship broken builds |
| Feature unification | Cargo unifies features across dependencies; wrong feature combos could pull unexpected deps |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-81-04 | T (Tampering) | CI coverage gap | mitigate | CI matrix explicitly tests all 7 Linux feature combos + 2 Windows + 2 macOS + 1 icons |
| T-81-05 | D (Denial of Service) | feature unification pulling tokio | mitigate | sync_consumer_no_async_runtime test + --no-default-features check proves no tokio in sync path |
</threat_model>

<verification>
1. CI matrix YAML parses correctly and contains no removed feature names
2. `sync_consumer_no_async_runtime` test passes in all feature configurations
3. `cargo check` passes for all 7 feature combinations on Linux
4. `cargo clippy` reports zero warnings
5. Both connector crates compile
6. `pre-release-check.sh` passes (fmt, clippy, tests, docs, panic check)
</verification>

<success_criteria>
- CI matrix has entries for: no-features, kde, portal, linux-kde, linux-portal, linux, native, icons, Windows, macOS (12 total)
- No CI entry references portal-tokio, portal-async-io, linux-async-io, or native-async-io
- sync_consumer_no_async_runtime test exists and passes
- Full workspace compiles and passes tests
- pre-release-check.sh exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/81-feature-matrix-cleanup-and-unified-from-system/81-02-SUMMARY.md`
</output>
